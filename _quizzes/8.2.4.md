# Phase 1 · 8.2.4 Dimensional modeling — 150 self-test questions

Q: What is dimensional modeling and what problem does it solve?
A: Dimensional modeling is a design technique that structures data into fact tables (measurements of a business process) and dimension tables (the descriptive context for those measurements), optimized for query understandability and analytical performance rather than transactional write efficiency. It solves the problem that a normalized OLTP schema is hard for analysts to navigate and slow to aggregate, by deliberately denormalizing context into wide, human-readable dimensions. In a fund mart you would model NAV measurements as a fact and fund, share class, investor, and date as dimensions.

Q: What is the "grain" of a fact table?
A: The grain is the precise definition of what a single row in the fact table represents — the level of detail captured by one measurement event. Declaring the grain in one plain sentence ("one row per fund per share class per valuation date") fixes the meaning of every subsequent column choice. Kimball's first rule is that you declare the grain before you list a single dimension or fact column, because every other modeling decision follows from it.

Q: Why does Kimball insist you declare the grain first, before anything else?
A: Because the grain determines which dimensions can attach (only dimensions consistent with that level of detail belong) and what each fact column can mean, so deciding it first prevents incoherent designs. If you pick columns first and infer the grain later, you typically end up with a mixed-grain table where some rows are daily and some are monthly, which silently breaks aggregations. Declaring "one row per X per Y per date" up front makes the table reviewable and the SUMs trustworthy.

Q: Give the grain of a NAV fact table as a one-sentence declaration.
A: "One row per fund, per share class, per valuation date." This says the table captures the official net asset value snapshot at the most atomic reporting level a fund administrator strikes prices at. Any measure on that row — NAV per share, total net assets, shares outstanding — must be true at exactly that fund/share-class/date intersection, and nothing finer or coarser may sneak in.

Q: What is the difference between a fact table and a dimension table?
A: A fact table stores the numeric measurements of a business process (the "verbs" — amounts, counts, prices) plus foreign keys to dimensions, and it is typically long and narrow with many rows. A dimension table stores the descriptive textual context (the "nouns" — who, what, where, which) used to filter and group, and it is typically short and wide with many attribute columns. Facts are what you aggregate; dimensions are what you slice and label by.

Q: What are the three fundamental fact table types in Kimball's taxonomy?
A: Transaction fact tables, periodic snapshot fact tables, and accumulating snapshot fact tables. A transaction fact records one row per discrete event as it happens; a periodic snapshot records one row per entity per regular time interval; an accumulating snapshot records one row per workflow instance and updates it as the instance moves through milestones. Every fact table you design should be one of these three — mixing them is a design smell.

Q: What is a transaction fact table?
A: A transaction fact table has one row for each measurement event at the moment it occurs, such as a single subscription order or a single fee charge. It is the most atomic and flexible grain, capturing the full detail of "something happened" and supporting the widest range of analyses. Its measures are usually fully additive across all dimensions, and rows are inserted but rarely updated.

Q: What is a periodic snapshot fact table?
A: A periodic snapshot fact table captures the state of a process at regular, predictable intervals — one row per entity per period — even when nothing changed in that period. It answers "what was the state at end of day/month?" rather than "what events occurred?". A daily NAV table is the canonical example: one row per fund/share class for every valuation date, regardless of whether the price moved.

Q: Why is a daily NAV table a periodic snapshot rather than a transaction fact?
A: Because NAV is not an event that occurs at a random moment — it is a measured state of the fund struck on a regular valuation cadence, so you record one row per fund/share class per valuation date by design. You want a row to exist for every valuation date so a balance is always available for any reporting date, which is the defining behavior of a snapshot, not a transaction log. The measures (NAV per share, total net assets) are point-in-time balances, the hallmark of snapshot facts.

Q: What is an accumulating snapshot fact table?
A: An accumulating snapshot has one row per instance of a multi-step workflow, and that single row is repeatedly updated as the instance passes each milestone, with multiple date foreign keys (one per milestone) and lag/duration measures. It is built for pipeline processes with a definite beginning and end. A fund subscription lifecycle — order received, traded, settled, units allocated — fits this pattern, with each date column filled in as the order progresses.

Q: A measure is "additive". What does that mean and why does it matter?
A: An additive measure can be summed correctly across every dimension in the fact table, including time — for example, fee amounts can be added across funds, investors, and months. It matters because additivity is what lets a BI tool roll a fact up along any combination of dimensions without producing nonsense. The richest, easiest-to-use facts are fully additive, which is one reason transaction-grain amounts are so valuable.

Q: What is a semi-additive measure? Give a fund example.
A: A semi-additive measure can be summed across some dimensions but not across time, because it represents a balance or level rather than a flow. Total net assets and shares outstanding in a NAV snapshot are semi-additive: you can add net assets across share classes on a given date, but adding a fund's net assets across 30 days is meaningless double counting. The correct time aggregation for a balance is usually an average or an end-of-period value, not a SUM.

Q: What is a non-additive measure and how do you handle it in a fact table?
A: A non-additive measure cannot be summed across any dimension, including across rows, because it is a ratio, percentage, or unit rate — NAV per share and expense ratio are examples. The handling rule is to store the fully additive components (e.g. total net assets and shares outstanding) as facts and let the BI layer compute the ratio after aggregation, rather than storing or summing the ratio directly. Summing NAV-per-share across funds is a classic error this rule prevents.

Q: Why is summing "NAV per share" across funds wrong, and what should you store instead?
A: NAV per share is a ratio (total net assets divided by shares outstanding), and ratios are non-additive — adding per-share prices across funds produces a number with no meaning. You should store the additive numerator (total net assets) and additive denominator (shares outstanding) as facts, then divide the aggregated numerator by the aggregated denominator at query time. This "aggregate then divide" discipline keeps the ratio correct at every grain.

Q: What is a conformed dimension?
A: A conformed dimension is a dimension table that means exactly the same thing — same keys, same attributes, same values — when shared across multiple fact tables and business processes. A conformed `dim_fund` lets you join NAV facts, fee facts, and order facts and trust that "Fund A" is the identical entity in all of them. Conformance is what makes a collection of marts behave like one integrated warehouse rather than disconnected silos.

Q: What does it mean for two dimensions to be conformed, technically?
A: Two dimensions conform when they are either identical, or one is a strict subset (the same keys and a shared subset of attributes at a rolled-up grain) of the other. A monthly summary mart can use a month-grain date dimension that conforms to the daily date dimension as long as the shared attributes match exactly. The test is that a label or attribute used to constrain one fact yields the same result when used against another.

Q: What is the enterprise bus matrix?
A: The bus matrix is a grid with business processes as rows and conformed dimensions as columns, where each cell is marked when that dimension applies to that process's fact table. It is the master planning and communication artifact of a Kimball warehouse, showing at a glance which marts share which dimensions and in what order to build them. For a fund administrator the rows would be orders, NAV calculation, fee accrual, and transfer agency, with columns for fund, share class, investor, and date.

Q: How do you read a row versus a column of the bus matrix?
A: A row shows all the conformed dimensions that attach to one business process — effectively the dimensional footprint of that fact table. A column shows every business process that uses a given conformed dimension — revealing where that dimension must be reused identically. Columns with many marks are your highest-value conformed dimensions, because building them once correctly pays off across many marts.

Q: For a fund administrator, name the four processes and four conformed dimensions you'd put on the bus matrix.
A: Processes (rows): subscription/redemption orders, NAV calculation, fee accrual, and transfer-agency activity. Conformed dimensions (columns): fund, share class, investor, and date. Each process gets its own fact table at its own grain, but all four reference the same four shared dimensions so the marts join cleanly — for example, fee facts and NAV facts both hang off the identical `dim_fund` and `dim_date`.

Q: Why is the bus matrix considered an architecture artifact and not just documentation?
A: Because it commits the team to a shared set of conformed dimensions before any mart is built, it enforces integration architecturally rather than hoping silos align later. It also sequences delivery: you can build one process's mart at a time (incremental, agile) while guaranteeing each new mart plugs into the same bus of dimensions. In a regulated shop it doubles as a governance map showing which reference data must be mastered once and reused everywhere.

Q: What is a slowly changing dimension (SCD)?
A: An SCD is a dimension whose descriptive attributes change occasionally over time — like a fund's domicile, name, or investment manager — and the term names the strategies for handling those changes in the dimension table. The core question is whether to overwrite history, preserve it, or something in between. Kimball numbers the standard responses (Type 0 through Type 7), with Types 1, 2, and 3 being the most common.

Q: Describe an SCD Type 1 change.
A: Type 1 overwrites the old attribute value with the new one in place, keeping no history — the dimension row simply reflects the current truth. It is appropriate for corrections (a misspelled fund name) or attributes where history genuinely does not matter. The gotcha is that any prior report rerun will now show the new value against old facts, because the past version is gone forever.

Q: Describe an SCD Type 2 change and the columns it requires.
A: Type 2 preserves full history by inserting a new dimension row each time a tracked attribute changes, so the entity has multiple rows over time distinguished by a surrogate key. It requires bookkeeping columns: a `valid_from` date, a `valid_to` date (or null/high-date for the open row), and usually a boolean `is_current` flag. Facts join to whichever row was effective at the fact's date, giving point-in-time-correct context.

Q: Describe an SCD Type 3 change.
A: Type 3 adds an extra column to hold a single prior value — for example `current_domicile` plus `previous_domicile` — so you can report along either the new or the old categorization without a full history. It is used when only the last change matters and you need to compare "as-is" versus "as-was" side by side. It cannot track an unlimited number of changes the way Type 2 can; it remembers only one prior state.

Q: What is an SCD Type 6 and where does its name come from?
A: Type 6 is a hybrid combining Types 1, 2, and 3 (1 + 2 + 3 = 6, hence the name), giving you Type 2 history rows plus a Type 1 "current" attribute column on every row, so you can report point-in-time-correct or always-current from the same dimension. Each historical row carries both the value that was effective then and a column overwritten with the latest value. It lets one query pick "as-was" or "as-is" simply by choosing which column to group on.

Q: Which SCD type is the default for fund and security reference data, and why?
A: SCD Type 2 is the default for fund and security reference data, because regulated reporting must be point-in-time correct: a NAV struck last March must be reportable against the fund's attributes as they stood last March, not as they stand today. Type 2's history rows with `valid_from`/`valid_to` make "as-of" reporting possible, which audit and regulators expect. Overwriting (Type 1) would silently rewrite history and break reconciliations.

Q: What is the difference between a natural (business) key and a surrogate key in a dimension?
A: A natural key is the identifier the business uses (an ISIN for a share class, an LEI for a legal entity), while a surrogate key is a meaningless integer the warehouse generates as the dimension's primary key. Facts join on the surrogate key, not the natural key. The surrogate is essential for SCD Type 2 because the same natural key (ISIN) appears on multiple history rows, so only an independent surrogate can uniquely identify each version.

Q: Why can't you use the ISIN directly as the primary key of an SCD2 fund-share-class dimension?
A: Because under SCD Type 2 the same ISIN appears on several rows — one per version of the share class over time — so the ISIN is no longer unique and cannot be the primary key. You instead generate a surrogate integer key that is unique per version, store the ISIN as a natural-key attribute, and have facts reference the surrogate. The ISIN remains indexed so you can look up the right version for a given date.

Q: In an SCD2 dimension, what role do `valid_from` and `valid_to` play?
A: They define the effective-date window during which that particular version of the row was the current truth: the row applies to any business date `>= valid_from` and `< valid_to` (or `<= valid_to` depending on convention — be consistent). They are the mechanism for point-in-time joins: given a fact date, you select the dimension row whose window contains it. The currently active row typically has `valid_to` set to null or a far-future sentinel like `9999-12-31`.

Q: What is the `is_current` flag in an SCD2 dimension and why keep it if you already have `valid_to`?
A: `is_current` is a boolean marking the one active version of each natural key, true only for the row whose effective window is still open. It is redundant with `valid_to` but kept as a convenience and performance shortcut: queries that only need today's value filter `WHERE is_current = true` instead of comparing dates against a sentinel. The gotcha is that the flag and the dates must be maintained atomically so they never disagree.

Q: Write the join logic for a point-in-time query against an SCD2 dimension.
A: You join the fact to the dimension on the natural key AND a date-band predicate, for example `ON f.isin = d.isin AND f.valuation_date >= d.valid_from AND f.valuation_date < d.valid_to`. This picks exactly the dimension version that was effective on the fact's date. For "as-of today" reporting you instead join on the surrogate key the fact already carries, or filter the dimension to `is_current = true`.

Q: A point-in-time query returns two dimension rows for one fact. What went wrong?
A: The SCD2 effective-date windows overlap — two versions of the same natural key both claim the fact's date — which usually means a load bug left `valid_to` of the old row not closed when the new row opened. The fix is to ensure that when a new version is inserted, the prior row's `valid_to` is set to the new row's `valid_from` so the bands are contiguous and non-overlapping. Half-open intervals (`>= valid_from AND < valid_to`) avoid the boundary double-match.

Q: What is a junk dimension?
A: A junk dimension is a single dimension table that consolidates several low-cardinality flags and indicators (yes/no flags, small codes) that would otherwise clutter the fact table or spawn many tiny dimensions. It holds one row for each observed combination of those flag values, and the fact carries one foreign key to it. For example, an order fact's "is_first_subscription", "is_in_specie", and "channel" flags can be folded into one `dim_order_flags`.

Q: Why use a junk dimension instead of leaving the flags on the fact table?
A: Because scattering many narrow flag columns across a huge fact table wastes width and makes the model noisy, while a junk dimension keeps the fact narrow (one FK) and centralizes the flags where they can carry descriptive labels. It also lets you enumerate and constrain on combinations cleanly. You store only the combinations that actually occur, so a junk dimension of a handful of flags stays small.

Q: What is a degenerate dimension?
A: A degenerate dimension is a dimension key that lives directly in the fact table with no corresponding dimension table, because it has no descriptive attributes of its own — typically an operational transaction identifier. An order number or a SWIFT message reference on an order fact is a degenerate dimension: useful for grouping line items of the same transaction or for drill-back, but there is nothing else to describe about it. It is left as a column on the fact, not modeled as a separate table.

Q: Where does an order number belong in a dimensional model, and what is it called?
A: It belongs as a column directly on the fact table and is called a degenerate dimension, because the order number identifies a transaction but has no attributes worth putting in a separate dimension table. It still functions as a dimension — you can group all fact rows sharing one order number — but building a one-column `dim_order_number` table would add a pointless join. Keeping it inline preserves the operational drill-back without overhead.

Q: How do junk and degenerate dimensions differ?
A: A junk dimension is a real, separate table that bundles several unrelated low-cardinality flags/indicators, referenced from the fact by a foreign key. A degenerate dimension is a single operational identifier (like an order number) stored inline on the fact with no separate table because it has no descriptive attributes. One reduces fact-table width by collecting flags into a table; the other accepts a key on the fact precisely because there is nothing to collect.

Q: What is the difference between a star schema and a snowflake schema?
A: In a star schema each dimension is a single flat, denormalized table joined directly to the fact, producing the characteristic star shape. In a snowflake schema, dimensions are normalized into multiple related tables (a dimension referencing sub-dimension lookup tables), so a single logical dimension spans several joined tables. The star trades storage redundancy for query simplicity and speed; the snowflake trades those away for normalization.

Q: Why does Kimball consider snowflaking usually a modeling smell in marts?
A: Because snowflaking re-normalizes dimensions back toward an OLTP shape, adding joins that make the model harder for users and BI tools to navigate and often slower, for a storage saving that is negligible against the fact table's size. The dimensional method deliberately denormalizes dimensions for usability and performance, so snowflaking usually signals someone optimizing the wrong thing. The space "wasted" by a flat dimension is trivial compared with the multi-billion-row fact.

Q: When is a snowflaked or outrigger dimension actually justified?
A: It can be justified for a genuinely shared sub-dimension referenced from another dimension (an "outrigger"), such as a date attribute inside a dimension, or for a very large, sparsely used dimension where normalization meaningfully cuts size or eases maintenance. Even then Kimball treats it as an exception requiring justification, not the default. The bar is: does the join actually buy you integration or material savings, or are you just normalizing out of habit?

Q: What is the financial-services "accounts" dimensional pattern Kimball describes?
A: In financial services, the account is the central entity many processes hang off, so it becomes a core conformed dimension shared across products and transactions, often with SCD2 history because account attributes change. In a fund context the analogous conformed anchors are the fund, the share class, and the investor account, each modeled as a slowly changing dimension. Transactions (orders, fees, holdings) become facts referencing these account-like dimensions.

Q: What does "heterogeneous products" mean in dimensional modeling and why is it tricky?
A: Heterogeneous products are different product lines (e.g. equity funds, money-market funds, derivatives) that share some common attributes and measures but each have many product-specific ones, so a single flat fact/dimension would be either too generic or full of nulls. Kimball's solution is a core fact and core dimension holding the common attributes for all products, plus custom fact/dimension extensions per product line for the type-specific detail. This lets you query across all products generically or drill into one line's specifics.

Q: What is a "fee fact" and why model fees as their own fact table?
A: A fee fact captures fee/charge measurements — management fees, performance fees, distribution fees — typically at transaction or accrual grain, with the amount as an additive measure and foreign keys to fund, share class, investor, and date. Fees are a distinct business process from NAV calculation, so they get their own fact table at their own grain rather than being crammed into the NAV snapshot. Sharing conformed dimensions then lets you join fee facts to NAV facts to compute, say, fees as basis points of net assets.

Q: A fee-accrual process and a NAV-calculation process share the same dimensions but have different grains. How do you model them?
A: As two separate fact tables, each with its own declared grain (fee accrual perhaps one row per fund/share class/fee-type/accrual date; NAV one row per fund/share class/valuation date), both referencing the same conformed `dim_fund`, `dim_share_class`, and `dim_date`. You never force two different grains into one table. The conformed dimensions are exactly what let you join the two marts when you need fees-over-assets ratios.

Q: What does "drill across" mean and what makes it possible?
A: Drill-across means querying two or more fact tables (often at different grains) and combining their results on shared dimension attributes — for instance, total net assets from the NAV mart and total fees from the fee mart, grouped by fund and month. It is made possible only by conformed dimensions: each fact is aggregated separately to a common dimensional grain, then the result sets are joined on the conformed labels. You do not join the two fact tables directly; you join their aggregates.

Q: Why should you avoid joining two fact tables directly to each other?
A: Because fact tables are at different grains and have many rows, a direct fact-to-fact join multiplies rows and produces wrong, inflated measures (a "fan trap" or many-to-many explosion). The correct approach is to aggregate each fact to a common dimensional level independently and then join those aggregates on conformed dimension keys (drill-across). This is a frequent cause of double-counted totals in naive marts.

Q: What is a "factless fact table" and give a fund-administration example?
A: A factless fact table has no numeric measures; it records that an event or relationship occurred, represented purely by its foreign keys, and you analyze it by counting rows. An example is an eligibility or coverage table recording which share classes are registered for distribution in which countries on which dates — there's no amount, but counting rows answers "how many classes are registered in Luxembourg today?". Event-tracking and coverage scenarios are the two classic uses.

Q: What is the role of the date dimension and why is it almost always conformed?
A: The date dimension provides rich calendar attributes (day, month, quarter, fiscal period, business-day flag, holiday) so queries can filter and group by meaningful time concepts without date arithmetic in SQL. It is conformed across virtually every process because almost every fact references time, making it the most reused column on the bus matrix. It is a textbook dimension you pre-populate with one row per calendar day rather than deriving on the fly.

Q: What is an additive fact's relationship to the "rule" that you should always store the lowest practical grain?
A: Storing the lowest (most atomic) grain keeps facts maximally additive and re-aggregatable, because you can always sum atomic rows up to any summary level the business asks for, but you can never split a pre-aggregated row back down. Atomic grain therefore future-proofs the mart against new questions. The cost is more rows, which modern columnar engines like DuckDB handle well, so the bias is toward atomic detail.

Q: In dbt + DuckDB, how would you typically materialize a star schema mart's dimensions and facts?
A: You write each dimension and fact as a dbt model (a `SELECT` in a `.sql` file) and materialize them as tables via `{{ config(materialized='table') }}` or in `dbt_project.yml`, with staging models feeding dimension and fact models. dbt manages the build order through `ref()` dependencies, so dimensions build before the facts that reference them. DuckDB executes the SQL as the local analytical engine, giving you a versioned, testable star without a server.

Q: How would you implement SCD2 logic in dbt?
A: dbt provides snapshots: you define a snapshot block with a `unique_key`, a strategy (`timestamp` or `check`), and the columns to watch, and `dbt snapshot` maintains `dbt_valid_from`/`dbt_valid_to` rows automatically on each run. Alternatively you hand-write the SCD2 logic in an incremental model that closes the prior row and inserts a new one when watched attributes change. The snapshot approach is the idiomatic, less error-prone route for fund reference data.

Q: What columns does a dbt snapshot add to implement SCD2?
A: A dbt snapshot adds `dbt_valid_from` and `dbt_valid_to` (the effective-date window, with `dbt_valid_to` null on the current row), `dbt_scd_id` (a unique hash key per version), and `dbt_updated_at`. These give you the surrogate version key and effective dating that point-in-time joins need. You then join facts to the snapshot on the natural key with a `dbt_valid_from`/`dbt_valid_to` band predicate to get as-of-date attributes.

Q: An auditor asks for the NAV report "as it would have read on 2024-03-31, with fund attributes as of that date." How does your model satisfy this?
A: You query the NAV periodic-snapshot fact filtered to `valuation_date = '2024-03-31'` and join it to the SCD2 fund dimension on the natural key with `'2024-03-31' >= valid_from AND '2024-03-31' < valid_to`, returning the fund attributes effective on that date rather than today's. Because the snapshot fact preserves the historical NAV and the SCD2 dimension preserves the historical attributes, the report reconstructs point-in-time truth. This is exactly why fund reference data is SCD2.

Q: What is a "role-playing dimension" and give a fund-flow example?
A: A role-playing dimension is a single physical dimension referenced multiple times from one fact, each reference playing a different semantic role, usually surfaced through SQL views or aliases. The date dimension is the classic case: a subscription order fact may reference the same `dim_date` as order date, trade date, and settlement date simultaneously. You alias the dimension once per role so each foreign key resolves to clearly named columns.

Q: What is a "mini-dimension" and when do you split one off?
A: A mini-dimension extracts a cluster of rapidly changing or browse-heavy attributes out of a large dimension into its own small dimension, so frequent changes don't bloat the main dimension with SCD2 rows. You split one off when an attribute group changes so often that Type 2 history would explode the parent dimension's row count. The fact then carries a foreign key to both the base dimension and the mini-dimension.

Q: What is a "bridge table" and what problem does it solve?
A: A bridge table resolves a many-to-many relationship between a fact and a dimension, or within a dimension's hierarchy, by sitting between them and often carrying a weighting/allocation factor. In funds, a multi-manager fund or a portfolio holding multiple instruments needs a bridge so a single fact can fairly allocate across several dimension members. The weighting factor lets you split a measure without double-counting when fanning out the join.

Q: Why are many-to-many relationships between facts and dimensions dangerous if modeled naively?
A: Because a plain join across a many-to-many relationship multiplies fact rows by the number of related dimension members, inflating any SUM and producing double-counted totals like overstated AUM. A bridge table with allocation weights, applied carefully, lets you either allocate the measure proportionally or report it as an impact without overstating. Recognizing the many-to-many before joining is what prevents the double count.

Q: The lesson warns that "ungrained fact tables are where double-counted AUM reports come from." Explain the mechanism.
A: If a fact table's grain is undeclared or mixed, rows at different levels of detail coexist (say, both per-share-class and per-fund net-asset rows), so a naive `SUM(total_net_assets)` adds the fund-level rows and the share-class rows that compose them, counting the same assets twice. A single, explicitly declared grain guarantees every row is at the same level so SUMs are exact. The discipline of one-sentence grain declaration is precisely the guard against inflated AUM.

Q: How do you decide whether an attribute belongs in a dimension or as a fact?
A: Ask whether you primarily filter/group by it (dimension attribute) or aggregate it numerically (fact measure); descriptive, low-churn, used-as-a-label values go in dimensions, while additive numeric measurements go in facts. A value that is numeric but used mainly to constrain (like a credit rating bucket) often becomes a dimension attribute despite being a number. When in doubt, the "do I SUM it or GROUP BY it?" test usually decides.

Q: What is a "degenerate" versus "junk" decision for a cluster of order-related flags plus the order number?
A: The order number, being a bare identifier with no attributes, stays on the fact as a degenerate dimension; the cluster of small order flags (channel, in-specie, first-subscription) gets folded into a junk dimension referenced by one foreign key. So one order fact row holds the order number inline plus a single junk-dimension key. This keeps the fact narrow while preserving both the operational identifier and the flag combinations.

Q: Why should surrogate keys be integers rather than reusing natural business keys?
A: Integer surrogate keys are compact, fast to join and index, insulate the warehouse from changes or reformatting of business keys, and crucially allow SCD2 versioning where one natural key maps to many rows. They also provide a stable home for special rows like an "unknown"/"not applicable" member. Reusing a natural key (ISIN, LEI) as the PK breaks the moment you need history or the source reissues the identifier.

Q: How do you handle a fact whose dimension value is missing or not yet known at load time?
A: You point the fact's foreign key at a special dimension row reserved for "unknown" or "not applicable" (commonly a surrogate key like `-1`) rather than leaving the key null. This keeps the fact-to-dimension join an inner join with no row loss and gives reports an explicit "Unknown" bucket instead of silently dropping facts. Null foreign keys are avoided because they break joins and hide data-quality problems.

Q: What is the "centipede fact table" anti-pattern?
A: A centipede fact table has too many foreign keys (dozens of "legs"), usually because the designer made a separate dimension for every low-cardinality code instead of grouping flags into junk dimensions or using a proper hierarchy. It signals over-normalized dimensional thinking. The fix is to consolidate correlated low-cardinality attributes into junk dimensions and reconsider whether some "dimensions" are really just attributes.

Q: What is the difference between a "transaction" grain and a "snapshot" grain when modeling investor holdings?
A: A transaction grain records each subscription/redemption event (units bought or sold) as a row, capturing flows; a snapshot grain records the holding balance per investor/share class at each period end, capturing levels. Flows are fully additive across time; balances are semi-additive (not summable across time). You often build both: a transaction fact for activity analysis and a periodic snapshot for position reporting.

Q: Why might you build both a transaction fact and a periodic snapshot for the same process?
A: Because they answer different questions cheaply: the transaction fact preserves every flow for detailed activity and audit analysis, while the periodic snapshot precomputes the running balance so position-as-of-date queries don't have to sum the entire transaction history each time. Recomputing balances from transactions for every report is expensive at scale, so the snapshot is a performance and clarity convenience. Kimball calls this a "complementary" snapshot.

Q: How do conformed dimensions support incremental, agile warehouse delivery?
A: Because the bus matrix fixes conformed dimensions up front, each business process's mart can be built and shipped independently knowing it will plug into the same shared dimensions, so you deliver value one mart at a time without a big-bang build. Later marts reuse the already-built dimensions rather than reinventing them. This is the architectural reason Kimball pairs the bus matrix with incremental delivery.

Q: A new fee-mart and the existing NAV-mart both define their own `dim_fund` slightly differently. What is the risk and the fix?
A: The risk is non-conformance: "Fund A" might carry different attributes or keys in each mart, so drill-across results disagree and reconciliations fail — exactly the silo problem dimensional modeling exists to prevent. The fix is to build a single conformed `dim_fund` (one definition, one set of surrogate keys) and have both facts reference it. Governance over conformed dimensions is what keeps the marts integrated.

Q: What is the practical difference between "as-was" and "as-is" reporting, and which SCD type gives you both at once?
A: "As-was" reports facts against the dimension attributes effective at the time of the fact (historical truth), while "as-is" reports the same facts against today's attributes (current restated view). SCD Type 6 (and Type 2 augmented with a current-value column) provides both from a single dimension: group by the historical column for as-was, or the overwritten current column for as-is. Regulators usually want as-was; management dashboards often want as-is.

Q: Why might management prefer "as-is" reporting while regulators require "as-was"?
A: Management often wants to compare history under today's structure (e.g. current fund groupings) to spot trends consistently, which is as-is restatement. Regulators and auditors require the report to reflect the world as it actually stood at the reporting date — the fund's real domicile, name, and manager then — which is as-was, point-in-time-correct reporting. Supporting both from one SCD2/Type-6 dimension avoids maintaining two models.

Q: In a fee fact, why is the fee amount additive but a fee rate (in basis points) not?
A: The fee amount is a currency measurement of an actual charge, so it sums correctly across funds, investors, and time — it is fully additive. The fee rate in basis points is a ratio (fee relative to assets), and ratios are non-additive: averaging or summing rates across funds is meaningless. You store the amount as a fact and, if you need an overall rate, divide the summed fees by the summed assets at query time.

Q: How would you compute "fees as basis points of average net assets" across the NAV and fee marts?
A: Aggregate total fees from the fee fact and average net assets from the NAV snapshot fact independently to a common grain (say fund and month), join the two aggregates on the conformed fund and date dimensions (drill-across), then divide summed fees by average net assets and scale to basis points. You never join the two fact tables row-to-row. The conformed dimensions are what make the aligned join correct.

Q: What is the danger of computing an average from a periodic snapshot's semi-additive measure incorrectly?
A: If you naively SUM a balance (like net assets) across time and divide by the count, or worse just SUM it, you double-count assets that persist across snapshots and overstate the figure. The correct time aggregation for a balance is an average of the period-end values (or an end-of-period value), respecting its semi-additivity. Mislabeling a balance as additive is a classic source of inflated AUM-over-time figures.

Q: How are hierarchies (fund → share class, or country → region) usually represented in a star schema?
A: As denormalized attribute columns within a single flat dimension — the fund dimension carries both the share-class attributes and the parent-fund attributes, and a geography dimension carries country plus its region/continent. This avoids the snowflake of separate hierarchy tables and lets users drill up and down by selecting columns. The redundancy is accepted because it keeps queries simple and fast.

Q: What is the "audit dimension" pattern and why is it useful in a regulated mart?
A: An audit dimension attaches lineage and data-quality metadata to fact rows — load timestamp, source system, ETL batch id, quality flags — via a foreign key, so each measurement carries provenance. In a regulated fund mart this supports traceability requirements: you can show which load produced a given NAV figure and whether it passed quality checks. It turns operational metadata into a queryable dimension rather than buried log entries.

Q: Why does Kimball recommend descriptive text values in dimensions rather than cryptic codes?
A: Because dimensions are the source of report labels and filter values, spelling out human-readable descriptions ("Luxembourg SICAV", "Management Fee") makes reports self-explanatory and reduces lookups, whereas raw codes force users to memorize meanings. You can keep the operational code as an additional attribute, but the verbose label is what drives usability. Dimensions are deliberately verbose precisely because that is where meaning is presented to users.

Q: What is a "step" in an accumulating snapshot for a subscription order, and what measures does it enable?
A: Each step is a milestone in the order lifecycle — order received, dealt/traded, settled, units allocated — and each has its own date foreign key on the single order row. With all milestone dates present, you can compute lag measures like days from receipt to settlement, and monitor pipeline bottlenecks. The row is updated as each milestone is reached, which is the defining behavior of the accumulating snapshot.

Q: Why is an accumulating snapshot poorly suited to immutable, insert-only storage layers?
A: Because its core behavior is updating the same row repeatedly as milestones complete, which clashes with append-only or immutable storage (and with some columnar engines' update costs). On such platforms you typically rebuild affected rows or use merge/upsert patterns, accepting the overhead. If frequent in-place updates are impractical, a transaction fact recording each milestone event may be the better fit.

Q: How do you model fund domicile (e.g. a fund redomiciling from one jurisdiction to Luxembourg) so history stays correct?
A: Store domicile as an SCD2 attribute on the fund dimension: when the fund redomiciles, close the prior row's `valid_to`, insert a new row with the new domicile and a fresh surrogate key, and keep `is_current` accurate. NAV facts struck before the change still join to the old-domicile row via the date-band predicate, preserving point-in-time truth. This is the textbook reason fund reference data is SCD2.

Q: What goes wrong if you model fund domicile as SCD Type 1 in a regulated mart?
A: A Type 1 overwrite replaces the old domicile everywhere, so reruns of historical regulatory reports would show the new domicile against NAVs that were actually struck under the old jurisdiction — a misstatement of history. Auditors reconciling prior filings would find the mart disagrees with what was originally reported. This silent history rewrite is exactly why such attributes are SCD2, not Type 1.

Q: What is the "snapshot fact has no transactions in a period" situation and how does the snapshot handle it?
A: A periodic snapshot records a row for every entity every period regardless of activity, so even a fund with no subscriptions still gets a NAV row each valuation date showing its current balance. This guarantees a value is always available for any reporting date, unlike a transaction fact where a quiet period leaves gaps. The snapshot's completeness is precisely why it suits balance/position reporting.

Q: How does the choice of fact type affect whether you can answer "what was the balance on an arbitrary date"?
A: A periodic snapshot answers it directly with a single indexed lookup because a balance row exists for every period; a transaction fact answers it only by summing all transactions up to that date, which is correct but expensive and requires a complete event history. Accumulating snapshots track a workflow's state, not periodic balances. Choosing the snapshot type is what makes "balance as of date" cheap.

Q: Why is the surrogate key on a fact (pointing to an SCD2 dimension version) sometimes preferable to a date-band join?
A: If the ETL resolves and stores the correct SCD2 version's surrogate key on the fact at load time, then as-was reporting is a simple equi-join on the surrogate, avoiding the runtime cost and error risk of a `BETWEEN`-style date-band predicate. The fact "remembers" which version was effective when the event occurred. The trade-off is that you must resolve the version correctly during load and can't easily re-point it later.

Q: When loading a fact, how do you resolve which SCD2 dimension surrogate key to assign?
A: You look up the dimension by natural key (e.g. ISIN) and select the version whose effective window contains the fact's business date — `WHERE isin = ? AND fact_date >= valid_from AND fact_date < valid_to` — and store that row's surrogate on the fact. This "freezes" the correct historical context onto the fact at load time. Getting this lookup right is the heart of correct SCD2 fact loading.

Q: What is the difference between the bus matrix and an actual data model (ERD) of the warehouse?
A: The bus matrix is a high-level planning grid of processes against conformed dimensions, with no columns or keys — it scopes and sequences the warehouse. An ERD/star-schema diagram is the detailed physical model showing fact and dimension tables, columns, keys, and joins for a specific mart. The matrix guides which marts to build and how they conform; the star schema is how one of them is actually implemented.

Q: How would you justify each dimension in a fund bus matrix as conformed, junk, or degenerate?
A: Fund, share class, investor, and date are conformed dimensions because they are shared identically across multiple processes (NAV, fees, orders, TA). An order-flags cluster would be a junk dimension, local to the order fact, bundling small indicators. The order number (or a SWIFT message reference) is a degenerate dimension stored inline on the order fact with no separate table. Classifying each member this way is part of the lesson's "done when" checklist.

Q: Where does a LEI fit in a fund-administration dimensional model?
A: The LEI (Legal Entity Identifier) is a natural-key attribute on a legal-entity dimension — the fund's management company, the depositary, or a counterparty — used to identify and conform that entity across processes and against external regulatory data. It sits as a descriptive attribute on the relevant dimension, not as a fact. If the entity's attributes change over time, that dimension is SCD2 so the LEI's associated details stay point-in-time correct.

Q: Where does an ISIN fit in the model, and at what grain?
A: An ISIN identifies a specific security or fund share class, so it is the natural key of the share-class dimension at one-row-per-share-class (or per-version under SCD2) grain. Facts reference the share-class dimension by surrogate key, with the ISIN available as a lookup attribute. Because the same ISIN spans multiple SCD2 versions, it cannot itself be the dimension's primary key.

Q: How should a SWIFT message reference on a settlement fact be modeled?
A: As a degenerate dimension — an operational identifier stored directly on the fact, since the reference identifies a specific message/transaction but carries no descriptive attributes of its own. It supports drill-back to the operational system and grouping of related fact rows. Building a separate single-column dimension table for it would add a join with no analytical payoff.

Q: An EMT/EPT-style regulatory extract needs cost and charge figures per share class per reporting period — how does dimensional modeling serve this?
A: You source it from a charges/fees fact at the share-class/period grain joined to conformed share-class and date dimensions (and an SCD2 share-class dimension for point-in-time-correct attributes), aggregating additive charge amounts and computing ratios as aggregate-then-divide. The conformed dimensions guarantee the figures align with the NAV mart for consistency checks. The clean grain prevents the double-counting that would corrupt a regulatory cost disclosure.

Q: Why is "declare the grain in one written sentence" a review gate for an architect?
A: Because if the modeler cannot state the grain in a single unambiguous sentence, the fact table's meaning is undefined and its aggregations cannot be trusted — the architect literally cannot review the mart. The one-sentence test forces clarity and surfaces mixed-grain mistakes before columns are added. It is the cheapest, highest-leverage correctness check in dimensional design, which is why the lesson elevates it.

Q: What is a "transaction-grain" fee fact versus an "accrual-grain" fee fact?
A: A transaction-grain fee fact records each individual fee charge event as it is billed, while an accrual-grain fee fact records the daily/periodic accrual amounts that build up to a charge over time. They answer different questions — actual charges versus how the liability accumulated — and may coexist as separate facts. Each must have its grain declared explicitly so accrual rows and charge rows are never summed together.

Q: How do you prevent double counting when a fund-of-funds holds positions in underlying funds?
A: Recognize the many-to-many/hierarchy and avoid summing parent and child net assets together; use a bridge or allocation approach, or report the fund-of-funds and its underlyings at clearly separated grains so a single SUM never adds the same assets at two levels. Declaring the grain and being explicit about look-through allocation is what stops the inflated AUM. This is a concrete instance of the "ungrained tables double-count AUM" warning.

Q: What is the difference between a "snapshot" and an "accumulating snapshot" — do not confuse them.
A: A (periodic) snapshot records a fresh row per entity at each regular interval, capturing balances over time, and is insert-only with many rows per entity. An accumulating snapshot records one row per workflow instance and updates that same row as it progresses through milestones, with multiple date keys and lag measures. Periodic = recurring states over time; accumulating = one evolving row per process instance.

Q: Why does dimensional modeling deliberately accept data redundancy in dimensions?
A: Because the redundancy from denormalizing hierarchies and descriptions into flat dimensions buys query simplicity, performance, and user understandability, which are the goals of an analytical mart, and the storage cost is trivial next to the fact table. Update anomalies, the usual argument against redundancy, are managed by the controlled ETL load rather than by ad-hoc writes. This is the conscious trade that distinguishes dimensional from normalized design.

Q: What is the first thing to check when a mart's total AUM report comes back roughly double the expected figure?
A: Check the fact table's grain and your join cardinality first: a mixed/undeclared grain or a many-to-many join (e.g. fact-to-fact, or a fund hierarchy joined without allocation) is the usual cause of a doubled total. Verify that one declared grain sentence holds for every row and that you aren't summing parent and child levels together. The lesson explicitly ties doubled AUM to ungrained fact tables.

Q: A point-in-time NAV report shows today's fund name for a NAV struck a year ago. What is the modeling defect?
A: The fund dimension is SCD Type 1 (overwriting) instead of Type 2, so the historical NAV joins to the current, overwritten attribute row rather than the version effective a year ago. The fix is to make the fund name an SCD2 attribute with effective dating and join on a date-band predicate so the year-old NAV picks up the name that was current then. This is the canonical as-was-versus-as-is failure.

Q: Why might a snowflaked dimension actually slow down a self-service BI tool beyond just extra joins?
A: Many BI tools and semantic layers assume a star (single table per dimension); a snowflake forces the tool either to pre-join the normalized pieces or to expose multiple tables that confuse business users and can produce wrong aggregations if the user joins them incorrectly. Beyond raw join cost, it degrades usability and increases the chance of fan-trap errors. This usability hit is a core reason Kimball calls snowflaking a smell in marts.

Q: How do you choose between SCD Type 2 and Type 3 for a given attribute?
A: Use Type 2 when you need the full history of changes and point-in-time-correct joins (most regulated reference data); use Type 3 when only the immediately prior value matters and you want an easy "current vs previous" comparison without history rows. Type 3 cannot track more than one change back, so any attribute that changes repeatedly or needs full audit history must be Type 2. The number of changes you must retain is the deciding factor.

Q: What is the role of the "current flag" plus effective dates together in everyday querying?
A: The effective dates (`valid_from`/`valid_to`) enable arbitrary as-of-date joins for historical reporting, while the `is_current` flag gives a fast path for "today's value" queries without date arithmetic. Together they let one SCD2 dimension serve both point-in-time and current-state needs efficiently. They must be kept consistent by the load so the open row is the one flagged current.

Q: Summarize the dimensional design sequence an architect should follow for a new fund mart.
A: First identify the business process; second declare the fact table's grain in one sentence; third pick the dimensions consistent with that grain (reusing conformed ones from the bus matrix); fourth identify the facts (measures) and their additivity. Reference data dimensions like fund and share class are modeled SCD2 for point-in-time correctness, flags are bundled into junk dimensions, operational ids stay as degenerate dimensions, and snowflaking is avoided. Following this order is what produces a reviewable, double-count-proof mart.

Q: You run a `dbt snapshot` for the first time and every fund shows `dbt_valid_to` as null — is that a bug?
A: No, that is expected: on the first snapshot run every natural key gets exactly one row that is the current version, so its `dbt_valid_to` is null because no later version has superseded it yet. A null `dbt_valid_to` is dbt's open-ended marker for the active record, equivalent to a `9999-12-31` sentinel. You would only see a populated `dbt_valid_to` after a subsequent run detects a changed watched column and closes the prior row.

Q: A `dbt snapshot` with `strategy='check'` is creating a new row on every run even though nothing changed — first thing to check?
A: First check the `check_cols` list and the source query for a column whose value is non-deterministic across runs — a `current_timestamp`, a row-order-dependent value, or a floating-point field that re-rounds differently. The `check` strategy hashes the listed columns and inserts a new version whenever the hash differs, so any volatile column silently triggers spurious SCD2 rows. The fix is to exclude or stabilize that column, or switch to `check_cols='all'` only over genuinely stable attributes.

Q: How do you express "watch every column for changes" in a dbt snapshot, and why is that risky for fund data?
A: You set `check_cols='all'`, which tells dbt to hash all non-key columns and version the row when any of them changes. It is risky for fund reference data because an upstream cosmetic change (a re-cased description, a reformatted code, a recomputed derived field) then spawns a new SCD2 version that does not represent a real business change, bloating history and muddying audit. Best practice is to enumerate only the business-meaningful watched columns explicitly.

Q: In DuckDB, what DDL would you use to give a dimension a surrogate primary key that auto-increments?
A: You create a sequence and default the key from it, for example `CREATE SEQUENCE seq_dim_fund START 1;` then `CREATE TABLE dim_fund (fund_sk BIGINT DEFAULT nextval('seq_dim_fund') PRIMARY KEY, ...);`. DuckDB has no `AUTO_INCREMENT` keyword, so sequences with `nextval()` are the idiomatic surrogate-key generator. The surrogate is then unique per SCD2 version while the ISIN natural key repeats across versions.

Q: Write a DuckDB query that closes the prior SCD2 row when inserting a new version of a fund.
A: You issue an `UPDATE dim_fund SET valid_to = DATE '2026-06-15', is_current = false WHERE isin = 'LU0000000001' AND is_current = true;` immediately before inserting the new open row with `valid_from = DATE '2026-06-15'` and `valid_to = DATE '9999-12-31'`. Doing the close and the insert in the same transaction keeps the bands contiguous and prevents a window where two rows are both current. The half-open convention (`valid_to` of old equals `valid_from` of new) avoids any date double-match.

Q: A point-in-time join returns zero dimension rows for a fact that should match — first thing to check?
A: First check whether the fact's business date falls before the earliest `valid_from` in the dimension, which leaves it outside every effective window, and whether your band predicate's inequalities match your loading convention. A common cause is loading dimension history starting later than the oldest facts, so early facts have no covering version. The remedy is an "unknown"/earliest-version member or backfilling the dimension's initial `valid_from` to a low sentinel like `1900-01-01`.

Q: Why prefer the half-open interval `valid_from <= d < valid_to` over the closed `valid_from <= d <= valid_to` in SCD2?
A: The half-open convention makes adjacent versions perfectly tileable — the prior row's `valid_to` equals the next row's `valid_from`, so a date exactly on the boundary matches exactly one row instead of two. The closed convention forces you to set `valid_to` to the day before the change, inviting off-by-one errors and gaps. Half-open is the Kimball-recommended default precisely because it eliminates boundary double-matches without manual date arithmetic.

Q: How would you detect overlapping SCD2 windows in a dimension with a single SQL check?
A: You self-join the dimension on the natural key and look for rows whose windows intersect, e.g. `SELECT a.isin FROM dim_fund a JOIN dim_fund b ON a.isin = b.isin AND a.fund_sk <> b.fund_sk AND a.valid_from < b.valid_to AND b.valid_from < a.valid_to;` — any returned ISIN has overlapping versions. A clean SCD2 dimension returns no rows. This is the kind of assertion you would wrap in a dbt test or a `dbt test`-style data check.

Q: How do you write a dbt test that guarantees only one current row per natural key in an SCD2 dimension?
A: You add a `unique` test (or a `dbt_utils.unique_combination_of_columns` test) scoped to the rows where `is_current = true`, typically via a filtered model or a `where` config on the test, asserting the natural key is unique among current rows. If two rows for the same ISIN are both flagged current the test fails, surfacing the load bug. This guards the invariant that exactly one version is open per entity.

Q: A fact row's foreign key points to surrogate `-1`. What does that signify and is it an error?
A: It signifies the fact was loaded with a missing or not-yet-known dimension value and was deliberately pointed at the reserved "Unknown"/"Not applicable" member rather than left null. It is not necessarily an error — it is the designed late-arriving or missing-data fallback — but a rising count of `-1` keys is a data-quality signal worth alerting on. Keeping the key non-null preserves the inner join so no facts are silently dropped.

Q: What is a "late-arriving dimension" and how do you load a fact that references one?
A: A late-arriving dimension is a dimension member whose record shows up after the fact that references it has already arrived — for example a NAV fact for a share class whose reference record loads the next day. You handle it by inserting a placeholder dimension row keyed on the natural key (marked unknown/incomplete) so the fact can resolve its surrogate immediately, then updating that row in place when the real attributes arrive. This avoids dropping or holding back facts waiting on reference data.

Q: What is a "late-arriving fact" and why does it complicate SCD2 surrogate resolution?
A: A late-arriving fact is an event that loads well after its business date — say a backdated correction trade — so at load time you must resolve the SCD2 dimension version that was effective on the historical fact date, not today's version. You look up the dimension by natural key with the band predicate against the fact's old business date, not the load date. Using the current version would attach wrong as-was context and corrupt point-in-time reporting.

Q: How do you handle a fund's domicile change that the source reports retroactively, after NAV facts were already loaded against the old version?
A: You insert the correct SCD2 version with the real effective date and, because facts may have been loaded with surrogate keys pointing at a now-mis-dated version, you re-resolve and re-point the affected facts (or rely on date-band joins if facts were not pre-resolved). Date-band joins recover automatically once the dimension history is corrected; pre-resolved surrogate keys need a targeted update. This is why some shops keep the natural key on the fact even when storing the surrogate, to enable re-resolution.

Q: Why might an architect keep both the surrogate key and the natural key on a fact table?
A: Storing the surrogate gives a fast equi-join for as-was reporting, while also keeping the natural key (ISIN, LEI) lets you re-resolve versions after late-arriving corrections, run date-band joins as a cross-check, and trace lineage back to source. The small extra width buys flexibility and auditability that pure-surrogate facts lack. In regulated marts the re-resolution and traceability benefits usually justify carrying both.

Q: How does a fund's stock split or share-class consolidation affect the additivity of shares outstanding in the NAV snapshot?
A: It breaks naive time-series additivity even though shares outstanding was already semi-additive: comparing pre- and post-split shares as if continuous overstates or understates growth because the unit basis changed. You handle it by storing the split factor as a dimension attribute or a separate adjustment fact and computing split-adjusted series at query time, never by summing raw share counts across the event. The NAV-per-share ratio remains the safest comparable measure across the split.

Q: When would you choose an accumulating snapshot over a transaction fact for the subscription-to-settlement lifecycle?
A: Choose the accumulating snapshot when the analytical focus is pipeline progress and durations — days from order receipt to settlement, how many orders are stuck between trade and settle — because one updatable row per order with milestone date keys makes lag measures trivial. Choose a transaction fact when you need an immutable audit log of each status-change event or your storage layer dislikes updates. The accumulating snapshot trades update cost for cheap stage-duration analytics.

Q: For a T+2 settlement workflow, which milestone date columns would the accumulating snapshot carry, and what lag measure follows?
A: It carries at least `order_received_date_key`, `trade_date_key`, `settlement_date_key`, and `units_allocated_date_key`, each a foreign key into the conformed date dimension. From them you derive lag measures such as `days_trade_to_settle = settlement_date - trade_date`, which should equal two business days under T+2 and flags exceptions when it does not. Monitoring that lag is exactly how you spot settlement breaks in a TA pipeline.

Q: How would a move to T+1 settlement change an accumulating-snapshot order fact and its monitoring?
A: The structure is unchanged — the same milestone date keys remain — but the expected `days_trade_to_settle` lag tightens from two business days to one, so your exception thresholds and SLA monitors must be re-baselined to flag anything exceeding T+1. Compressed timelines also mean late-arriving reference data and unresolved dimensions have less slack, raising the importance of robust late-arriving-dimension handling. The model is stable; the operational tolerances and data-timeliness assumptions shift.

Q: How do you model a measure that is additive over funds but must be averaged over time, in a way BI tools handle correctly?
A: You store the raw semi-additive balance (e.g. `total_net_assets`) at snapshot grain and configure the semantic layer's default time aggregation to `AVG` (or last-value) while leaving cross-fund aggregation as `SUM`. Tools like a dbt semantic layer, LookML, or Power BI let you set per-measure aggregation rules so users cannot accidentally SUM a balance over time. Encoding the semi-additivity in metadata prevents the inflated-AUM-over-time error at the tool level.

Q: What is the difference between an "additive fact" and a "calculated/derived fact" you might add for convenience?
A: An additive fact is a raw stored measurement that sums correctly across all dimensions; a calculated fact is a value derived from other facts (or facts and attributes) that you may persist for convenience but that often is non-additive. Persisting a derived ratio like NAV-per-share is convenient for display yet dangerous to sum, so the rule is to also keep the additive components and treat the derived column as read-only. Never let a stored derived ratio be re-aggregated.

Q: How would you store a multi-currency NAV so cross-fund totals remain meaningful?
A: You store the NAV in its native share-class currency plus a base-currency conversion (e.g. EUR) using the official FX rate effective on the valuation date, keeping both as additive amounts and the rate as a dimension/fact attribute. Cross-fund totals then sum the base-currency column, not the mixed native amounts, because adding amounts in different currencies is meaningless. The applied FX rate and its date must be retained for auditability of the converted figure.

Q: Where does the FX rate used for NAV conversion belong in the dimensional model?
A: The applied rate is best stored as an additional measure on the NAV fact (the exact rate used for that fund/share-class/date), with a conformed currency dimension supplying descriptive currency attributes. Storing the rate on the fact freezes the historical conversion so reruns reproduce the original base-currency figure even if a rate table is later corrected. A separate FX-rate fact at currency-pair/date grain can feed the load but should not be relied on for as-was reproduction.

Q: How do you model a share class that is registered for distribution in multiple countries, for a coverage report?
A: You use a factless fact table at fund/share-class/country/effective-date grain — one row per registration relationship — referencing conformed share-class, country, and date dimensions, with no numeric measure. You answer "how many classes are registered in Luxembourg today?" by counting rows under a date-effective filter. The many-to-many between class and country is captured as rows in the factless fact rather than by exploding a dimension.

Q: A "coverage" factless fact for distribution registrations needs to answer "which classes are NOT registered in Germany?" — how?
A: You compute the complement: take all active share classes and left-anti-join (or `NOT EXISTS`) against the registration factless fact filtered to Germany and the as-of date, returning classes with no matching registration row. Factless facts naturally answer "what happened" by counting present rows, so "what did not happen" requires comparing against the full population of possible combinations. This complement query is the classic second use of coverage tables.

Q: How do you model an EET-style ESG/sustainability data extract at the right grain in a fund mart?
A: You source it from a sustainability fact (or attribute set) at the share-class/reporting-period grain, joined to a conformed SCD2 share-class dimension so the disclosed values are point-in-time correct for the reporting date, with additive components stored separately from any derived ratios. Because ESG classifications change, the relevant attributes belong on an SCD2 dimension to reproduce what was disclosed when. The conformed share-class and date dimensions keep it reconcilable with the EMT cost extract and the NAV mart.

Q: Why must regulatory extracts like EMT, EPT, and EET reconcile to the same conformed dimensions rather than being built as independent silos?
A: Because regulators and clients expect the cost figures (EMT), product/pre-contractual data (EPT), and ESG data (EET) for "Share Class X" to describe the identical entity, which only holds if all three hang off one conformed, SCD2 share-class dimension and one conformed date dimension. Independent silos drift, so the same ISIN can carry inconsistent attributes across extracts and fail cross-checks. Conformance is what lets you assert the three extracts are mutually consistent for the same point in time.

Q: How does the EU AI Act's traceability/record-keeping expectation interact with the audit-dimension pattern in a mart?
A: If a mart feeds or is fed by an AI system whose outputs require record-keeping, the audit dimension's lineage metadata (source system, load batch, model/version id, quality flags, timestamp) gives you the queryable provenance to evidence how a given figure was produced. Attaching that metadata per fact row turns "show the trail for this number" into a join rather than a log forensics exercise. The pattern predates the AI Act but maps cleanly onto its traceability and human-oversight documentation needs.

Q: What is the difference between a "minidimension" and an "outrigger", concretely?
A: A mini-dimension is a deliberately split-off cluster of fast-changing or browse-heavy attributes given its own dimension and referenced by an additional foreign key from the fact, to keep the parent dimension from exploding under SCD2. An outrigger is a normalized sub-dimension referenced from within another dimension (dimension-to-dimension), used sparingly for a genuinely shared lookup. The mini-dimension attaches to the fact; the outrigger attaches to a dimension.

Q: When a mini-dimension is split off, how do you analyze the combined "profile at a point in time"?
A: The fact carries foreign keys to both the base dimension and the mini-dimension simultaneously, so each fact row already records which base version and which profile band co-occurred at that event. To study the relationship over time independent of facts, you can add a Type-2 "current profile" pointer on the base dimension. The co-occurrence on the fact is what lets you reconstruct the full profile as it was when the measurement happened.

Q: How do you model a many-to-many between a fund and its multiple investment managers without overstating AUM?
A: You insert a bridge table between the fund dimension and a manager dimension carrying an allocation weight per manager that sums to 1.0 per fund, so a measure fanned across managers is multiplied by the weight rather than duplicated. Querying by manager then allocates net assets proportionally; querying without the manager dimension uses the fund grain directly and avoids the bridge. The weighting column is the specific mechanism that prevents the double count.

Q: A drill-across report shows fees that are exactly double the expected value while NAV is correct — first thing to check?
A: First check whether the fee aggregate was joined directly to the NAV fact (or to a dimension at a finer grain) so fee rows fanned out across multiple NAV or date rows before summing. Drill-across requires aggregating each fact to the common grain independently and only then joining the two result sets on conformed keys; a premature join multiplies one side. The doubling pattern on just one measure is the signature of a fan-out on that fact's join path.

Q: What is a "fan trap" and how does it manifest in a fund mart join?
A: A fan trap occurs when you join a one-to-many relationship and then aggregate a measure from the "one" side, causing it to be repeated and summed once per "many" row — for example joining a fund's single net-asset row to its many fee rows and then SUMming net assets. The net assets get multiplied by the fee-row count. You avoid it by aggregating each fact independently to a shared grain before joining, never SUMming a measure across a join that fanned it out.

Q: How do you decide between adding an attribute to an existing dimension versus creating a new conformed dimension?
A: Add it to the existing dimension if it is a descriptive attribute of that same entity at the same grain (e.g. fund domicile belongs on `dim_fund`); create a new conformed dimension if it identifies a distinct entity reused across multiple processes (e.g. investor, country). The test is "is this a property of the entity, or is it its own entity that many facts reference?". Misplacing an entity as an attribute blocks its reuse as a conformed join key elsewhere.

Q: Why is a "monster dimension" (very wide, very tall) a problem and what are two mitigations?
A: A monster dimension — millions of rows and dozens of attributes, like a global investor dimension under heavy SCD2 churn — slows browses and balloons under history. Two mitigations are splitting volatile attributes into a mini-dimension to cap SCD2 growth, and moving rarely-used or large text attributes out so the hot path stays narrow. The aim is to keep the frequently-joined core compact while still capturing detail.

Q: How would you implement an "as-of-date" parameterized point-in-time NAV query in DuckDB?
A: You bind the as-of date once and reuse it in both the fact filter and the dimension band, e.g. `SELECT ... FROM fact_nav f JOIN dim_fund d ON f.isin = d.isin AND f.valuation_date >= d.valid_from AND f.valuation_date < d.valid_to WHERE f.valuation_date = DATE '2024-03-31';`. The band predicate selects the version effective on the fact date, giving as-was attributes. Parameterizing the date lets the same query reproduce any historical report on demand.

Q: What is the difference in DuckDB between `CREATE TABLE AS SELECT` for a dimension and `CREATE VIEW` for a role-playing alias?
A: `CREATE TABLE AS SELECT` (CTAS) materializes the dimension's rows physically so facts join to stored data, which is what you do for the base date dimension. A `CREATE VIEW` over that table, aliased per role (order date, trade date, settlement date), exposes the same physical dimension multiple times without copying it — the canonical way to surface a role-playing date dimension. The view costs no storage and keeps a single source of calendar truth.

Q: In dbt, how do you guarantee dimensions are built before the facts that reference them?
A: You reference upstream models with the `ref()` function inside the fact model's SQL (e.g. `from {{ ref('dim_fund') }}`), and dbt infers the dependency DAG so `dbt run` topologically orders dimension builds before fact builds. You never hardcode build order; the `ref()` edges are the dependency graph. Breaking a dependency by hand-writing a table name instead of `ref()` is how out-of-order build bugs creep in.

Q: How would you add a dbt `relationships` test to enforce referential integrity from a fact's foreign key to its dimension?
A: In the fact model's schema YAML you add a `relationships` test on the foreign-key column pointing at the dimension's key, e.g. `tests: [{relationships: {to: ref('dim_fund'), field: fund_sk}}]`. It fails if any fact carries a `fund_sk` absent from the dimension, catching orphaned keys before they corrupt joins. Pair it with a `not_null` test (allowing the `-1` unknown member) to enforce that every fact resolves to a real dimension row.

Q: Why is `accepted_values` a useful dbt test for junk-dimension flag columns?
A: A junk dimension's flag columns have a small known domain (e.g. channel in {`DIRECT`, `PLATFORM`, `ADVISOR`}), and `accepted_values` fails the run if an unexpected value sneaks in from a source change. This catches upstream drift that would otherwise create unanticipated flag combinations and silently expand the junk dimension. Constraining the domain at test time keeps the junk dimension's enumerated combinations trustworthy.

Q: How do you size and populate a junk dimension — all theoretical combinations or only observed ones?
A: You populate only the flag combinations that actually occur (or are realistically possible), not the full Cartesian product, because most combinations never appear and pre-generating all of them wastes rows. You add new combinations lazily as they first show up during loads. For a handful of low-cardinality flags even the full product is tiny, but observed-only keeps it minimal and meaningful.

Q: What is the risk of putting a high-cardinality attribute (like an exact timestamp) into a junk dimension?
A: A junk dimension is meant for low-cardinality flags; inserting a high-cardinality attribute like a precise timestamp causes the combination count to explode toward one row per fact, defeating the purpose and bloating the dimension. High-cardinality continuous values belong either as a fact (if numeric/additive) or split into a date and a time-of-day dimension at coarse grain. The cardinality test is what keeps a junk dimension small.

Q: How do you split a precise event timestamp so it is usable dimensionally?
A: You decompose it into a date foreign key into the conformed date dimension and, if intraday analysis matters, a separate time-of-day dimension at a coarse grain (e.g. minute or hour buckets), keeping any full-precision timestamp as a fact attribute for drill-back. This keeps both dimensions low-cardinality and reusable while preserving the exact instant on the fact. Jamming a full timestamp into one dimension would make it effectively per-row.

Q: Why model "date" and "time-of-day" as two separate dimensions rather than one combined datetime dimension?
A: Combining them would multiply the row count to one per calendar minute/second across all dates (millions of rows) and destroy reuse, whereas a date dimension (one row per day) and a separate time-of-day dimension (one row per minute or bucket within a day) stay small and independently conformed. Facts carry one foreign key to each. The split keeps each dimension reusable across every process that needs either calendar or intraday context.

Q: How would you validate that a periodic NAV snapshot has no missing valuation dates for an active share class?
A: You cross-join the active share class against the date dimension's valuation (business) days for the period and left-anti-join the NAV fact, returning any fund/share-class/date that should have a row but does not. Because a periodic snapshot is meant to have a row per entity per period, a gap signals a load failure or a missed valuation. This completeness check is exactly what makes "balance as of any date" reliable.

Q: A regulator asks for the cost extract "as filed on 2025-03-31" but a fee was corrected afterward — how does your model reproduce the original?
A: If fee facts are immutable and corrections are booked as new adjustment rows (not overwrites), you reproduce the original by aggregating only rows whose load/effective date is on or before the original filing date, excluding the later correction. This requires either an audit-dimension load date on each fee row or an SCD2/append-only fee design rather than in-place updates. Overwriting the original fee would make the as-filed figure unrecoverable, which is why corrections are appended.

Q: Why is an append-only correction pattern preferable to overwriting facts in a regulated mart?
A: Append-only corrections (booking a reversing/adjusting row rather than mutating the original) preserve the full audit trail so you can reproduce any figure exactly as it stood at any prior point and show the correction history. Overwriting destroys the as-filed value and breaks reconciliation against earlier regulatory submissions. The trade-off is that consumers must net the original and adjustment rows, which the declared grain and a clear adjustment flag make safe.

Q: How do you keep a derived "is_current" flag and the `valid_to` sentinel from disagreeing during concurrent loads?
A: You update the flag and the dates inside a single transaction (close old row's `valid_to` and set `is_current = false`, then insert the new open row in the same atomic unit), and you add a data test asserting `is_current = true` exactly when `valid_to` is the sentinel. Atomicity plus the cross-check is what prevents a window where the flag and the dates contradict each other. In DuckDB you wrap the statements in `BEGIN; ... COMMIT;` so a failure rolls back cleanly.

Q: Summarize the architect's review checklist for signing off a new fund fact table.
A: Confirm the grain is stated in one unambiguous sentence and every row obeys it; verify each measure's additivity is classified (additive, semi-additive, non-additive) and ratios are stored as components; check every foreign key resolves to a conformed dimension with an unknown member for missing values; ensure reference dimensions are SCD2 with non-overlapping, gap-free windows and a single current row per key. Finally validate no fact-to-fact joins, junk dimensions for flag clusters, degenerate dimensions for operational ids, and no gratuitous snowflaking — the same disciplines that make the mart reviewable and double-count-proof.

Q: Why is a surrogate key on the date dimension often a meaningful integer like `20240331` rather than an arbitrary sequence value?
A: The date dimension is the one place Kimball tolerates a "smart" surrogate — an integer in `YYYYMMDD` form — because dates never change, never need SCD2 versioning, and the encoded value aids partition pruning and human readability. It still functions as a surrogate key on facts while letting you eyeball or range-scan by date without a join. Every other dimension keeps meaningless sequence surrogates because their natural keys can churn or version, but the immutable date is the principled exception.

Q: A NAV fact and a holdings snapshot disagree on total net assets for the same fund and date — what reconciliation steps do you take first?
A: First confirm both facts share the identical declared grain interpretation and conformed `dim_fund`/`dim_date` keys, then check that the holdings snapshot was struck at the same valuation point and currency basis as the NAV, including the same FX rate and date. Disagreements usually trace to a grain or timing mismatch (one includes pending trades or accruals the other excludes) or a currency-conversion difference rather than a true error. Because both hang off conformed dimensions, the drill-across comparison itself is valid; the gap localizes which input differs.
