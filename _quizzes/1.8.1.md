# Phase 1 · 1.8.1 Idempotency — 150 self-test questions

Q: What does it mean for an operation to be idempotent?
A: An operation is idempotent if applying it more than once produces the same end state as applying it exactly once. The mechanism that matters in pipelines is that retries, replays, and reruns converge on one correct result instead of accumulating duplicates. For example, `SET balance = 100` is idempotent whereas `balance = balance + 10` is not, because the second form's effect depends on how many times it ran.

Q: Why is idempotency described as "the single biggest reliability lever" in pipelines?
A: Because networks, brokers, and orchestrators all retry on failure, so any non-idempotent step turns an ordinary transient failure into corrupted data. If every step is retry-safe, you can re-run freely without fear, which underpins disaster recovery, replay, and regulated reruns. Without it, a single timeout on a NAV load can double-book trades and force a manual reconciliation.

Q: What are the three places in a pipeline where idempotency must hold?
A: The source read, the transform, and the sink write — each must be retry-safe on its own. You get an idempotent read by reading a deterministic slice (a fixed date partition rather than "everything new since last time"), an idempotent transform by making it a pure deterministic function of its input, and an idempotent sink write by using upsert/MERGE or overwrite-partition instead of blind append. If any one of the three appends or mutates incrementally, the whole rerun stops being safe.

Q: What is an idempotency key?
A: An idempotency key is a client-supplied identifier attached to a request or record that lets the server answer "have I already applied this?" deterministically. The mechanism is that the sink stores seen keys (or enforces uniqueness on them) so a retried request with the same key is recognised and not applied twice. Payment APIs like Stripe use exactly this: you send an `Idempotency-Key` header so a retried charge does not bill the customer twice.

Q: Why must the idempotency key be supplied by the client rather than generated by the server?
A: Because the whole point is to survive a retry where the server may never have seen the first attempt succeed — only the client knows "this is the same logical request I sent before." If the server minted the key, every retry would look like a brand-new request and get a fresh key, defeating dedup. The client therefore generates the key once and reuses it across all retries of that one logical operation.

Q: What is the difference between a natural dedup key and a synthetic one?
A: A natural key is a business identifier that already uniquely identifies the record, such as an order reference or `ISIN` plus valuation date. A synthetic key is one you mint yourself (a UUID or a hash of the payload) when no business field, or no stable combination of fields, is unique enough to rely on. You prefer natural keys when they exist and are trustworthy, and fall back to synthetic only when the data forces it.

Q: When does a natural business key suffice for deduplication?
A: When some column or combination of columns is guaranteed unique per logical record and is stable across retries — for trades, often `order_ref`, or `ISIN` plus trade date plus a sequence. It suffices precisely when re-sending the same logical row always carries the same value in that key, so a uniqueness constraint can catch the duplicate. If the "unique" field can repeat legitimately (two genuine fills at the same price), the natural key alone is not enough.

Q: When are you forced to mint a synthetic dedup key?
A: When no natural field, or combination, reliably distinguishes one logical record from another — for example a feed of unkeyed events where two genuinely distinct rows can be byte-identical except for arrival order. You then mint a deterministic synthetic key, such as a hash of the meaningful payload plus a source-provided sequence number, so retries of the same row reproduce the same key. The danger is choosing inputs that differ between a retry and the original, which would let duplicates slip through.

Q: Why can hashing the entire row be a poor choice of synthetic key?
A: Because if any volatile field changes between the original send and the retry — an ingestion timestamp, a load-batch id, a reformatted decimal — the hash changes and the retry is treated as a new record, reintroducing the duplicate you were trying to prevent. The fix is to hash only the stable, business-meaningful fields. It is a classic gotcha: the key must be a function of identity, not of incidental metadata.

Q: What is an idempotent write at the sink?
A: An idempotent write is one where re-issuing the same logical insert leaves the table in the same state rather than adding a second row. In Postgres the two main mechanisms are `INSERT ... ON CONFLICT` (upsert) and `MERGE`, both keyed on a unique constraint so a repeat collapses into a no-op or an in-place update. This is the sink-side half of "at-least-once delivery plus idempotent consumer."

Q: What is an upsert?
A: An upsert is a single statement that inserts a row if it does not exist and updates it if it does, decided by a uniqueness check. It is the canonical idempotent write because a repeated load of the same key updates in place instead of appending. In Postgres it is spelled `INSERT ... ON CONFLICT (key) DO UPDATE` (or `DO NOTHING`).

Q: What is the basic syntax of `INSERT ... ON CONFLICT` in Postgres?
A: It is `INSERT INTO t (...) VALUES (...) ON CONFLICT (conflict_target) DO NOTHING` or `... DO UPDATE SET col = EXCLUDED.col`. The `conflict_target` names the column(s) or constraint whose violation triggers the alternative action, and `DO UPDATE` updates the existing conflicting row. For example `INSERT INTO trades (order_ref, qty) VALUES ('A1', 100) ON CONFLICT (order_ref) DO UPDATE SET qty = EXCLUDED.qty`.

Q: What does the special `EXCLUDED` table refer to in `ON CONFLICT DO UPDATE`?
A: `EXCLUDED` references the row that was proposed for insertion but excluded because of the conflict, so `EXCLUDED.col` is the new incoming value. You use it in the `SET` list to copy incoming values onto the existing row, e.g. `SET price = EXCLUDED.price`. It reflects the effect of any `BEFORE INSERT` triggers and requires `SELECT` privilege on the columns referenced.

Q: What is the difference between `ON CONFLICT DO NOTHING` and `ON CONFLICT DO UPDATE`?
A: `DO NOTHING` silently skips the row on conflict, leaving the existing row untouched, which is ideal for an append-only fact where a duplicate carries no new information. `DO UPDATE` overwrites the existing row with the incoming values, which you want when a re-sent record may carry corrections. Choose `DO NOTHING` for pure dedup and `DO UPDATE` when later versions of the same key should win.

Q: Why must `ON CONFLICT` have a unique index or constraint to infer against?
A: Because the database decides "is this a conflict?" by looking for a violation of a specific unique index or constraint (the arbiter), and with no such constraint there is nothing to violate — so there is nothing to do on conflict. The conflict target columns must match a unique or primary-key index. Without one, duplicates can land and the statement cannot tell them apart.

Q: What is a conflict target's "arbiter index"?
A: The arbiter index is the unique index Postgres uses to detect the conflict, inferred from the columns or expression you list in `ON CONFLICT (...)`, or named explicitly via `ON CONFLICT ON CONSTRAINT name`. Inference matches the columns without regard to their order and supports partial unique indexes through a `WHERE` predicate. Naming the constraint directly is less flexible because it breaks if the index is later replaced.

Q: Can `ON CONFLICT DO UPDATE` use a `WHERE` clause, and what does it do?
A: Yes — a `WHERE` after the `SET` list makes the update conditional, so only existing rows matching the predicate are actually updated. A common use is "only update if the incoming version is newer," e.g. `... DO UPDATE SET ... WHERE EXCLUDED.updated_at > t.updated_at`. Note all conflicting rows are still locked even when the `WHERE` is false; rows the predicate excludes are simply not changed.

Q: Why does a repeated run of a `DO NOTHING` upsert leave row counts unchanged?
A: Because the second run's rows all collide with the unique key already present from the first run, so every one of them is skipped and no new rows are inserted. The end state — and therefore the row count and any whole-table checksum — is identical to after the first run. This is precisely the property you assert when you claim a loader is rerunnable.

Q: What is the overwrite-partition pattern?
A: It is the batch-pipeline idiom where a rerun rewrites a deterministic slice of output — typically one date or business partition — by deleting-and-reinserting (or atomically swapping) that slice rather than appending to the table. Because the slice is fully recomputed from its input, re-running produces the same bytes regardless of how many times it runs. It is the partition-level analogue of an idempotent write.

Q: Why does the overwrite-partition pattern make a batch job rerunnable?
A: Because the job's output for a given partition is a pure function of that partition's input: re-executing replaces the entire slice with a freshly computed identical copy, so partial prior runs are erased rather than added to. A failed run that produced half a partition is harmless because the rerun overwrites the whole thing. The key constraint is that the partition boundary must be deterministic, e.g. "all trades dated 2026-06-12," not "rows since last run."

Q: Why is appending fundamentally unsafe for reruns, while overwriting a partition is safe?
A: Appending adds rows unconditionally, so a rerun (or a retried half-run) stacks a second copy on top of the first, doubling the data. Overwriting a deterministic partition discards whatever was there and writes the recomputed result, so the count of runs cannot affect the outcome. The lesson: prefer "replace this slice" semantics over "add these rows" in any step that might be retried.

Q: A loader appends rows and a network blip causes the orchestrator to retry the activity. What is the likely data outcome?
A: You get duplicate rows — the first attempt likely committed some or all of its inserts before the connection dropped, and the retry inserts them again because nothing tells it they already exist. The first thing to check is whether the sink has a unique constraint on the dedup key; if not, the append simply doubles. The fix is to switch the append to an upsert keyed on the natural or synthetic dedup key.

Q: What does "effective exactly-once" mean?
A: It means the observable result is as if each message were processed exactly once, achieved by combining at-least-once delivery with an idempotent consumer — not by guaranteeing the message is physically delivered exactly once. The delivery layer may hand the same message over several times; the consumer deduplicates so duplicates have no effect. This is the practical, achievable form of exactly-once that real systems ship.

Q: Why is true exactly-once delivery often called a myth?
A: Because between any two machines an acknowledgement can be lost, so a sender can never be certain a message arrived; it must choose between resending (risking a duplicate) or not (risking a loss). You cannot get both at-most-once and at-least-once over an unreliable channel, so "exactly-once delivery" is unattainable. What you can build is exactly-once effect: at-least-once delivery plus idempotent processing.

Q: How do at-least-once delivery and an idempotent consumer combine to give exactly-once semantics?
A: At-least-once guarantees no message is lost, at the cost of occasional duplicates; the idempotent consumer guarantees duplicates have no additional effect. Together, every message is reflected in the final state exactly once even though some were delivered twice. The idempotent consumer is what "absorbs" the redundancy the delivery guarantee creates.

Q: Why can you not simply choose at-most-once delivery to avoid duplicates?
A: Because at-most-once drops a message whenever the acknowledgement is uncertain, so you trade duplicates for silent data loss — unacceptable for a trade or NAV feed where a missing record is a reconciliation break. At-least-once keeps everything and pushes the dedup responsibility to the consumer, which is the controllable side. In regulated pipelines, losing data is worse than seeing a duplicate you can dedup.

Q: In a managed orchestrator like Azure Data Factory, what actually re-executes when an activity is retried after failure?
A: The activity is re-run from its start with the same inputs — the orchestrator does not resume mid-statement, it re-invokes the whole activity. So if a copy or stored-procedure activity partially committed before failing, the retry repeats the entire operation, and any non-idempotent side effect happens again. This is exactly why the activity's logic, not the orchestrator, must be idempotent.

Q: Why does retry/rerun behaviour in orchestrators put the idempotency burden on your activity rather than the platform?
A: Because the orchestrator can only re-invoke your activity; it has no way to know which rows your code already wrote, so it cannot "undo" a partial run for you. If your activity appends, the retry double-appends; if it upserts or overwrites a partition, the retry is harmless. The platform gives you retries for free, but only idempotent activity logic makes those retries safe.

Q: What is the relationship between a retry and a rerun?
A: A retry is the orchestrator automatically re-attempting a failed activity in the same pipeline run, usually after a transient error; a rerun is a human or schedule deliberately re-executing the pipeline (often for a past date) to recompute or backfill. Both re-invoke your logic with the same logical inputs, so both demand idempotency. The difference is who triggers it and why, not what safety property is required.

Q: For a trades CSV with order refs and ISINs, how would you decide the natural dedup key?
A: You pick the smallest column set that uniquely identifies one logical trade and is stable across resends — often `order_ref` alone if the source guarantees it is unique. If `order_ref` can repeat across days or systems, you widen it, e.g. `(source_system, order_ref)` or `(ISIN, trade_date, sequence_no)`. You then put a unique constraint on that set so the upsert has an arbiter to detect duplicates.

Q: Why is `ISIN` alone never a valid dedup key for a trades load?
A: Because an `ISIN` identifies a security, not a trade — thousands of trades can share one `ISIN`, so deduping on it would collapse all trades in that instrument into a single row. You need it combined with something that distinguishes the individual trade, such as trade date plus an order reference or sequence. `ISIN` contributes to the key but can never be the whole key for trade-level idempotency.

Q: How would you prove a loader is idempotent after writing it?
A: Run it twice against the same input and compare the table row count and a whole-table checksum after each run; identical numbers prove the second run changed nothing. The checksum matters because counts alone miss in-place mutations — two runs could keep the count but alter values. Committing both results to the repo turns the claim "it is idempotent" into reproducible evidence.

Q: Why is a row-count comparison alone insufficient to prove idempotency?
A: Because a non-idempotent `DO UPDATE` could leave the count unchanged while silently rewriting column values on the second run — for instance overwriting a corrected price back to the original. A row count cannot detect content drift, only cardinality drift. You therefore pair the count with a content checksum so both quantity and contents are verified.

Q: How can you compute a whole-table checksum in Postgres to verify idempotency?
A: Concatenate the rows in a deterministic order and hash the result, for example `SELECT md5(string_agg(t::text, '|' ORDER BY order_ref)) FROM trades t`. The deterministic `ORDER BY` is essential because `string_agg` without ordering can return rows in any sequence, producing a different hash for identical data. Comparing this single value before and after a rerun gives a one-glance idempotency check.

Q: Why must the `ORDER BY` inside a checksum aggregation be deterministic?
A: Because Postgres does not guarantee row order without an explicit `ORDER BY`, so two runs over identical data could concatenate the rows differently and yield different hashes — a false negative on idempotency. Ordering by a unique key (or a unique key set) makes the concatenation, and thus the hash, reproducible. A common gotcha is ordering by a non-unique column, which still leaves ties unordered.

Q: You kill the loader mid-run and then rerun it. What property guarantees the final state is still identical?
A: Idempotency of the sink write plus determinism of the input slice: the rerun reapplies the same upsert (or rewrites the same partition), so whatever the killed run partially committed is overwritten or no-opped into the same end state. The partial run leaves no lasting damage because nothing the loader does depends on run count. You verify this exactly as before — the row count and checksum match the clean single-run baseline.

Q: After a kill-and-rerun, why might the row count be correct but you still need the checksum?
A: Because a kill mid-run can leave rows that were inserted but not yet updated to their final values, or vice versa; an upsert rerun fixes the contents but a count check alone would already have passed even before the fix. The checksum confirms every column landed at its final correct value, not just that the right number of rows exist. It is the content-level proof that the partial run left no subtle corruption.

Q: Is wrapping a load in a single transaction enough to make it idempotent? Why or why not?
A: No — a transaction gives atomicity (all-or-nothing for one attempt) but not idempotency across attempts. A committed transaction that is then retried will run its appends again, so you get atomic duplication. Transactions and idempotency are orthogonal: you need the write itself to be a no-op on repeat (upsert/overwrite), and the transaction just makes each attempt clean.

Q: How do `INSERT ... ON CONFLICT` and `MERGE` differ for concurrent upserts in Postgres?
A: `INSERT ... ON CONFLICT` is specifically designed for concurrent insert conflicts and handles them gracefully under high concurrency, collapsing a race into an update. `MERGE` follows ordinary transaction-isolation rules and can raise a `unique_violation` or cardinality violation when concurrent sessions race on the same key. For a simple concurrent upsert, the Postgres docs recommend `ON CONFLICT`; reserve `MERGE` for multi-action conditional logic.

Q: In which version was `MERGE` introduced in PostgreSQL?
A: `MERGE` was introduced in PostgreSQL 15. Before that, the idiomatic upsert was `INSERT ... ON CONFLICT`, available since PostgreSQL 9.5, which remains the recommended choice for plain concurrent upserts. If you target an older server, `MERGE` is simply unavailable and you must use `ON CONFLICT` or a manual staged approach.

Q: What can `MERGE` do that `INSERT ... ON CONFLICT` cannot?
A: `MERGE` can perform `INSERT`, `UPDATE`, and `DELETE` in a single statement across multiple `WHEN` branches — including `WHEN NOT MATCHED BY SOURCE` to delete target rows with no source match. `ON CONFLICT` only inserts with an optional update of the conflicting row and has no delete branch. So `MERGE` suits a full reconcile-and-sync; `ON CONFLICT` suits a focused idempotent insert.

Q: Why does `MERGE` not provide an `EXCLUDED` pseudo-table?
A: Because `MERGE` joins an explicit source to the target, so inside a `WHEN` clause you reference the source columns directly by their alias rather than through a synthetic `EXCLUDED` row. `ON CONFLICT` has no join — it inserts proposed values — so it needs `EXCLUDED` to expose the row that was about to be inserted. The two statements have different shapes, hence different ways to name incoming values.

Q: What is a staged MERGE (load-to-staging-then-merge) and why use it?
A: You bulk-load the incoming batch into a staging table, then run a single `MERGE` (or `INSERT ... SELECT ... ON CONFLICT`) from staging into the target keyed on the dedup key. It is fast because the bulk load avoids per-row conflict handling, and it is idempotent because the merge step collapses duplicates against the target. It also lets you validate or transform in staging before touching the production table.

Q: Why can a non-deterministic transform break pipeline idempotency even with an idempotent sink?
A: Because if the transform produces different output for the same input on each run — say it stamps `now()` into a column or sorts non-deterministically — then the "same" row carries different content, so an upsert keeps rewriting it and a checksum never stabilises. Idempotency of the whole pipeline requires the transform to be a pure function of its input. Pushing volatile values like wall-clock time outside the dedup-relevant columns is the usual fix.

Q: What makes a source read non-idempotent, and how do you fix it?
A: A read is non-idempotent when "what it returns" depends on hidden state, such as "give me everything new since the last offset" where the offset advances destructively — a retry then sees a different (smaller) set. You fix it by reading a deterministic, named slice: a fixed date partition, an explicit offset range, or a snapshot, so re-reading returns the identical rows. The principle is that the read's result must be a function of stated parameters, not of mutable cursor state.

Q: Why is `SELECT ... FROM feed WHERE loaded = false` a fragile basis for an idempotent load?
A: Because the predicate depends on a mutable flag that the load itself flips, so a crash between reading and flipping leaves the same rows visible to a retry, and a crash after flipping but before committing can hide rows entirely. The read is coupled to side effects, which is exactly what idempotency forbids. A deterministic partition or an atomic claim-and-commit is more robust.

Q: How does a uniqueness constraint act as a last line of defence for idempotency?
A: Even if upstream logic accidentally tries to insert a duplicate, a `UNIQUE` constraint on the dedup key makes the database reject it, surfacing the problem instead of silently doubling data. Combined with `ON CONFLICT DO NOTHING`, the constraint turns a would-be duplicate into a harmless no-op. It converts "we hope nothing duplicates" into "the schema guarantees nothing duplicates."

Q: What is the risk of relying only on application-side dedup without a database constraint?
A: If two concurrent loaders both check "does this key exist?" and both see "no," they will both insert, creating a duplicate that the application logic believed it had prevented. Only a unique constraint enforced by the database closes this race, because the second insert hits the constraint. Application checks are advisory; the constraint is authoritative.

Q: Why is choosing the primary key well central to idempotency?
A: Because the primary (or unique) key is the identity the sink uses to recognise "I have seen this before," so a wrong or too-narrow key either lets duplicates through or wrongly collapses distinct rows. The key must exactly match the business notion of "the same logical record." Getting it right is the design decision that makes every upsert and overwrite correct.

Q: A double run of your loader yields different checksums but identical row counts. What is the most likely cause?
A: Some column is being written non-deterministically — a load timestamp, a generated UUID, a `random()` default, or a `now()` value — so contents differ run to run even though cardinality does not. The first thing to check is which columns differ between the two runs, typically by diffing or hashing per column. The fix is to make those columns deterministic or exclude them from the meaningful payload.

Q: A double run yields different row counts. Where do you look first?
A: At the sink write: it is almost certainly appending rather than upserting, or the unique constraint that the upsert relies on is missing or does not match the conflict target. Confirm the dedup key has a unique index and that the `ON CONFLICT` target names it correctly. If the constraint is absent, the database has no way to recognise the duplicates and every row inserts twice.

Q: Why is the order reference often the ideal idempotency key for a trade load?
A: Because an order reference is a stable, source-issued identifier for one logical order that the upstream system reuses if it resends — exactly the property an idempotency key needs. Re-sending the same order carries the same reference, so a unique constraint on it catches the duplicate. You should still confirm uniqueness scope (per source, per day) before trusting it as the sole key.

Q: In a transfer-agency subscription/redemption feed, why does idempotency matter for investor positions?
A: Because a retried or replayed subscription that is not deduplicated would credit the investor's holding twice, corrupting their position and any downstream NAV-based fees or statements. Keying the load on a stable deal reference and upserting ensures a redelivered instruction updates rather than re-applies. In a regulated TA context this is the difference between a clean book and a reportable error.

Q: How does idempotency relate to EMT/EPT file ingestion?
A: EMT and EPT files (the European MiFID/PRIIPs templates) are periodic regulatory data drops that may be re-sent as corrected versions, so the loader must key on the instrument identifier plus reporting period and upsert, letting a corrected file overwrite rather than duplicate. Without that, a resent EMT file doubles the instrument rows and breaks downstream cost/charge reporting. The natural key is typically the `ISIN` plus the reporting reference date.

Q: Why is overwrite-by-partition a natural fit for a daily NAV load?
A: Because a NAV is computed per fund per valuation date, so the deterministic slice "fund X, date D" is exactly one partition that a rerun can fully replace — recomputing it yields the same value. If a valuation is corrected, overwriting the partition cleanly supersedes the prior figure with no duplicate NAV rows. The valuation date gives you a stable, reproducible partition boundary.

Q: How would you make a SWIFT message ingestion idempotent?
A: Key on the message's unique reference fields — for an MT message the transaction reference (field 20) and related references — and upsert so a redelivered message updates rather than reinserts. SWIFT networks can redeliver, so without dedup you would book the same settlement instruction twice. The message reference is the natural idempotency key the standard already provides.

Q: Why is an LEI not a suitable idempotency key for transaction records?
A: Because an LEI identifies a legal entity (a counterparty or fund), not an individual transaction, so many records share the same LEI — deduping on it would wrongly collapse distinct trades or instructions for that entity. Like `ISIN`, an LEI is a useful attribute but identifies a party, not an event. The idempotency key must be the record's own identity, e.g. a transaction reference.

Q: Under DORA's resilience expectations, how does idempotency support disaster recovery?
A: Because recovery often means replaying or re-running ingestion from a checkpoint, idempotent loads let you re-execute without fear of double-counting, so the recovered state matches the pre-incident state exactly. DORA pushes regulated financial entities to demonstrate they can recover cleanly; a non-idempotent pipeline cannot make that demonstration. Idempotency is what makes "just re-run it" a safe recovery action rather than a second incident.

Q: Why does a replay-based architecture (reprocessing an event log) demand idempotent consumers?
A: Because replay re-delivers events that were already processed once, so any consumer that is not idempotent will double-apply them and corrupt derived state. The whole appeal of an event log — rebuild state by replaying from the start — only works if applying an event twice equals applying it once. Idempotent consumers are the precondition that makes replay a feature rather than a hazard.

Q: What is the difference between idempotency and commutativity?
A: Idempotency means repeating the same operation has no extra effect; commutativity means the order of two different operations does not change the result. They are independent properties: an upsert is idempotent, while two upserts to different keys are also commutative, but a "last write wins" upsert to the same key is order-sensitive. Distinguishing them matters when reasoning about out-of-order delivery, not just duplicates.

Q: If messages can arrive out of order, why is a plain "last write wins" upsert sometimes wrong?
A: Because an older message arriving after a newer one will, under naive last-write-wins, overwrite the newer correct value with stale data — idempotent against duplicates but not safe against reordering. The fix is a version or timestamp guard, e.g. `DO UPDATE ... WHERE EXCLUDED.version > t.version`, so only strictly newer data wins. This combines idempotency (duplicates no-op) with monotonicity (stale updates rejected).

Q: How does a monotonic version column make an upsert safe against both duplicates and reordering?
A: A duplicate carries the same version, so the `WHERE EXCLUDED.version > t.version` guard makes it a no-op; a stale (older) message carries a lower version and is also rejected; only a genuinely newer version updates. This single guard gives you idempotency and ordering safety at once. The version must be sourced from the producer's logical clock, not the loader's arrival time.

Q: Why is `created_at = now()` a dangerous default on an idempotently loaded table?
A: Because `now()` is evaluated on each insert attempt, so a re-inserted (or upserted-then-reinserted) row can carry a different timestamp than the original, breaking content checksums and confusing audit trails. If you need an immutable creation time, set it only on first insert (e.g. leave it untouched in the `DO UPDATE`) or source it from the data. A wall-clock default is a frequent silent cause of "the data keeps changing on rerun."

Q: In `INSERT ... ON CONFLICT DO UPDATE`, how do you preserve the original `created_at` while still updating other columns?
A: Simply omit `created_at` from the `SET` list — `DO UPDATE` only changes the columns you name, so the existing `created_at` is left intact while you update, say, `price = EXCLUDED.price`. Only set `created_at = EXCLUDED.created_at` if you actually want the latest send's value. This is how you get an immutable insertion time alongside mutable business fields.

Q: What restriction does Postgres place on `ON CONFLICT` and partitioned tables?
A: You cannot use `ON CONFLICT DO UPDATE` to change a partition key in a way that would move the row to a different partition; ordinary column updates are fine. The conflict handling otherwise works across the partitioned table via its unique indexes. The restriction exists because moving a row between partitions is a delete-plus-insert, which the upsert path does not perform.

Q: Why can't an exclusion constraint serve as the arbiter for `ON CONFLICT DO UPDATE`?
A: Because `ON CONFLICT DO UPDATE` only supports unique indexes and unique/primary-key constraints as arbiters; exclusion constraints are not supported for the `DO UPDATE` action. You can still get a conflict error from an exclusion constraint, but you cannot route it into an upsert. For idempotent writes you therefore rely on a plain unique constraint over the dedup key.

Q: What cardinality rule does `ON CONFLICT` enforce within a single statement?
A: A single `INSERT ... ON CONFLICT` cannot affect the same existing row more than once, and the proposed rows must not duplicate each other in the arbiter index terms; violating this raises a cardinality error. So if your one statement contains two rows with the same dedup key, Postgres rejects it rather than applying them twice. You must dedup the incoming batch before the upsert, or load via staging.

Q: Why might `INSERT ... ON CONFLICT` unexpectedly raise a unique violation, and when?
A: While `CREATE INDEX CONCURRENTLY` or `REINDEX CONCURRENTLY` is running on the relevant unique index, `INSERT ... ON CONFLICT` statements on that table may unexpectedly fail with a unique violation. This is a documented transient condition tied to concurrent index builds, not a flaw in your loader. The mitigation is to avoid concurrent index maintenance during heavy upsert load, or to retry the failed statement.

Q: What SQLSTATE does Postgres raise on a unique-constraint violation, and why does it matter for idempotency?
A: It raises SQLSTATE `23505` (`unique_violation`). It matters because a loader can catch `23505` to treat the duplicate as "already loaded" and continue, turning an error into an idempotent no-op — though using `ON CONFLICT DO NOTHING` is cleaner than catching the exception. Knowing the exact code lets you write precise error handling rather than swallowing all errors.

Q: Why is catching `unique_violation` in application code an inferior idempotency mechanism compared to `ON CONFLICT`?
A: Because catching the exception means the failed `INSERT` has already aborted the surrounding transaction (or savepoint), so you must roll back and retry, which is slower and more error-prone than letting the database resolve the conflict in one statement. `ON CONFLICT` resolves the duplicate inline without aborting anything. Exception-catching also risks masking unrelated `23505`s from a different constraint.

Q: How can a savepoint help if you must rely on catching duplicate-key errors?
A: Wrapping each risky insert in a savepoint lets you roll back just that statement on `23505` without discarding the whole transaction, then continue with the next row. Without a savepoint, the first unique violation poisons the entire transaction and all subsequent statements fail with `25P02` (in-failed-sql-transaction). Still, `ON CONFLICT` is preferable because it avoids the abort entirely.

Q: What is "at-least-once" delivery in one sentence, and what does it cost you?
A: At-least-once guarantees every message is delivered one or more times — never lost — at the cost of occasional duplicate deliveries. The duplicates are the price of never losing data over an unreliable channel where acknowledgements can vanish. You pay that price by making the consumer idempotent so the duplicates are harmless.

Q: Why is idempotency a precondition for safe automatic retries in any orchestrator?
A: Because automatic retries re-execute work whose success was uncertain, and only idempotent work can be re-executed without changing the result; non-idempotent work duplicates or corrupts on each retry. So enabling retries on a non-idempotent activity actively manufactures bad data. The rule is: make the step idempotent first, then turn retries on.

Q: What does "deterministic slice" mean and why is it required for rerunnable batch output?
A: A deterministic slice is an output partition whose membership is fixed by stated parameters (e.g. all rows for valuation date D), independent of when or how often the job runs. It is required because a rerun must recompute and replace exactly the same set; a slice defined by mutable state ("since last run") would shift between runs and break the overwrite. Determinism of the slice is what lets overwrite equal idempotence.

Q: Why is "process everything since the last watermark, then advance the watermark" risky on failure?
A: Because if the job advances the watermark before its output is durably committed, a crash loses the in-flight slice (the watermark moved past data never written); if it advances after but crashes mid-write, a rerun reprocesses an overlapping slice. Either way the read is coupled to mutable cursor state, which is non-idempotent. Committing the output and the watermark atomically, or overwriting a deterministic partition, removes the hazard.

Q: How does idempotency interact with exactly-once stream processing frameworks?
A: Such frameworks typically achieve exactly-once effect by combining checkpointed at-least-once delivery with either idempotent sinks or transactional (two-phase) sink commits, so reprocessing after a checkpoint restore does not double-apply. The framework gives you the replay; the idempotent or transactional sink gives you the "once" semantics. It is the same at-least-once-plus-idempotent-consumer pattern, packaged.

Q: Why is a transactional sink an alternative to an idempotent sink for exactly-once effect?
A: Because a sink that commits its writes in the same transaction (or coordinated commit) as the consumer's offset advance ensures either both happen or neither, so a replay re-reads from an offset whose effects were never committed — no duplicate appears. Idempotency tolerates the duplicate write; transactionality prevents the duplicate write from ever committing. Both yield exactly-once effect; the choice depends on whether your sink supports transactional commit.

Q: When is `DO NOTHING` preferable to `DO UPDATE` for a fund-data load?
A: When the records are immutable facts that are never legitimately corrected via resend — for example a settled, finalised trade where a redelivery should be ignored rather than overwrite the booked row. `DO NOTHING` then makes redeliveries pure no-ops and protects the original. If the source can send corrections, you instead want `DO UPDATE` (often guarded by a version) so the correction wins.

Q: What problem arises if two columns can both serve as a unique key but you upsert on only one?
A: You can get a conflict on the column you did not name — the insert violates the second unique constraint and aborts instead of being caught by `ON CONFLICT`, raising `23505`. The conflict target must match the constraint that will actually be violated. The fix is to choose one true dedup key and ensure the conflict target names exactly that constraint.

Q: How do you make a Python loader idempotent end-to-end with `uv run`?
A: Read a deterministic input slice, transform it as a pure function (no wall-clock or random values in keyed columns), and write with a parameterised `INSERT ... ON CONFLICT (key) DO UPDATE`/`DO NOTHING`; invoke the whole thing via `uv run loader.py` so the environment is reproducible. Reproducible environment plus deterministic read plus idempotent write yields a rerunnable loader. You then prove it with the double-run count-and-checksum check.

Q: Why does using `uv run` rather than a bare `python` invocation matter for reproducible idempotency evidence?
A: Because `uv run` resolves and uses the project's locked dependency set, so the loader behaves identically across machines and reruns — a different pandas or driver version could change formatting and thus the checksum. Reproducibility of the runtime is part of reproducibility of the result. Running bare `python` risks picking up a different environment and producing non-comparable evidence.

Q: Why should the dedup key be enforced at the schema level even in a "trusted" internal pipeline?
A: Because trust is not a guarantee — a code change, a bad backfill, or a concurrent run can still attempt duplicates, and only a `UNIQUE` constraint stops them at the database boundary. The constraint converts an entire class of "we assumed it wouldn't happen" bugs into immediate, visible errors. It is cheap insurance that makes the idempotency property structural rather than aspirational.

Q: What does "the design is not finished until you can point to where idempotency holds" mean for an architect?
A: It means for every step — read, transform, write — you must be able to name the concrete mechanism that makes a rerun safe (a deterministic partition, a pure transform, an upsert on a stated key). If any step has no such answer, that step is a latent double-booking waiting for the next retry. Architecting includes locating and naming each idempotency boundary, not just drawing boxes and arrows.

Q: How would you demonstrate idempotency evidence in a way an auditor accepts?
A: Commit to the repo the exact commands run, the row counts and whole-table checksums after the first run and after the second run (and after a kill-and-rerun), showing they are identical. Reproducibility plus a content-level checksum gives objective, re-verifiable proof rather than assertion. In a regulated context this turns "the load is rerunnable" into evidence that survives challenge.

Q: Why is appending with a "load batch id" column not a substitute for idempotency?
A: Because tagging each append with a batch id still leaves multiple physical copies of the same logical record in the table, so downstream queries double-count unless they always filter to one batch — fragile and easy to forget. Idempotency means the table holds one correct row per key regardless of run count, not many rows you must remember to filter. Batch ids help lineage, not dedup.

Q: How does idempotency support backfilling a historical date range?
A: Because each date is a deterministic partition that the backfill overwrites (or upserts) independently, you can re-run any past date — or the whole range — repeatedly and converge on the same correct state. A non-idempotent backfill would stack duplicates on dates that already had data. Idempotency is precisely what makes "just backfill 2026-01 through 2026-05 again" a safe operation.

Q: What is the danger of making the synthetic key depend on input row order?
A: If the key incorporates a position or arrival sequence that the source does not reproduce on resend, a retried batch in a different order yields different keys, so the same logical rows look new and duplicate. The synthetic key must derive only from stable content, not from where a row happened to sit in the file. A reordered redelivery is a realistic event you must design against.

Q: How does idempotency change the way you handle "did my write succeed?" uncertainty?
A: Instead of trying to determine whether the prior attempt committed — often impossible after a lost ack — you simply re-issue the idempotent write, knowing a repeat is harmless. Idempotency converts an unanswerable question into a no-op, which is its core operational value. This is why retry-on-uncertainty is safe only when the operation is idempotent.

Q: Why is "exactly-once" a property of the system as a whole rather than of the network?
A: Because no network can deliver exactly once, but a system composed of an at-least-once channel plus an idempotent (or transactional) consumer can produce exactly-once effects on its state. The guarantee lives at the boundary where duplicates are absorbed, not on the wire. Saying a system is exactly-once is a statement about its end-to-end design, not its transport.

Q: In a staged-load design, where exactly does deduplication happen?
A: Either in the staging table before the merge (dedup the batch so the cardinality rule is not violated) or in the `MERGE`/`ON CONFLICT` step that resolves staging rows against the target by key. Often you do both: collapse intra-batch duplicates in staging, then let the upsert handle batch-vs-target duplicates. Splitting the two stages keeps each step simple and lets you validate before touching production.

Q: Why must you dedup the incoming batch before an `ON CONFLICT` upsert if it can contain repeated keys?
A: Because `ON CONFLICT` forbids affecting the same target row twice in one statement and rejects proposed rows that duplicate each other in the arbiter terms, raising a cardinality error. So two rows with the same key in one `INSERT` fail rather than upsert. You pre-dedup (e.g. keep the latest per key via `DISTINCT ON`) or load through staging to avoid this.

Q: How would you use `DISTINCT ON` to keep the latest version per key before upserting in Postgres?
A: Use `SELECT DISTINCT ON (order_ref) * FROM staging ORDER BY order_ref, version DESC` to collapse each key to its highest-version row, then upsert that result. `DISTINCT ON` keeps the first row per group as ordered, so ordering by the key then by version descending yields the latest per key. This satisfies `ON CONFLICT`'s one-row-per-key requirement deterministically.

Q: Why is determinism of the transform also a regulatory concern, not just a correctness one?
A: Because in a regulated fund context you may have to reproduce a past output for an auditor or regulator, and only a deterministic transform over a preserved input can regenerate the identical figure. Non-determinism means you cannot prove what you produced on a given day. Idempotent, deterministic pipelines make historical reproduction — and therefore defensible reporting — possible.

Q: What is the relationship between idempotency and "rerunnable" batch jobs in DDIA's framing?
A: DDIA frames a well-designed batch job as one whose output is a deterministic function of its input, so it can be safely re-run on failure with no externally visible difference — that re-runnability is idempotency at the job level. The job avoids in-place mutation of shared state and instead produces output that fully replaces the prior run's output. Idempotency is the property; rerunnability is its operational payoff.

Q: A scheduled daily job ran twice (a duplicate trigger fired) and downstream NAV is now wrong. What is the root-cause class?
A: The job's sink write is non-idempotent — most likely appending — so the duplicate trigger doubled that day's rows; an idempotent overwrite-partition or upsert would have made the second trigger a no-op. The first thing to check is whether the day's partition holds duplicate rows for the same keys. The durable fix is to make the load idempotent so a stray re-trigger cannot corrupt NAV.

Q: Why is "idempotent consumer" the more controllable half of exactly-once than "exactly-once delivery"?
A: Because you cannot change the physics of an unreliable network to deliver exactly once, but you fully control your consumer's logic and can make it dedup. Engineering effort spent making delivery perfectly once is wasted on an impossible target; effort spent on an idempotent consumer is achievable and sufficient. So the design pressure goes to the consumer, where you have leverage.

Q: How does an idempotency key store (a "seen keys" table) implement an idempotent consumer?
A: The consumer records each processed key (often within the same transaction as its effect) and checks that store before acting, skipping any key already present. The atomic write of effect-plus-key is what makes a redelivery a no-op even across crashes. This is the explicit, general form of what a unique constraint plus `ON CONFLICT DO NOTHING` does implicitly for an insert.

Q: Why must the "seen keys" record be written in the same transaction as the side effect it guards?
A: Because if the effect commits but the key record does not (or vice versa), a redelivery either re-applies the effect or wrongly skips an unapplied one — the two must succeed or fail together to stay consistent. Atomicity of effect-plus-key is the crux of a correct idempotent consumer. Splitting them across transactions reintroduces exactly the double-apply you were preventing.

Q: When is `ON CONFLICT DO UPDATE` effectively a no-op on rerun, and when does it still "change" the row?
A: It is a true no-op when the incoming values equal the stored values, so the update writes the same data; the visible state is unchanged even though an update technically executed. It would change the row if any source-derived column differs — which on a clean rerun of deterministic data it should not. The checksum staying constant is the proof the update was effectively idempotent.

Q: Summarise the idempotency checklist an architect should apply to every pipeline stage.
A: For the read, confirm it returns a deterministic slice independent of run count; for the transform, confirm it is a pure function with no wall-clock or random keyed values; for the write, confirm it upserts or overwrites a partition on a unique business or synthetic key. Then prove the whole thing with identical row counts and checksums across a double run and a kill-and-rerun. If you can point to the mechanism at all three stages and show the evidence, the design's retry-safety is finished.

Q: Your `INSERT ... ON CONFLICT (order_ref) DO UPDATE` fails with `there is no unique or exclusion constraint matching the ON CONFLICT specification`. What is the first thing to check?
A: Check that a unique index or constraint exists on exactly the columns named in the conflict target — here `order_ref` — because Postgres infers the arbiter from a matching unique index and raises that error (SQLSTATE `42P10`) when none matches. A common cause is a unique index on `(source_system, order_ref)` while you wrote `ON CONFLICT (order_ref)`, so the column set does not match. Fix by either adding the single-column unique index or widening the conflict target to match the existing one.

Q: You run `INSERT ... ON CONFLICT (order_ref) DO UPDATE` on a partitioned trades table and it errors. Why, and what works instead?
A: In older Postgres, `DO UPDATE` on a partitioned table fails because uniqueness could not be enforced globally across partitions, so there is no single arbiter to update against. Modern Postgres supports it only when the unique index includes the partition key (so the constraint is enforceable per partition); otherwise you must use `DO NOTHING`, which is always allowed, or upsert directly into the specific child partition. The reliable cross-version idempotent choice on a partitioned table is `ON CONFLICT DO NOTHING` plus a deterministic overwrite of the partition.

Q: How would you count how many rows a `DO NOTHING` upsert actually inserted versus skipped on a rerun?
A: Add `RETURNING` to the statement — `INSERT ... ON CONFLICT DO NOTHING RETURNING id` — because Postgres returns only the rows actually inserted, omitting the skipped conflicting rows. On a clean rerun of already-loaded data the `RETURNING` set is empty, which is direct proof every row was a no-op. Counting the returned rows gives you a per-run insert tally without a separate before/after `COUNT(*)` comparison.

Q: You need to know which existing rows an `ON CONFLICT DO NOTHING` skipped, but `RETURNING` gives nothing back. What are your options?
A: `RETURNING` only surfaces newly inserted rows, never the skipped conflicting ones, so to see the pre-existing rows you run a separate `SELECT` against the dedup keys, or in PostgreSQL 19+ use the new `ON CONFLICT ... DO SELECT` to return the conflicting rows. A common workaround on older versions is a no-op `DO UPDATE SET col = col` so the row qualifies for `RETURNING`, but that incurs needless write amplification. The clean answer is a follow-up `SELECT` keyed on the same dedup columns.

Q: Two parallel loader instances both run `ON CONFLICT DO UPDATE` on overlapping keys and you see a `deadlock detected` error. What SQLSTATE is it and how do you handle it?
A: It is SQLSTATE `40P01` (`deadlock_detected`), raised when two transactions acquire row locks in opposite orders and Postgres aborts one to break the cycle. Because the upsert is idempotent, the correct handling is simply to retry the aborted transaction — re-running it converges on the same state. Reducing deadlocks further means inserting in a consistent key order so both sessions lock rows in the same sequence.

Q: Under `SERIALIZABLE` isolation your idempotent loader aborts with `could not serialize access`. What code is this and why is retry safe?
A: It is SQLSTATE `40001` (`serialization_failure`), which `SERIALIZABLE` and `REPEATABLE READ` raise when concurrent transactions cannot be ordered into a consistent serial schedule. Retry is safe precisely because the loader is idempotent: re-executing the whole transaction reproduces the same final state with no duplication. The Postgres docs explicitly say applications at these isolation levels must be prepared to retry on `40001`, so a retry loop around the idempotent load is the intended design.

Q: Why must a serialization-failure retry re-run the entire transaction, not just re-issue the failed statement?
A: Because `40001` means the database could not order the whole transaction consistently, so any values your code chose based on reads inside that transaction may now be invalid; only re-running the full logic, including the reads, produces a correct result. Re-issuing just the last statement would commit decisions made against a now-superseded snapshot. This is why retry loops wrap the begin-to-commit block, and why the loop count itself must not feed any keyed column.

Q: How does idempotency let you set a generous orchestrator retry count without fear?
A: Because each retry of an idempotent activity converges on the same state, you can set a high retry count (and exponential backoff) to ride out long transient outages, knowing extra attempts cannot duplicate data. A non-idempotent activity would multiply corruption with every retry, forcing you to keep the count at one and lose resilience. The design rule is: make the activity idempotent first, then turn retries up rather than down.

Q: An ADF copy activity has a "fault tolerance / skip incompatible rows" option. How does that interact with idempotency?
A: Skipping incompatible rows changes which rows land but does not by itself make the load rerunnable — a rerun still re-copies the surviving rows, so without an idempotent sink you get duplicates of the rows that did succeed. Fault tolerance addresses bad-row handling, not retry-safety, and the two are independent concerns. You still need an upsert or overwrite-partition sink so that re-copying the same compatible rows is a no-op.

Q: Your daily NAV overwrite-partition job deletes the partition then inserts, but a crash between delete and insert leaves the partition empty. How do you make the swap atomic?
A: Wrap the `DELETE` and `INSERT` in one transaction so they commit together — a crash rolls back the delete and leaves the prior data intact, and the rerun redoes the whole slice. Better still, load into a staging partition and use an atomic `ALTER TABLE ... ATTACH/DETACH PARTITION` or a single transactional swap so readers never see an empty slice. The hazard is doing delete and insert in separate transactions, which exposes a window of missing NAV.

Q: Why is `TRUNCATE` then `INSERT` inside one transaction a valid idempotent overwrite for a whole table?
A: Because `TRUNCATE` is transactional in Postgres — it rolls back if the transaction aborts — so the truncate-and-reload either fully replaces the table or leaves it untouched, and a rerun reproduces identical contents. It is faster than `DELETE` for clearing an entire small dimension table because it does not scan rows. The constraint is that it overwrites the entire table, so it suits full-refresh dimensions, not a single partition of a large fact table.

Q: How does `INSERT INTO target SELECT ... FROM staging ON CONFLICT DO UPDATE` differ operationally from row-by-row upserts?
A: The set-based form runs one statement that the planner optimises and that takes locks once, whereas row-by-row upserts pay per-statement parse, plan, and round-trip overhead and hold the transaction open longer. Both are idempotent, but the staged set-based upsert is dramatically faster for thousands of trades and is the recommended bulk pattern. The catch is you must pre-dedup staging so no two rows share the arbiter key within the one statement.

Q: In your staged trades load, the `INSERT ... SELECT ... ON CONFLICT` raises a cardinality violation. What in staging caused it and how do you fix it?
A: Staging contains two or more rows with the same dedup key, and `ON CONFLICT` forbids affecting the same target row twice in one statement, raising `ON CONFLICT DO UPDATE command cannot affect row a second time` (SQLSTATE `21000`). Fix it by collapsing staging to one row per key first, e.g. `SELECT DISTINCT ON (order_ref) ... ORDER BY order_ref, version DESC`. This guarantees the upsert sees a single proposed row per arbiter key.

Q: A corrected EMT file arrives with the same `ISIN` and reporting date but updated cost figures. Which upsert action and guard do you want?
A: You want `ON CONFLICT (isin, reporting_date) DO UPDATE` so the correction overwrites the prior figures rather than being skipped, guarded by `WHERE EXCLUDED.file_version > t.file_version` (or a newer timestamp) so only genuinely later versions win. `DO NOTHING` would wrongly keep the stale figures, and an unguarded `DO UPDATE` would let a late-arriving old file clobber a newer correction. The version guard gives idempotency against duplicates plus correctness under reordering.

Q: How does the overwrite-partition pattern handle a deletion that should remove a trade from a prior NAV date?
A: Because the partition is fully recomputed from its current input, a trade absent from the recomputed slice simply does not appear after the overwrite — deletes are handled for free by rewriting the whole slice. A pure upsert load, by contrast, never removes rows the source dropped, so it cannot express deletions without an explicit delete step. This is a key reason overwrite-partition is preferred when the source is the authoritative full picture of a slice.

Q: Why might `ON CONFLICT DO UPDATE` alone fail to reflect deletions, and what pattern fixes it?
A: An upsert only inserts or updates the keys present in the incoming batch; keys that vanished from the source are left untouched in the target, so logically deleted records linger. The fix is either overwrite-partition (rewrite the whole slice, so absent keys disappear) or a `MERGE` with a `WHEN NOT MATCHED BY SOURCE THEN DELETE` branch. Choose based on whether the batch represents a complete slice or only changed rows.

Q: A `MERGE ... WHEN NOT MATCHED BY SOURCE THEN DELETE` is run twice. Is it idempotent, and why?
A: Yes — the first run reconciles the target to match the source (inserting, updating, deleting as needed), and the second run finds the target already equal to the source, so every branch is a no-op. Idempotency holds because the end state is a pure function of the source snapshot, independent of run count. The caveat is that the source must be a deterministic, complete slice; a partial or shifting source would delete rows that should remain.

Q: Why is `MERGE` not guaranteed to be concurrency-safe for upserts the way `INSERT ... ON CONFLICT` is?
A: `MERGE` evaluates its `WHEN MATCHED`/`WHEN NOT MATCHED` decision against a snapshot, so two concurrent merges can both decide "not matched" and both attempt an insert, producing a `unique_violation` or a duplicate depending on constraints. `INSERT ... ON CONFLICT` is built specifically to collapse such insert races into an update atomically. The Postgres docs therefore steer plain concurrent upserts to `ON CONFLICT` and reserve `MERGE` for richer conditional logic where you control concurrency.

Q: When a `MERGE` hits a concurrent insert race and raises `23505`, what is the idempotent recovery?
A: Catch or let the transaction abort, then retry the whole `MERGE` — on the second pass the now-committed row makes the `WHEN MATCHED` branch fire, so the retry updates instead of inserting and converges correctly. The retry is safe because the merge's outcome is a function of the source snapshot, not of how many attempts it took. Wrapping the merge in a bounded retry loop on `23505`/`40001` is the standard hardening.

Q: How would you express the dedup key for a SWIFT MT540-series settlement message that can be redelivered?
A: Key on the sender's transaction reference (field `:20:`) optionally combined with the related reference (field `:21:`) and the message type, because SWIFT guarantees the sender's reference is unique per instruction and reused on redelivery. Upserting on that composite makes a redelivered MT54x a no-op rather than a second booked settlement. The reference fields are exactly the stable, source-issued identity an idempotency key needs.

Q: For an ISO 20022 message such as a `sese.023` settlement instruction, what field anchors idempotency?
A: The transaction or instruction identifier carried in the message (for example the `TxId`/`InstrId` within the settlement instruction) anchors it, because it is the sender-assigned unique reference that survives redelivery. You upsert keyed on that identifier so a replayed `sese.023` updates rather than re-books. As with SWIFT MT, the standard supplies the natural idempotency key; you must not invent one from volatile envelope metadata.

Q: An investor's subscription is replayed and you key the load on the deal reference, but two genuine subscriptions share that reference. What went wrong and how do you widen the key?
A: The deal reference was not unique per logical instruction — perhaps it is reused per investor per day — so deduping on it collapsed two real subscriptions into one, understating the position. Widen the key to a combination that is truly unique per instruction, such as `(deal_ref, investor_id, instruction_seq)` or a source-issued message id. The lesson is to validate the uniqueness scope of any natural key against real data before trusting it.

Q: How do you decide between `DO NOTHING` and `DO UPDATE` for a transfer-agency subscription feed?
A: If the feed only ever sends new immutable instructions and never corrects them, `DO NOTHING` makes redeliveries harmless no-ops and protects the booked instruction. If the feed can send a corrected amount or status for the same deal reference, you need `DO UPDATE` (version-guarded) so the correction wins while duplicates still no-op. The decision hinges on whether the source semantics include in-place corrections.

Q: Why is `now()` evaluated once per statement in Postgres, and how does that subtly affect idempotency reasoning?
A: `now()` (and `CURRENT_TIMESTAMP`) returns the transaction start time, so it is stable within one transaction but differs across the separate transactions of two runs — meaning a rerun that re-inserts a row stamps a different value. For idempotency this means even a "transaction-stable" clock is not run-stable, so it still breaks content checksums if placed in a persisted column. Use `clock_timestamp()` only for elapsed-time measurement, never for a value you persist and later compare.

Q: You want an immutable `loaded_at` audit column but also a fully reproducible checksum. How do you reconcile them?
A: Exclude `loaded_at` from the checksum's column list (hash only business-meaningful columns) so the audit timestamp can vary without breaking the idempotency proof. Alternatively, source the timestamp from the data itself (e.g. the file's reporting date) so it is deterministic across reruns. The principle is to separate audit/lineage metadata from the identity-and-content payload that idempotency checks.

Q: Why is a column-scoped checksum sometimes better than a whole-row `md5(t::text)` for proving idempotency?
A: A whole-row cast includes every column, so any volatile audit field (load timestamp, batch id) makes the hash differ run-to-run and produces false negatives. Listing only the business columns — `md5(string_agg((isin || '|' || qty || '|' || price), '|' ORDER BY order_ref))` — checks the data that actually matters while tolerating benign metadata drift. It trades a little verbosity for a checksum that means "the meaningful content is identical."

Q: In the checksum `md5(string_agg(t::text, '|' ORDER BY order_ref))`, why can a `NULL` column produce a misleading hash, and how do you guard it?
A: When a row is cast to text and a column is `NULL`, the row representation can be ambiguous if a non-null value could ever look like the empty/`NULL` slot, risking two different datasets hashing alike. Guarding means normalising nulls explicitly, e.g. wrapping columns in `coalesce(col::text, '∅')` so a `NULL` is distinguishable from an empty string. It is a corner case, but in regulated reconciliation an undetected difference is exactly what you must avoid.

Q: Why must the `ORDER BY` inside `string_agg` use a key that is unique, not merely sorted?
A: If the ordering column has ties, the rows within a tie group can be concatenated in any internal order, so two runs over identical data may produce different hashes — a false idempotency failure. A unique key (or a unique tuple) fully determines the concatenation order, making the hash reproducible. Ordering by a non-unique column like `isin` alone is the classic mistake; add the trade-level key to break ties deterministically.

Q: How would you checksum a table too large to `string_agg` into one value without exhausting memory?
A: Compute a per-row hash and aggregate them order-independently, e.g. `SELECT sum(('x' || md5(t::text))::bit(64)::bigint) FROM trades t`, so no single giant string is built. Because addition is commutative, you avoid needing a global `ORDER BY`, and the sum is stable regardless of scan order. This scales to large fact tables where concatenating every row would be prohibitive.

Q: Why does an order-independent hash aggregate (summing per-row hashes) remove the need for a deterministic `ORDER BY`?
A: Because summation is commutative and associative, the total is the same no matter what order the rows are added, so scan-order nondeterminism cannot change the result. Each row still contributes a deterministic per-row hash, so the aggregate detects any content change. This is why XOR- or sum-of-hashes schemes are popular for whole-table fingerprints on large tables.

Q: A reordered redelivery of a CSV produces different synthetic keys and you get duplicates. What property must the synthetic key have?
A: The key must be a pure function of stable row content only, never of the row's position or arrival order, so the same logical row yields the same key regardless of file ordering. Hashing business fields like `isin || trade_date || qty || price || source_seq` is order-independent; including a line number is not. Designing for reordered redelivery is mandatory because feeds routinely re-send in a different sequence.

Q: Why can a floating-point price formatted differently on resend silently break a payload-hash synthetic key?
A: If the source emits `1.5` one time and `1.50` or `1.500000001` another, the byte representation differs, so the hash differs and the retry looks like a new row, reintroducing a duplicate. The fix is to normalise the value before hashing — cast to a fixed-scale `numeric` like `round(price::numeric, 4)` — so equal economic values hash identically. Decimal normalisation is a common, easily-missed step in synthetic-key construction.

Q: How does choosing `numeric` over `float` for prices support idempotency in a NAV pipeline?
A: `numeric` stores exact decimal values, so the same price always serialises and hashes identically, whereas `float` can introduce tiny binary-rounding differences that vary across drivers or runs and perturb checksums. Exact decimals also avoid spurious "the data changed" diffs on rerun. For monetary and NAV figures, exactness is both a correctness and an idempotency-evidence requirement.

Q: Two unique constraints exist (`order_ref` and `(source, ext_id)`) and your upsert targets only `order_ref`. A row violates the other constraint. What happens?
A: The insert violates the unmonitored `(source, ext_id)` constraint, which `ON CONFLICT (order_ref)` does not arbitrate, so Postgres raises `23505` and aborts the statement instead of upserting. `ON CONFLICT` only handles conflicts on its named arbiter, never on other constraints. The fix is to pick one canonical dedup key, or restructure so a single constraint expresses identity, then target that.

Q: Why can you not list two different arbiter constraints in a single `ON CONFLICT` clause?
A: `ON CONFLICT` supports exactly one arbiter (one set of inferred columns or one named constraint), because the upsert needs a single, unambiguous "what counts as a duplicate" to resolve against. If a row could conflict on either of two distinct keys, the statement has no defined resolution and you must instead enforce a single identity or stage-and-dedup before loading. Trying to handle multiple identity notions in one statement is a sign the data model needs one true key.

Q: How do you make a loader idempotent when the only "key" is the entire row content (a truly unkeyed event feed)?
A: Create a unique index on a deterministic hash of the meaningful content — `md5(col1 || '|' || col2 || ...)` stored in a generated column — and upsert with `ON CONFLICT (content_hash) DO NOTHING`. The hash becomes the synthetic identity, so byte-identical events collapse and genuine duplicates no-op. You must normalise volatile formatting first so the hash is stable across resends.

Q: What is the risk of using a `GENERATED ... STORED` hash column as the dedup key if the hash expression changes later?
A: If you alter the generation expression (add or drop a column, change normalisation), existing rows keep their old hashes while new rows get new ones, so the same logical event can now produce a different key and slip past dedup. Any change to the identity function must be treated as a schema migration that rehashes existing rows. Stability of the hash definition over time is as important as stability across one resend.

Q: Why does adding a column to a synthetic-key hash require a backfill, not just a forward change?
A: Because the dedup key's meaning changed: rows loaded before the change carry hashes computed without the new column, so a resend of an old event now hashes differently and is treated as new. To keep idempotency intact you recompute (backfill) the hash for all existing rows under the new definition, then the unique index again reflects one identity per event. Forgetting the backfill creates a silent duplication window around the deployment.

Q: A loader uses `WHERE loaded = false` to pick rows and flips the flag after writing. Why is this not idempotent across a crash, and what replaces it?
A: If it writes the data then crashes before committing the flag flip, a rerun re-selects and re-writes the same rows (duplicating on a non-upsert sink); if it flips first then crashes, the rows are written but appear "done" and may be lost. The read is coupled to a side effect it mutates, which idempotency forbids. Replace it with a deterministic slice (a date partition) or claim-and-commit the flag in the same transaction as the write so they are atomic.

Q: How does reading by an explicit, named offset range make a stream consumer's read idempotent?
A: Because the read is parameterised — "give me offsets 1000 through 1999" returns exactly those records every time, independent of any mutable cursor — a retry re-reads the identical set. Contrast "everything since the last committed offset," where the cursor advances destructively and a retry sees a different (smaller) set. Pinning the read to stated parameters is what makes re-reading reproducible.

Q: Why is committing the consumer offset and the sink write in one transaction an alternative to an idempotent sink?
A: If the offset advance and the write commit atomically, a replay always re-reads from an offset whose effects either fully committed or never did, so no duplicate write can appear — transactionality prevents the duplicate rather than absorbing it. This is the transactional-sink route to exactly-once effect, an alternative to an idempotent sink that tolerates the duplicate. The choice depends on whether your sink supports a coordinated commit with the offset store.

Q: In Kafka terms, how do at-least-once delivery and an idempotent consumer combine for exactly-once effect?
A: Kafka's consumer can redeliver a record after a rebalance or restart (at-least-once), so the consumer must dedup — typically by upserting on a record key or storing processed offsets — so a redelivered record has no additional effect. The broker provides no-loss delivery; the idempotent consumer provides the "once" semantics on the sink. This is the same at-least-once-plus-idempotent-consumer pattern packaged for a log.

Q: Why does enabling Kafka's idempotent producer not free your consumer from being idempotent?
A: The idempotent producer only prevents duplicate writes from producer retries into the log itself; it says nothing about the consumer redelivering already-processed records after a restart or rebalance. End-to-end exactly-once effect still requires the consumer side to dedup or commit transactionally. Producer idempotency and consumer idempotency solve different halves of the path and are not substitutes.

Q: How would you structure a Python `uv run` loader's transaction so a kill cannot leave a partition half-written?
A: Open a single transaction, perform the partition delete (or staging swap) and the bulk `INSERT ... ON CONFLICT` within it, and commit once at the end, so a SIGKILL before commit rolls everything back and a rerun reproduces the whole slice. Never commit per-row or between the delete and insert, which would expose a partial state. Invoking via `uv run` ensures the same locked driver and dependency versions on every run so the committed bytes are comparable.

Q: Why should the idempotency double-run evidence be generated with `uv run` and the exact commands committed?
A: Because reproducibility of the runtime is part of reproducibility of the result — a different driver or pandas version could reformat a value and change the checksum, making the evidence non-comparable. `uv run` pins the locked dependency set, and committing the exact commands plus the before/after counts and checksums lets an auditor re-execute and confirm. This turns "the load is rerunnable" into objective, re-verifiable proof rather than assertion.

Q: A reconciliation shows the same logical trade as two rows differing only in a trailing-zero quantity (`100` vs `100.0`). What idempotency defect does this reveal?
A: The dedup key (or the value used to build a synthetic key) was not normalised, so two byte-different but economically identical representations were treated as distinct and both landed. It reveals that identity was defined on raw text rather than canonicalised values. The fix is to cast quantities and prices to a fixed-scale `numeric` before keying or hashing so `100` and `100.0` collapse to one row.

Q: Why does idempotency make a regulated "reproduce that day's output" request answerable, and what would break it?
A: Because an idempotent, deterministic pipeline regenerates the identical figure from the preserved input slice, you can re-run a past valuation date and show byte-for-byte the same NAV an auditor saw. Non-determinism — a `now()` stamp, a `random()` default, a non-deterministic sort, or an unpinned dependency — would yield a different output and destroy reproducibility. Preserving the input plus a pure transform plus a pinned runtime is what makes historical reproduction defensible under DORA and UCITS oversight.

Q: How can a partial unique index act as the arbiter for an idempotent upsert that only dedups active records?
A: Define the index with a predicate — `CREATE UNIQUE INDEX ON trades (order_ref) WHERE status <> 'cancelled'` — and target it with `ON CONFLICT (order_ref) WHERE status <> 'cancelled'`, so only the active subset enforces uniqueness. This lets cancelled rows coexist while live trades still dedup, which a full unique index could not express. The conflict-target `WHERE` must match the index predicate for inference to succeed, or Postgres raises `42P10`.

Q: How can you tell, after an `INSERT ... ON CONFLICT DO UPDATE`, whether a given row was inserted or updated?
A: Add `RETURNING (xmax = 0) AS was_inserted` — on a freshly inserted row the system column `xmax` is `0`, whereas an updated existing row carries a non-zero `xmax`, so the boolean distinguishes the two cases. This is handy in a loader that must log "n inserted, m updated" as idempotency evidence without a second query. It is an implementation detail rather than a documented contract, so treat it as a pragmatic heuristic and confirm behaviour on your Postgres version.

Q: How does `SELECT ... FOR UPDATE SKIP LOCKED` support an idempotent claim-and-process queue pattern?
A: A worker claims rows by locking them with `FOR UPDATE SKIP LOCKED`, processes them, and marks them done in the same transaction, so concurrent workers never grab the same row and a crash rolls the claim back for safe re-claiming. Idempotency still requires the downstream write to be an upsert, because a worker can die after writing but before committing the done-flag, leading to a re-claim. The pattern prevents double-claiming under concurrency; the idempotent sink prevents double-effect across crashes.
