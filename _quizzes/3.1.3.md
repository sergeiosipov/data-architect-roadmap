# Phase 2 · 3.1.3 + 3.3.1 Data lake on object storage — 150 self-test questions

Q: What is an object store, and how does it differ fundamentally from a POSIX filesystem?
A: An object store is a key-value system that maps an opaque string key to an immutable blob of bytes plus metadata, accessed over HTTP rather than as a mounted device. Unlike a POSIX filesystem it has no inodes, no in-place byte updates, no atomic rename, and no real directory tree — the key space is flat. Treating it like a local disk (assuming cheap renames, partial writes, or fast `ls` on huge "folders") is the single most common architectural mistake when designing a lakehouse on S3 or MinIO.

Q: What does it mean that an S3-style key space is "flat"?
A: It means there is exactly one level of namespace inside a bucket: every object is addressed by its full key string, and there is no native concept of a nested directory containing entries. A key like `bronze/dt=2026-06-13/nav.parquet` is one indivisible string, not a path walked through three folder objects. The slashes are pure convention that tooling interprets as a hierarchy, but the store itself just sees a long unique string.

Q: If S3 has no folders, why do tools like the AWS console and `mc ls` show a folder tree?
A: They synthesize the appearance of folders by treating `/` in keys as a delimiter and grouping common prefixes, so `bronze/dt=2026-06-13/` looks like a folder. This is a presentation trick layered over a flat key space; no "folder object" exists on the server. The practical consequence is that creating, renaming, or deleting a "folder" is never a single server-side operation — it is an operation per object that happens to share that prefix.

Q: Why is there no atomic rename of an object on S3?
A: Because S3 has no rename primitive at all — a "rename" must be implemented as a server-side copy of the object to the new key followed by a delete of the old key, which is two operations and therefore not atomic. Between the copy and the delete a reader can see both keys, neither, or a partially propagated state. This is why frameworks that rely on rename-to-commit (classic Hadoop output committers) are slow and unsafe on raw S3.

Q: Why is "no atomic rename" specifically dangerous for a data lake commit protocol?
A: Many engines finalize a job by writing output to a temp location and then renaming it to the final path, treating the rename as the atomic "publish" step. On S3 that rename is a copy+delete loop over potentially thousands of files, so it is slow, costs money per object, and can leave the destination half-populated if it fails midway — readers may then see partial results. This is exactly why table formats like Iceberg and Delta use a separate atomic metadata-pointer swap instead of relying on object rename.

Q: What is the relationship between MinIO and the S3 API?
A: MinIO is an open-source, S3-compatible object store that implements the Amazon S3 REST API, so the same SDKs, the `mc` client, and `s3://` tooling work against it unchanged. It lets you run the exact object-store semantics — flat keys, no rename, multipart upload, lifecycle rules — locally on your native Ubuntu laptop without a cloud account. That makes it the ideal local substrate for practising lakehouse design before moving the same code to AWS S3 or ADLS Gen2.

Q: How do you typically run MinIO locally for this lesson on Ubuntu?
A: Run it as a container via Docker Compose, exposing the S3 API port (default `9000`) and the web console port (default `9001`), with `MINIO_ROOT_USER` and `MINIO_ROOT_PASSWORD` set as environment variables. You then point the `mc` client or a pyarrow/boto3 client at `http://localhost:9000` with those credentials. This mirrors how you would later target a real S3 or ADLS endpoint, only the endpoint URL and credentials change.

Q: What is a bucket, and what are its main constraints?
A: A bucket is the top-level container for objects and the unit at which you set region, access policy, versioning, and lifecycle rules. Bucket names must be globally (on AWS) or server-unique, DNS-compatible (lowercase, no underscores in virtual-host style), and there is a soft cap on buckets per account, so you partition data by prefix inside a bucket rather than by spawning thousands of buckets. For a fund lakehouse you might use one bucket like `fund-lakehouse` and separate bronze/silver/gold by key prefix.

Q: What `mc` command creates a bucket on a MinIO server, and what must exist first?
A: First you register the server as an alias, e.g. `mc alias set local http://localhost:9000 ACCESSKEY SECRETKEY`, then create the bucket with `mc mb local/fund-lakehouse`. The alias stores the endpoint and credentials so subsequent commands address `local/...` paths. If `mc mb` fails it is almost always because the alias is wrong, the server is unreachable on port `9000`, or the bucket name violates naming rules.

Q: What is a "prefix" in object-storage terminology?
A: A prefix is any leading substring of object keys, most usefully the part up to a delimiter such as `bronze/dt=2026-06-13/`. Listing and many access policies operate on prefixes, so the prefix is the practical unit of "where" data lives even though no folder exists. Choosing a good prefix layout is the main lever you have over both query efficiency and request cost.

Q: How does partitioning data lake files map onto object-store keys?
A: Partitioning is implemented purely by encoding partition values into the key prefix, typically as `key=value` segments like `dt=2026-06-13/region=LU/`. There is no partition metadata in the store itself — the partition scheme is a naming convention that query engines parse to prune. So "Hive-style partitioning" on S3 is just a disciplined prefix convention, not a server feature.

Q: Why is the layout of your keys a cost decision, not just an organizational one?
A: Because object stores charge per request (LIST, GET, PUT) and a query engine discovers and reads files by issuing those requests against prefixes. A layout that lets the engine target a narrow prefix issues few LISTs and GETs; a flat or badly partitioned layout forces broad LISTs and reads of irrelevant files, inflating both latency and the per-request bill. Thus the same dataset can cost very differently to query depending solely on how its keys are shaped.

Q: What does a date-range scan cost, in request terms, over a `dt=YYYY-MM-DD/` layout?
A: To scan a 30-day range the engine issues at least one LIST per day-prefix (more if a day has many files and listing paginates) plus one GET per file it actually reads. So the request count scales with the number of partitions touched and the number of files inside them, which is why over-partitioning into tiny files is as harmful as no partitioning. You estimate it as roughly (partitions in range × LIST pages) + (files read × GETs).

Q: Why is "too many small files" a recognized anti-pattern on object stores?
A: Each file needs its own GET (and metadata/footer read), and each partition needs its own LIST, so a million tiny Parquet files turns one logical scan into millions of requests, dominating cost and latency regardless of total data size. Small files also waste compression and statistics efficiency and inflate metadata. The remedy is compaction: periodically rewriting many small files into fewer right-sized ones, often targeting roughly 128 MB to 1 GB per file.

Q: Conversely, why can files that are too large also hurt?
A: Very large files reduce parallelism because an engine often assigns work at file or row-group granularity, so a few huge files leave most workers idle, and any single read failure forces re-reading a lot. They also make selective predicate pushdown coarser because fewer files means fewer chances to skip whole files by statistics. The goal is a middle ground that balances request overhead against parallelism, commonly hundreds of MB per file.

Q: What is the difference between durability and availability for an object store?
A: Durability is the probability that stored bytes are not lost over time (commonly quoted as eleven nines, 99.999999999%, achieved by replication or erasure coding across devices). Availability is the probability the service can serve a request at a given moment (e.g. 99.9% or 99.99%), which can dip during an outage even when nothing is lost. You can have a temporarily unavailable but still perfectly durable store: your data is safe but you cannot read it right now.

Q: How does MinIO achieve durability across disks on a single deployment?
A: MinIO uses erasure coding, splitting each object into data and parity shards distributed across drives (and nodes) so the object survives the loss of up to the configured number of drives. This is more space-efficient than full replication for the same fault tolerance. The trade-off is that you need a minimum number of drives/erasure-set members for the scheme to work, which is why production MinIO is deployed across multiple disks or servers.

Q: What is a storage tier (storage class), and why do they exist?
A: A storage tier is a named cost/performance profile for objects, ranging from hot frequently-accessed tiers to cold archival tiers that are cheap to store but slow and pricier to retrieve. They exist because access patterns change over an object's life: a NAV file is hot at month-end and effectively never read after a year, so paying hot-tier prices for it forever is wasteful. Tiers let you pay for the access pattern you actually have.

Q: How do cold/archive tiers make long-term regulatory archives cheap?
A: Archive tiers (S3 Glacier, ADLS Archive) drop the per-GB storage price dramatically in exchange for high retrieval latency (minutes to hours) and retrieval fees. Regulatory data such as fund records that must be retained for years but read almost never is the perfect fit: you pay almost nothing to keep it and accept slow, rare retrieval. The architect's job is to make sure nothing latency-sensitive ever lands in archive by accident.

Q: What is an object lifecycle policy?
A: A lifecycle policy is a set of rules attached to a bucket that automatically transitions objects between storage tiers or expires (deletes) them based on age or other conditions, with no application code involved. It is how you operationalize the "hot now, cold later, gone eventually" plan declaratively. For example, transition bronze data to a cheaper tier after 90 days and delete it after retention expires.

Q: In the lesson's `Do`, you expire bronze objects after 30 days — what does an expiration rule actually do?
A: An expiration rule causes the store to delete objects matching the rule's prefix/filter once they are older than the specified number of days, freeing storage and removing transient raw data automatically. It runs asynchronously on the server's schedule, so deletion may lag the exact 30-day mark by up to a day. This is ideal for bronze (raw landing) data that has already been promoted to cleaned silver tables and no longer needs to be retained.

Q: How would you set a lifecycle rule in MinIO with `mc`, and how do you confirm it?
A: You add it with `mc ilm rule add --expire-days 30 --prefix "bronze/" local/fund-lakehouse` (older syntax: `mc ilm add --expiry-days 30 ...`), then verify with `mc ilm rule ls local/fund-lakehouse` which prints the configured rules. Seeing the rule listed against the bucket is the proof the lesson's "show a working lifecycle rule" step asks for. If it does not appear, check the bucket name, the prefix, and that you targeted the right alias.

Q: Why does lifecycle expiration not delete objects at the exact second they turn 30 days old?
A: Lifecycle is implemented as a periodic background scan, not a per-object timer, so the store evaluates rules on its own schedule (typically daily). An object becomes eligible at the threshold but is physically removed on the next scan pass, which can be hours later. So treat expiration as "deleted within about a day after the threshold," not as a precise countdown.

Q: How do lifecycle rules interact with object versioning?
A: With versioning enabled, a delete created by expiration usually inserts a delete marker rather than removing data, and you need separate rules (noncurrent-version expiration, expired-delete-marker cleanup) to actually reclaim space. Otherwise old versions accumulate silently and storage keeps growing despite the expiration rule. This is a classic "my lifecycle rule is not freeing space" gotcha: the rule fired, but versioned history is still there.

Q: What is multipart upload and what problem does it solve?
A: Multipart upload lets you upload one large object as many independently transferred parts that the store reassembles into a single object on completion. It solves the reliability and throughput problem of large files: parts upload in parallel, and a failed part is retried alone instead of restarting the whole transfer. This is how multi-gigabyte holdings or NAV history files land dependably over an unreliable network.

Q: What are the three phases of a multipart upload?
A: Initiate (CreateMultipartUpload) returns an `UploadId` that ties the parts together; Upload Part sends each chunk with a part number and receives an ETag per part; Complete (CompleteMultipartUpload) sends the list of part numbers and ETags so the server assembles the final object. Until you complete, the object does not exist as a readable key. You can also Abort to discard the in-progress upload.

Q: What is the minimum and maximum part size in an S3-style multipart upload?
A: Each part must be at least 5 MiB except the last part, which can be any size, and each part can be at most 5 GiB. These bounds come from the S3 API and are honored by MinIO for compatibility. Choosing parts near the floor for a huge file is a mistake because you can exhaust the part-count limit.

Q: What is the maximum number of parts per multipart upload, and what does it imply for file size?
A: The maximum is 10,000 parts per upload, and with a 5 GiB cap per part that yields a theoretical single-object ceiling of about 5 TiB. The practical implication is you must scale part size with file size: a 1 TB file with 5 MiB parts would need ~200,000 parts and fail, so you pick a part size (e.g. tens to hundreds of MB) that keeps the count under 10,000. Many SDKs do this sizing automatically.

Q: What is an ETag in the context of multipart upload, and why is it not always an MD5?
A: For a single PUT, the ETag is typically the MD5 of the object, but for a multipart object the ETag is a hash of the concatenated part hashes followed by `-N` where N is the part count, so it is not the MD5 of the whole file. This means you cannot verify a multipart object's integrity by computing a plain MD5 and comparing to the ETag. If a tool reports a checksum mismatch on a large file, this format difference is the usual cause, not corruption.

Q: What happens to the parts if a multipart upload is never completed or aborted?
A: The uploaded parts remain stored (and billable) as an incomplete multipart upload that is invisible in normal object listings but still consumes space. They linger until you abort the upload or a lifecycle rule cleans them up. This is a real and sneaky cost leak; mature setups add a lifecycle rule to abort incomplete multipart uploads after, say, 7 days.

Q: Which lifecycle action specifically cleans up abandoned multipart uploads?
A: The `AbortIncompleteMultipartUpload` lifecycle action (configurable by days-after-initiation) tells the store to discard parts of uploads that were started but never completed. In `mc` this is part of ILM configuration alongside expiry rules. Adding it is best practice precisely because incomplete uploads are otherwise easy to forget and keep paying for.

Q: Why are multipart parts uploaded in parallel a big throughput win?
A: A single TCP stream is limited by latency and window size, so one large sequential upload underuses available bandwidth, whereas N parts over N connections multiply throughput up to the link or server limit. Parallelism also isolates failures, so one flaky part retries without resending gigabytes. This is why moving a large NAV history dump completes far faster as a 32-part parallel upload than as one stream.

Q: How does pyarrow let you write Parquet directly to a MinIO/S3 bucket?
A: You configure a filesystem (e.g. `pyarrow.fs.S3FileSystem` with `endpoint_override`, access key, secret, and `scheme="http"` for local MinIO) and pass an `s3://bucket/key` path to the Parquet/dataset writer. pyarrow then handles multipart upload under the hood for large outputs. Because the lesson mandates `uv run`, you invoke it like `uv run python write_nav.py` so the pinned environment and pyarrow version are used.

Q: For the lesson's `Do`, what key layout do you use when writing NAV Parquet files, and why?
A: You write into a date-partitioned layout such as `bronze/nav/dt=2026-06-13/part-0.parquet`, encoding the valuation date as a `dt=` prefix segment. This lets a query engine prune to the exact dates a report needs by targeting that prefix, turning a full-bucket scan into a few LISTs and GETs. It also aligns naturally with how funds produce one NAV set per valuation day.

Q: Why is valuation date (`dt`) usually a better top-level partition key than, say, fund ISIN for a NAV bronze layer?
A: Because most ingestion and most queries are time-scoped (load today's NAVs, report a date range), so partitioning by `dt` matches the dominant access pattern and keeps partition counts bounded (365 per year). Partitioning first by ISIN would create tens of thousands of tiny partitions and scatter a single day's load across them, hurting both write and scan efficiency. You can still partition by ISIN at a deeper level or rely on Parquet statistics within files for ISIN pruning.

Q: A query reads NAVs for a single ISIN across one month from a `dt=`-partitioned layout — how does pruning help and where does it stop?
A: The `dt` partitioning lets the engine LIST and GET only the ~30 day-prefixes in range instead of the whole bucket, which is the big win. Within those files, ISIN filtering relies on Parquet column statistics and row-group min/max to skip irrelevant row groups, not on the key layout. So the partition prunes the time dimension cheaply, and Parquet internals prune the ISIN dimension inside the selected files.

Q: How would you estimate the LIST/GET cost of scanning a quarter of NAV data in this layout?
A: Count the partitions in the range — about 90 day-prefixes for a quarter — and that gives roughly 90 LIST operations (more if any day's listing paginates beyond the page size, typically 1,000 keys). Then add one GET per file the engine decides to read after pruning. So cost ≈ (90 + pagination overhead) LISTs + (files read) GETs, which is why fat well-sized files per day keep the GET count low.

Q: What does a single `LIST` request return, and why does pagination matter for cost?
A: A LIST (ListObjectsV2) returns up to a page of keys (default and max 1,000) under a prefix, plus a continuation token if more exist. To enumerate a prefix with 5,000 objects you therefore issue at least 5 LIST calls, each billed. This is why prefixes with huge numbers of objects are expensive to enumerate and why narrow, well-chosen prefixes save money.

Q: Why is `LIST` often the surprise cost in a poorly designed lake, more than `GET`?
A: Because query planning frequently enumerates many prefixes to discover files before reading any data, and engines may re-list on every query if there is no metadata layer caching the file set. Over a flat or over-partitioned layout, planning can fan out into thousands of LISTs that read no actual data. Table formats like Iceberg fix this by tracking file lists in manifest metadata, replacing directory listing with a metadata read.

Q: How does a table format like Iceberg reduce object-store request costs versus raw directory listing?
A: Iceberg records the exact set of data files (with partition values and statistics) in manifest files, so the engine reads a small amount of metadata to know precisely which files to GET, eliminating broad LIST scans entirely. It also gives atomic commits via a metadata pointer swap, sidestepping the no-atomic-rename problem. The net effect is fewer, more targeted requests and safe concurrent writers.

Q: What is "eventually consistent listing" and why did it historically bite data lakes?
A: It means a freshly written object might not immediately appear in a LIST result, so an engine that writes files and then lists them to read back could miss some, producing silently incomplete results. AWS S3 historically had this for listings, which is why metadata-tracking layers (like S3Guard) existed. Modern S3 now provides strong read-after-write and listing consistency, but the design lesson — do not trust listing to reflect just-written state across systems — still informs lakehouse architecture.

Q: AWS S3 now offers strong read-after-write consistency — does that make a table format's metadata unnecessary?
A: No. Strong consistency removes one failure mode (missing freshly written keys in a listing), but it does not give you atomic multi-file commits, efficient file discovery without LISTs, or schema/partition evolution. You still want Iceberg/Delta metadata for atomicity, cheap planning, and concurrent-writer safety. Consistency and a transactional table layer solve different problems.

Q: What is the ADLS Gen2 hierarchical namespace (HNS)?
A: HNS is a feature of Azure Data Lake Storage Gen2 that overlays a real directory tree on top of blob storage, so directories are first-class objects rather than synthesized prefixes. With HNS enabled, the store understands paths and can manipulate whole directories as atomic units. It is enabled at account creation and turns flat blob storage into something with filesystem-like semantics for analytics.

Q: Why does ADLS Gen2 with HNS support atomic directory rename when S3 cannot?
A: Because HNS maintains an actual directory structure in metadata, a directory rename is a single metadata update of the directory node, not a copy of every contained object. So renaming a directory of 10,000 files is one atomic operation instead of 10,000 copy+delete pairs. This is precisely the capability S3's flat key space lacks.

Q: What does atomic directory rename buy a data pipeline concretely?
A: It enables the safe write-to-temp-then-rename publish pattern: a job writes all output into a temporary directory and atomically renames it to the final location, so consumers never see partial output and the publish is all-or-nothing. On HNS this rename is instant and atomic; on S3 it is a slow, non-atomic copy loop, which is why output committers are a pain there. It makes job finalization fast and correct.

Q: How does HNS change the cost and speed of renaming or deleting a large "folder"?
A: Without HNS, renaming or deleting N objects under a prefix costs work proportional to N (one operation per object), so it is slow and racks up requests; with HNS it is a single metadata operation regardless of how many files are inside. So a folder rename that is O(N) and expensive on flat blob/S3 becomes O(1) on HNS. That is the central operational advantage the lesson points to.

Q: Does ADLS Gen2 expose both blob and filesystem APIs, and why does that matter?
A: Yes — ADLS Gen2 offers the Blob REST API and the Data Lake Storage (DFS) filesystem API over the same data, so tools can use whichever fits. The DFS endpoint understands directories and atomic operations, while the blob endpoint gives broad compatibility with existing blob tooling. Knowing which API a tool uses matters because directory-atomic behavior is only available through the filesystem semantics.

Q: For the "explain why folder rename is a problem on S3 but atomic on ADLS Gen2" Done-when, what is the one-sentence answer?
A: On S3 a folder is just a shared key prefix with no folder object, so renaming it means copying and deleting every object one by one (slow, non-atomic, billed per object), whereas ADLS Gen2's hierarchical namespace stores directories as real metadata nodes, so a rename is a single atomic metadata update independent of file count. The difference is flat-key-space-plus-no-rename versus a true directory tree.

Q: Why might you still choose flat S3-style storage despite lacking atomic rename?
A: Because S3/MinIO are ubiquitous, cheap, massively scalable, and the de-facto standard that every engine and SDK supports, and modern table formats (Iceberg, Delta, Hudi) provide the atomicity you would otherwise want from rename via a metadata layer. So you get transactional behavior without HNS, plus portability across clouds. HNS is convenient but ties you to Azure's filesystem semantics.

Q: What is the practical difference between a bucket (S3) and a container/filesystem (ADLS Gen2)?
A: They are analogous top-level scopes for objects, but an ADLS Gen2 "container" (filesystem) with HNS can contain a real directory hierarchy, whereas an S3 bucket holds a flat key space. The access model and naming rules differ, but conceptually both are the boundary for policy and the root of your prefix/path layout. When porting a lakehouse, a bucket maps to a filesystem and prefixes map to directories.

Q: What is object immutability on these stores, and how do you "update" an object?
A: Objects are immutable: you cannot edit bytes in place, so an "update" means overwriting the key with a brand-new object (a fresh PUT) or, with versioning, creating a new version. There is no append-to-the-middle or partial write. This is why data lakes never mutate Parquet files; they write new files and let the table format track which files are current.

Q: How does object versioning work and what is it good for?
A: With versioning enabled, each PUT to an existing key creates a new version with its own version ID while keeping prior versions, and a delete inserts a delete marker rather than erasing data. It protects against accidental overwrite or deletion and supports point-in-time recovery. The cost is storage growth from retained versions, which you manage with noncurrent-version lifecycle rules.

Q: What is object-store object lock / WORM, and why is it relevant in regulated funds?
A: Object lock (WORM — write once, read many) prevents an object from being deleted or overwritten until a retention period expires or a legal hold is lifted. In regulated fund administration, records such as NAV evidence or reporting files often must be immutable for a mandated retention window (relevant to regimes like UCITS/AIFMD record-keeping and DORA resilience expectations). WORM gives auditable, tamper-evident retention enforced by the storage layer rather than by application discipline.

Q: How can lifecycle policies and object lock conflict, and how is that resolved?
A: A lifecycle expiration cannot delete an object still under a retention lock, so the lock wins until retention elapses; the object simply is not removed even though the rule fires. This is by design for compliance — you cannot accidentally lifecycle-away locked regulatory records. The gotcha is being surprised that storage is not shrinking; check for object-lock retention before assuming the rule is broken.

Q: What is server-side encryption (SSE) on an object store, and what are the common modes?
A: SSE encrypts object data at rest on the server, transparently on write and decrypt on read, with key-management modes typically being store-managed keys (SSE-S3), KMS-managed keys (SSE-KMS), or customer-provided keys (SSE-C). It protects data if the underlying disks are compromised and is often mandatory for regulated data. MinIO supports SSE with an external KMS for the customer-managed cases.

Q: Why is encryption-in-transit (HTTPS/TLS) to the object store also required, not just at-rest?
A: At-rest encryption protects stored bytes but does nothing for data crossing the network, where credentials and object contents could be intercepted; TLS protects the request/response in flight. For regulated fund data both are expected. Locally against MinIO you may use plain HTTP for convenience (`scheme="http"`), but any real deployment must enforce HTTPS endpoints.

Q: What does an HTTP `403` from an object store typically indicate when your code "should" have access?
A: A `403 Forbidden` (often `AccessDenied`) means authentication succeeded enough to be evaluated but authorization failed: the credentials lack permission for that action/resource, the bucket policy denies it, or the signature/clock is off. First checks are the exact action and ARN/prefix in the policy, whether the right credentials/alias are in use, and time skew. It is distinct from a `404`, which means the key or bucket was not found.

Q: What does a `404 NoSuchKey` versus `NoSuchBucket` tell you?
A: `NoSuchKey` means the bucket exists but no object lives at that exact key — usually a prefix typo, a missing partition, or a case/trailing-slash mismatch. `NoSuchBucket` means the bucket name itself does not resolve — wrong name, wrong region/endpoint, or wrong alias. Because keys are exact strings, `NoSuchKey` is frequently just an off-by-one in the partition path like `dt=2026-6-13` instead of `dt=2026-06-13`.

Q: Your pyarrow write to MinIO fails with a connection/endpoint error — what do you check first?
A: Verify the endpoint and scheme: for local MinIO you need `endpoint_override="localhost:9000"` and `scheme="http"` (not https), plus the correct access key/secret and `allow_bucket_creation`/region settings. Then confirm the MinIO container is actually up and listening on `9000` (e.g. `docker compose ps`). Most local failures are a wrong scheme, a stopped container, or mismatched credentials, not a code bug.

Q: A `mc ls` of a bucket returns nothing right after a big write — what are the candidate causes?
A: Likely you listed the wrong prefix or alias, the writer used a different key layout than you expect, or a multipart upload never completed so the object does not yet exist. Confirm the exact prefix, check `mc ls --recursive`, and look for incomplete multipart uploads. On modern S3 strong consistency rules out "not yet visible," but on some systems eventual listing consistency could also explain it.

Q: What is the difference between path-style and virtual-hosted-style S3 addressing, and which does MinIO need?
A: Virtual-hosted style puts the bucket in the hostname (`bucket.s3.amazonaws.com`), while path-style puts it in the URL path (`s3.endpoint/bucket/key`). Local MinIO typically requires path-style addressing, so clients must be configured with `force_path_style`/`path-style` enabled (pyarrow handles this when you set `endpoint_override`). A common local error is a DNS failure because the client tried virtual-hosted style against `localhost`.

Q: Why does a presigned URL exist and what does it let you do?
A: A presigned URL embeds temporary, signed authorization in the URL itself, letting a party GET or PUT a specific object for a limited time without having your credentials. It is how you let an external system upload a holdings file or download a report directly to/from the bucket securely. The signature scopes the URL to one operation and key and expires, so leakage risk is bounded.

Q: What is the practical meaning of "object stores are not strongly ordered across keys"?
A: There is no transaction spanning multiple keys, so writing several files is not atomic as a group — readers can observe some files present and others not mid-write. This is the deeper reason behind the temp-then-publish and table-format-commit patterns. You never rely on "I wrote files A, B, C" being visible together unless a metadata layer makes that commit atomic.

Q: How do you make multi-file writes appear atomic on a flat object store?
A: Use a transactional table format (Iceberg/Delta) whose commit is a single atomic swap of a metadata pointer that references the new file set, so readers either see the old snapshot or the new one, never a mix. The data files can be written first in any order; only the metadata commit makes them "live." This converts an unsafe multi-file write into an atomic, isolated transaction over flat storage.

Q: What is request rate scaling on S3, and why does prefix design affect it?
A: S3 scales request throughput per prefix, historically requiring high-entropy prefixes to avoid hotspotting; modern S3 auto-scales but very hot single prefixes can still throttle. Spreading writes across prefixes (e.g. by partition) distributes load and avoids `503 SlowDown`. So prefix design influences not just listing cost but also achievable write/read throughput.

Q: What does a `503 SlowDown` response from S3 mean and how do you handle it?
A: It signals the service is throttling your request rate, often because a single prefix is too hot, and the correct response is exponential backoff with retry (most SDKs do this automatically). Persistent throttling means you should spread keys across more prefixes or reduce burst concurrency. It is a rate problem, not an auth or existence problem, so do not confuse it with `403`/`404`.

Q: In the bronze/silver/gold (medallion) model, how does each layer map to object-store prefixes and lifecycle?
A: Bronze (raw landing) lives under a `bronze/` prefix and is typically short-lived with an aggressive expiration rule; silver (cleaned, conformed) and gold (curated marts) live under their own prefixes with longer or no expiration. Mapping layers to prefixes lets you apply different lifecycle, access, and tiering rules per layer with one bucket. Expiring bronze after 30 days, as in the lesson, is the canonical bronze lifecycle.

Q: Why expire bronze but retain silver/gold?
A: Bronze is reproducible raw input that has already been transformed into silver/gold, so keeping it forever just stores redundant data and cost; silver/gold are the curated assets that downstream reporting depends on. Expiring bronze reclaims space while the value-added layers persist. The exception is when bronze itself is the system of record for audit, in which case you archive rather than delete it.

Q: How does compression choice interact with object-store request cost?
A: Better compression (e.g. zstd vs snappy) shrinks each file, so a GET transfers fewer bytes and storage is cheaper, but it does not reduce the number of requests — those are driven by file and partition counts. So compression cuts byte-based costs and transfer time, while layout cuts request-based costs; you tune both. For a fund lakehouse, zstd on cold gold marts and snappy on hot frequently-decoded data is a common split.

Q: Why do object stores charge separately for storage, requests, and data transfer (egress)?
A: Because each is a distinct resource: storage is per-GB-month, requests (LIST/GET/PUT) are per-operation, and egress is per-GB leaving the region/cloud. A design can be cheap on one axis and expensive on another — many small files are cheap to store but request-expensive, and cross-region reads add egress. The architect must reason about all three, not just total GB.

Q: Why is cross-region or cross-cloud egress a specific concern for EU fund data?
A: Egress fees apply when data leaves a region, and for regulated EU funds there are also data-residency expectations that may forbid moving data outside the EU, so cross-region reads can be both costly and non-compliant. Keeping compute and storage in the same EU region (e.g. an Azure or AWS EU region) avoids egress and respects residency. The lesson's emphasis on EU-region posture ties directly to this.

Q: How do you compute the storage cost of keeping bronze NAV data without lifecycle versus with a 30-day expiry?
A: Without expiry, cost grows linearly forever: daily bronze volume × per-GB price × days retained, accumulating indefinitely. With a 30-day expiry, retained data plateaus at roughly 30 days of volume because old objects are deleted as new ones arrive, capping steady-state cost. The expiry turns an ever-growing bill into a constant one proportional to one month of ingest.

Q: What is the role of `mc cp` versus `mc mirror`?
A: `mc cp` copies individual objects or trees on demand, while `mc mirror` synchronizes a source and destination, copying only differences and optionally removing extras, like an object-store `rsync`. For one-off uploads you use `cp`; for keeping a local landing directory in sync with a bucket you use `mirror`. Both ultimately issue per-object PUTs, so on large trees they incur per-object request cost.

Q: Why can `mc mirror` of a huge directory be surprisingly slow or costly?
A: Because it must LIST both sides to compute differences and then PUT each changed object individually, so cost scales with object count, and many tiny files dominate the time. The fix is fewer, larger files and limiting how often you full-mirror. It is the same small-files problem reappearing in a sync tool.

Q: What metadata can an object carry, and how is it used?
A: Each object has system metadata (size, ETag, content-type, storage class, timestamps) and optional user metadata (arbitrary key-value pairs set at PUT). User metadata can tag objects with, say, source system or fund ID, but note it is fixed at write time and changing it requires rewriting (copying) the object. Lifecycle and some policies can filter on tags, which is a separate tagging mechanism from user metadata.

Q: What is the difference between object metadata and object tags for lifecycle filtering?
A: Object tags are mutable key-value labels designed for access control and lifecycle filtering (you can change them without rewriting the object), whereas user metadata is set at write time and immutable thereafter. Lifecycle rules can target objects by tag, which is more flexible than by prefix alone. So to lifecycle-manage a subset that does not align to a prefix, you tag the objects.

Q: A lifecycle rule with a prefix filter is not deleting the objects you expect — what is the first thing to verify?
A: Confirm the rule's prefix exactly matches the objects' keys, including any leading segment and case, because lifecycle prefix matching is literal string matching. A rule on `bronze/` will not touch keys under `Bronze/` or `landing/bronze/`. Also check the object ages are truly past the threshold and that versioning/object-lock is not preventing deletion.

Q: How does the choice of partition granularity (daily vs hourly) change request cost and file size?
A: Finer granularity (hourly) multiplies partition count and tends to produce many smaller files, raising LIST and GET counts and metadata overhead, while coarser (daily) yields fewer, larger files that are cheaper to scan but coarser to prune. You pick granularity to match query selectivity and ingest volume. For once-daily NAV cycles, daily partitioning is the natural fit; intraday tick data might justify hourly.

Q: Why might over-partitioning a low-volume fund dataset hurt more than help?
A: If each partition holds only a few small rows, you pay LIST and GET overhead per partition while gaining little pruning benefit and creating the small-files problem. The fixed per-request cost dominates the tiny per-partition data. For low-volume data, coarser partitions (or none, relying on Parquet statistics) are usually better.

Q: How do you reason about the cost of a "scan all funds for one date" query in the `dt=` layout?
A: A single date is one partition prefix, so it is roughly one LIST (plus pagination if that day has more than ~1,000 files) and one GET per file in that day. Because all funds for the date sit under the same `dt=` prefix, this is the cheap, well-aligned query the layout is designed for. The opposite query — one fund across all dates — is the expensive one, touching every date partition.

Q: Why is "one fund across all history" the expensive query in a date-partitioned NAV lake, and how do you mitigate it?
A: Because the fund's rows are spread across every `dt=` partition, the engine must touch all date prefixes, doing many LISTs and reading many files just to filter to one ISIN. Mitigations include relying on Parquet column statistics to skip row groups, maintaining a per-fund secondary layout or table, or using a table format whose metadata indexes file-level min/max on ISIN. The right fix depends on how common that access pattern is.

Q: What is the significance of an ISIN being a fixed 12-character identifier when stored in Parquet within these files?
A: Its fixed, low-cardinality-per-file nature makes it an excellent candidate for dictionary encoding and for min/max statistics that enable row-group pruning, so filtering by ISIN inside a Parquet file is efficient even without ISIN-based partitioning. Sorting files by ISIN further tightens the min/max ranges and improves skipping. So the key layout handles the date dimension and Parquet internals handle the ISIN dimension.

Q: How would EMT/EPT files typically land in an object-store-based lake?
A: They arrive as files (often structured CSV/XML payloads) and are PUT into a bronze landing prefix keyed by receipt date and source, e.g. `bronze/emt/dt=2026-06-13/<file>`, possibly via multipart upload if large. From there they are parsed into conformed silver tables. The object store is the durable, cheap landing zone; the transformation into queryable Parquet happens downstream.

Q: Why is multipart upload relevant when ingesting a large fund holdings extract?
A: A full holdings extract across many funds can be gigabytes, and a single-stream upload over an unreliable link risks timeouts and restarts; multipart uploads it in parallel chunks with per-part retry, so it lands reliably and faster. It also lets you start uploading parts while still producing later parts. This is exactly the "large NAV/holdings files land reliably" point in the syllabus.

Q: How does object-store durability support regulatory retention requirements?
A: The eleven-nines durability of replicated/erasure-coded object stores means archived regulatory records are extraordinarily unlikely to be lost over the retention window, satisfying the "we will still have it" requirement cheaply when combined with archive tiers. Add object lock for immutability and you also satisfy "it cannot be tampered with or deleted early." Together, durability plus WORM plus archive tier is the standard cheap-compliant-archive recipe.

Q: A monthly NAV reconciliation must read 12 month-end snapshots quickly, but they are in an archive tier — what is the design flaw?
A: Archive tiers have minutes-to-hours retrieval latency and retrieval fees, so data that must be read on a schedule should never sit in archive; the flaw is tiering by age alone without considering access pattern. Month-end snapshots that are read every reconciliation cycle belong in a hot or infrequent-access tier, not archive. The fix is a lifecycle/tiering policy keyed to actual access, keeping recurrently-read data warm.

Q: Why is it risky to point a query engine at a bucket root with no partitioning for a large dataset?
A: The engine must LIST the entire key space to discover files and may GET many irrelevant ones because it cannot prune, turning every query into a full enumeration and scan. Cost and latency then scale with total object count regardless of how selective the query is. Partitioning by a commonly-filtered key (like `dt=`) is what lets the engine target a small subset.

Q: How does "no atomic rename" affect a naive Spark job writing partitioned output to S3?
A: With the older FileOutputCommitter (v1), Spark writes to task temp directories and renames them to final locations to commit, which on S3 becomes slow copy+delete and risks partial visibility on failure. This is why S3-aware committers (the S3A magic/directory committers) or table formats exist — they avoid rename-based commit. A symptom is a job whose compute finishes fast but spends ages "committing."

Q: What is the S3A committer and what problem does it solve?
A: The S3A committers (magic and staging/directory) are Hadoop committers that finalize Spark/Hadoop output to S3 without rename, using multipart upload completion as the atomic commit step instead of copy-and-rename. They solve the slow, unsafe rename-based commit on flat object stores. They are a workaround for the missing atomic rename; table formats are the more modern, complete solution.

Q: How do you verify a lifecycle rule actually applied versus merely being configured?
A: Configuration is verified by listing rules (`mc ilm rule ls`), but application is verified by observing that objects older than the threshold actually disappear over time, or by checking object counts/storage trending down after the next scan. Because the scan is periodic, you confirm by waiting a scan cycle and re-listing, not by expecting instant deletion. Seeing both the rule present and old objects gone is full proof.

Q: Why might storage keep growing even though your expiry rule is correctly configured and old objects are eligible?
A: Common causes are versioning (deletes create delete markers while old versions persist), object lock retaining locked objects, incomplete multipart uploads accumulating invisible parts, or new ingest simply outpacing deletions. Each retains data the simple expiry rule does not remove. You diagnose by checking versioning status, object-lock retention, and incomplete uploads, then adding the matching cleanup rules.

Q: What is the difference between deleting an object and it being reclaimable when versioning is on?
A: A delete with versioning on inserts a delete marker that hides the object from normal reads, but all prior versions still consume storage until noncurrent-version expiration removes them. So "deleted" logically does not mean "space freed." Reclaiming space requires lifecycle rules targeting noncurrent versions and expired delete markers.

Q: Why is `s3://` (or the MinIO equivalent) an HTTP-based protocol, and what latency implication follows?
A: Every object operation is an HTTP request to a remote endpoint, so each GET/LIST/PUT incurs network round-trip latency far higher than a local disk seek. This is why minimizing request count (fewer, larger files; targeted prefixes; metadata-driven planning) matters so much — latency is per-request, not per-byte. Designs that issue thousands of tiny requests feel slow even on fast networks.

Q: How does reading a Parquet footer interact with object-store request cost?
A: An engine typically issues a small ranged GET to read the file footer (schema and row-group statistics) and then further ranged GETs for the needed columns/row groups, so each file can cost multiple range requests. Many tiny files multiply these footer reads, again favoring fewer larger files. HTTP range requests (`Range:` header) let the reader fetch only the byte spans it needs instead of the whole object.

Q: What is an HTTP range request and why is it central to efficient Parquet reading on object storage?
A: A range request uses the HTTP `Range` header to fetch only a specified byte span of an object, so a reader can pull just the footer and the specific column chunks it needs rather than the entire file. This is what makes columnar projection pushdown over object storage cheap: you download only the relevant bytes. Without range support, every read would transfer whole files, destroying the benefit of columnar layout.

Q: When porting a lakehouse from MinIO/S3 to ADLS Gen2, what changes and what stays the same?
A: The conceptual model — prefixes/paths, lifecycle/tiering, multipart-style uploads, partitioned Parquet — stays the same, and table formats abstract most differences; what changes is the endpoint, auth model, and that ADLS Gen2 with HNS gives you real atomic directory operations you did not have on S3. You also swap S3-specific addressing for the DFS filesystem API. So the design transfers, but you can exploit HNS atomicity and must adjust credentials/endpoints.

Q: Why do architects say object-store semantics "leak upward" into table formats and engines?
A: Because the absence of atomic rename, the per-request cost model, and listing behavior force design choices in everything above: table formats add metadata commits to get atomicity, engines add range reads and file pruning to control request cost, and pipelines avoid rename-based publish. You cannot design the upper layers correctly without understanding the storage layer's semantics. Ignoring them produces designs that corrupt under concurrency or cost a fortune in LISTs.

Q: Your `mc` commands suddenly fail with an access-denied or "unable to initialize" error after working yesterday — what do you check first?
A: First confirm the alias still holds the right endpoint and credentials with `mc alias ls`, because a rotated `MINIO_ROOT_PASSWORD`, a recreated container with new keys, or an edited `~/.mc/config.json` will break auth while everything else looks fine. Then check the server is up and reachable on its port (`mc admin info local` or `docker compose ps`). Stale credentials in the alias are the most common cause of a setup that worked yesterday and fails today.

Q: Why does writing partitioned NAV Parquet with `uv run` rather than a system `python` matter for reproducibility here?
A: Because `uv run` executes against the project's pinned, locked environment, so the exact pyarrow version — and therefore the Parquet writer behavior, default compression, and S3 filesystem handling — is deterministic across machines and reruns. A bare `python`/`pip` could pull a different pyarrow and silently change file layout or multipart behavior. For a regulated lakehouse where output must be reproducible and auditable, pinning the toolchain is part of the controls, not a convenience.

Q: Summarize why key layout choices are simultaneously cost, performance, and correctness choices.
A: Layout determines how many LIST/GET requests a query issues (cost), how much irrelevant data is scanned and how parallel the read is (performance), and — together with a table format — whether concurrent writers and atomic publishes work (correctness). A `dt=`-partitioned, right-sized-file layout under a transactional table format gives cheap targeted scans, good parallelism, and safe commits. A flat, tiny-file, rename-dependent layout gives the opposite on all three axes, which at month-end is the difference between a NAV file that lands cleanly and one that is silently dropped.

Q: A multipart upload fails with `EntityTooSmall` on completion — what is the cause and fix?
A: `EntityTooSmall` means at least one non-final part was smaller than the 5 MiB minimum, so the server rejects the assembly at `CompleteMultipartUpload`. The fix is to re-chunk so every part except the last is at least 5 MiB; a common cause is splitting a file by a fixed count where the resulting parts fall below the floor. SDKs usually prevent this by enforcing a minimum part size, so it most often appears in hand-rolled upload code.

Q: What does an `InvalidPart` error on `CompleteMultipartUpload` indicate, and what do you check?
A: `InvalidPart` is a `400` meaning one of the part numbers or ETags in your completion request does not match a part actually stored for that `UploadId`, so the server cannot assemble the object. Check that you sent the exact ETag returned by each `UploadPart` call and the correct part numbers in order, and that no part was silently dropped. A frequent cause is reusing a stale part list after a retried part produced a new ETag.

Q: What does a `NoSuchUpload` error mean during a multipart upload?
A: `NoSuchUpload` (a `400`) means the `UploadId` you referenced does not exist — it was never created, was already completed, was aborted, or was cleaned up by an `AbortIncompleteMultipartUpload` lifecycle rule before you finished. The fix is to re-initiate the upload and stop reusing the dead `UploadId`. This is exactly why a too-aggressive abort-incomplete lifecycle window can break legitimately slow uploads of large holdings extracts.

Q: When creating a bucket idempotently, what does `BucketAlreadyOwnedByYou` tell you versus `BucketAlreadyExists`?
A: `BucketAlreadyOwnedByYou` means the create is a no-op because you already own a bucket with that name, so your script can safely treat it as success, whereas `BucketAlreadyExists` means the globally-unique name is taken by someone else and you must choose another. On MinIO, name collisions are server-scoped rather than global, but the same distinction applies. Idempotent provisioning code should catch `BucketAlreadyOwnedByYou` and continue rather than aborting.

Q: What is the ABFS driver and the `abfss://` scheme, and where do you use them?
A: ABFS (Azure Blob File System) is the Hadoop/Spark driver for ADLS Gen2 that talks to the DFS endpoint to get hierarchical-namespace semantics, addressed via the `abfss://container@account.dfs.core.windows.net/path` URI (`abfss` is the TLS variant of `abfs`). You use it from Spark, Databricks, or Synapse to read and write ADLS Gen2 with real directory and ACL support. It is the ADLS Gen2 analogue of the `s3a://` driver for S3.

Q: Why does ADLS Gen2 expose two hostnames — `blob.core.windows.net` and `dfs.core.windows.net`?
A: The `blob.core.windows.net` endpoint serves the Blob REST API for broad compatibility with existing blob tooling, while `dfs.core.windows.net` serves the Data Lake (DFS) filesystem API that understands directories, atomic rename, and POSIX ACLs. Both front the same underlying data when HNS is enabled, but directory-atomic operations and fine-grained ACLs are only available through the DFS endpoint. Pointing a tool at the wrong endpoint silently loses the filesystem semantics you wanted.

Q: What POSIX-like access control does ADLS Gen2 add that flat S3 buckets lack, and how does it combine with RBAC?
A: With HNS, ADLS Gen2 supports POSIX-style ACLs (read/write/execute) set per directory and per file, layered on top of Azure RBAC role assignments at the container/account scope. RBAC grants coarse role-based access (e.g. Storage Blob Data Reader) and ACLs refine it to specific paths, with the effective permission being the combination. S3's flat model instead relies on IAM policies and bucket policies scoped by prefix, with no per-file inheritable directory ACLs.

Q: How do ADLS Gen2 directory ACLs behave on newly created child files, and why does that matter at scale?
A: A directory carries both an access ACL (applied to itself) and a default ACL that is copied onto children created under it, so new files inherit permissions at creation time rather than retroactively. This matters because changing a directory's default ACL does not rewrite permissions on existing children — you must recurse to update them. So planning ACL inheritance up front avoids expensive recursive ACL fixes across millions of fund files later.

Q: A pyarrow write succeeds but the object never appears in `mc ls` and shows zero bytes when forced — what multipart symptom is this?
A: This is the signature of an initiated-but-never-completed multipart upload: the parts exist as an incomplete upload (billable, invisible to normal listing) but no final key was assembled, so listings show nothing. Check `mc ls --incomplete` (or the S3 `ListMultipartUploads` call) to confirm orphaned uploads. The remedy is to abort them and add an `AbortIncompleteMultipartUpload` lifecycle action so they self-clean.

Q: Which `mc` flag lists in-progress incomplete multipart uploads, and why is it not in normal `mc ls`?
A: `mc ls --incomplete ALIAS/bucket` lists parts of multipart uploads that were initiated but not completed, which normal `mc ls` hides because no readable object key exists yet. Surfacing them is how you find the silent storage leak from abandoned uploads. You then clear them with `mc rm --incomplete` or let an ILM abort rule handle it on a schedule.

Q: How do you remove an incomplete multipart upload manually with `mc`?
A: Use `mc rm --incomplete ALIAS/bucket/key` (add `--recursive` to clear all incomplete uploads under a prefix) to discard the orphaned parts and reclaim the billed space. This maps to the S3 `AbortMultipartUpload` API under the hood. It is the one-off cleanup; for ongoing hygiene you still want the lifecycle abort rule rather than manual sweeps.

Q: Write a single `mc ilm rule add` command that expires bronze after 30 days, expires noncurrent versions after 7, and aborts stale uploads.
A: A combined form is `mc ilm rule add --prefix "bronze/" --expire-days 30 --noncurrent-expire-days 7 --expire-delete-marker local/fund-lakehouse`, with the abort-incomplete window configured via the rule's multipart-abort setting. The `--noncurrent-expire-days` reclaims old versions and `--expire-delete-marker` cleans up dangling delete markers so versioning does not silently retain everything. This is the production-grade bronze rule rather than a bare current-version expiry.

Q: What does the `--noncurrent-expire-days` flag do that plain `--expire-days` does not, on a versioned bucket?
A: `--expire-days` only expires the current version (inserting a delete marker on a versioned bucket), leaving all prior versions consuming storage, whereas `--noncurrent-expire-days` actually removes versions a set number of days after they stop being the HEAD. Without the noncurrent rule, a versioned bronze bucket keeps growing despite a working expiry. So on any versioned bucket you almost always need both flags together.

Q: What does `--expire-delete-marker` clean up, and why is it needed on versioned buckets?
A: It removes delete markers that have become the only remaining version of a key (an "expired" delete marker), which otherwise linger as zero-byte tombstones cluttering listings and slightly inflating metadata. After noncurrent versions are expired, the leftover delete marker serves no purpose, so this flag tidies it. Omitting it leaves a long tail of orphaned delete markers that make `ListObjectVersions` noisier and harder to audit.

Q: How would you confirm with `mc` whether versioning is even enabled on a bucket when diagnosing "storage not shrinking"?
A: Run `mc version info ALIAS/bucket`, which reports whether versioning is Enabled, Suspended, or Off; if Enabled, that explains why simple `--expire-days` is not reclaiming space. This is the first check before adding noncurrent-version rules. Pair it with `mc ls --versions ALIAS/bucket/prefix` to actually see the retained version history piling up.

Q: How do you inspect object-lock retention settings on a bucket or object with `mc`?
A: `mc retention info ALIAS/bucket/key` shows the retention mode and retain-until date for an object, and `mc legalhold info` shows any legal hold; bucket-level defaults appear via the object-lock configuration. This is how you confirm whether a lock — not a broken rule — is preventing lifecycle deletion. In regulated funds this check is routine when a compliance-locked NAV record refuses to expire.

Q: What is the difference between object-lock governance mode and compliance mode?
A: In governance mode, users with a special permission (`s3:BypassGovernanceRetention`) can shorten or remove the retention, making it a guardrail against accidents rather than an absolute lock, whereas compliance mode cannot be bypassed or shortened by anyone — not even the root account — until the retention period expires. Regulated fund records that must be provably immutable use compliance mode. Governance mode suits internal safety where authorized override is acceptable.

Q: For a NAV evidence file that regulation says must be unalterable for 7 years, which lock mode and why?
A: Compliance mode with a 7-year retain-until date, because it is non-bypassable even by the account root, giving auditable WORM that no insider can shorten — exactly the tamper-evidence regulators expect. Governance mode would be insufficient because a privileged user could override it. You combine compliance-mode lock with an archive storage tier so the immutable record is also cheap to retain.

Q: Can you enable object lock on an existing bucket that was created without it?
A: No — S3 and MinIO require object lock to be enabled at bucket creation time (which also forces versioning on), so you cannot retrofit it onto a pre-existing non-locked bucket. To add WORM you create a new lock-enabled bucket and migrate the data into it. Discovering this late is a common planning miss, so lock-enabled buckets for regulatory tiers should be provisioned from the start.

Q: Why does enabling object lock force versioning to be on?
A: Because WORM protection works by making each protected version immutable, and that requires versioning so an overwrite creates a new protected version rather than mutating the locked bytes. Without versioning there would be no per-version retention to enforce. So lock-enabled buckets are always versioned, which means you must also plan noncurrent-version lifecycle to control storage growth.

Q: How do `mc cp --recursive` and `mc mirror` differ when the destination already has some of the files?
A: `mc cp --recursive` re-copies every source object regardless of what exists at the destination (re-PUTting unchanged files and wasting requests), while `mc mirror` compares both sides and transfers only new or changed objects, like `rsync`. For incremental syncs `mirror` is far cheaper; for a guaranteed full overwrite `cp --recursive` is simpler. Using `cp --recursive` for a nightly sync of a large tree is a classic avoidable cost.

Q: What does `mc mirror --remove` do, and why is it dangerous?
A: `--remove` makes the mirror delete objects at the destination that no longer exist at the source, making the destination an exact replica including deletions. It is dangerous because a mistake at the source (or pointing at the wrong source) can cascade into mass deletion of good data at the destination. On regulated data you gate it carefully or avoid it, preferring additive sync plus lifecycle for removals.

Q: How do you make `mc` upload skip objects that already exist unchanged, and what does it compare?
A: `mc mirror` (and `mc cp` with appropriate options) skips objects whose size and ETag/metadata already match at the destination, so unchanged files are not re-transferred. It avoids needless PUTs and the associated request cost. The caveat is that ETag comparison is unreliable across single-PUT versus multipart-uploaded copies of the same bytes, since multipart ETags are not plain MD5s.

Q: Why can the same file have a different ETag on the source and destination after a mirror, even with identical bytes?
A: Because the ETag of a multipart-uploaded object is a hash-of-part-hashes with a `-N` suffix that depends on the part size used, so re-uploading identical bytes with a different part size yields a different ETag. A sync that compares ETags may then think the files differ and re-copy. This is why content verification across tools should use a real checksum (e.g. an SHA-256 the tool computes) rather than trusting ETag equality.

Q: How do you request a strong content checksum on upload so integrity survives multipart, instead of relying on ETag?
A: Modern S3 supports additional checksum algorithms (`CRC32`, `CRC32C`, `SHA1`, `SHA256`) requested via the `x-amz-checksum-*` headers or the SDK `ChecksumAlgorithm` parameter, and the store validates each part and the whole object independently of the ETag. This gives end-to-end integrity that the `-N` multipart ETag cannot. For regulated fund files where corruption must be detectable, enabling a `SHA256` checksum on PUT is the robust choice.

Q: What is the difference between `mc rm --recursive` over a prefix and a single delete, in request-cost terms?
A: A single delete is one `DeleteObject` request, while `mc rm --recursive` must first LIST the prefix to enumerate keys and then issue a delete per object (batched up to 1,000 per `DeleteObjects` call), so cost scales with object count. Deleting a "folder" of a million tiny files is therefore a million-key operation, not one. This is the same flat-key-space, O(N)-per-prefix tax that makes folder operations expensive on S3.

Q: How does the S3 `DeleteObjects` (batch) API reduce the cost of deleting many keys?
A: `DeleteObjects` accepts up to 1,000 keys in a single request, so deleting N objects takes ceil(N/1000) requests instead of N individual `DeleteObject` calls, cutting request count and latency dramatically. `mc rm --recursive` uses this batching under the hood. It does not change the fact that you still pay per object conceptually, but it collapses the round-trips that would otherwise dominate.

Q: When listing with a delimiter, what are `CommonPrefixes` and how does an engine use them to "walk folders"?
A: A `ListObjectsV2` call with `delimiter=/` returns objects directly under the prefix plus a `CommonPrefixes` list of the next-level "subfolder" prefixes, letting a tool emulate a directory walk one level at a time. An engine discovering a partitioned layout uses `CommonPrefixes` to find `dt=` values without reading every key. Recursively descending this way is exactly what makes deep, over-partitioned trees expensive — each level is another set of LIST calls.

Q: How does `StartAfter` (or `ContinuationToken`) in `ListObjectsV2` help, and when do you use each?
A: `ContinuationToken` resumes a paginated listing from where the previous page stopped (the store hands you the token), while `StartAfter` lets you begin a fresh listing after a specified key, useful for resuming or skipping ahead without a prior token. Both let you page through prefixes with more than 1,000 keys. You use the continuation token for normal pagination and `StartAfter` for deliberate range-based scans.

Q: Why does adding more leading-character entropy to keys help throughput on older S3, and is it still needed?
A: S3 historically partitioned request capacity by key prefix, so many keys sharing a hot prefix throttled, and adding high-entropy leading characters spread load across internal partitions. Since 2018 S3 auto-scales to ~3,500 writes and ~5,500 reads per second per prefix and adds partitions automatically, so artificial entropy is generally no longer required. You still avoid funneling extreme burst traffic through one narrow prefix to prevent transient `503 SlowDown`.

Q: A bulk NAV backfill triggers `503 SlowDown` despite modern S3 auto-scaling — what is happening and what do you do?
A: Auto-scaling reacts over time, so a sudden burst against a single new prefix can outrun the current per-prefix capacity before S3 has split it, producing transient `503 SlowDown`. The fix is exponential backoff with jitter (most SDKs do this) plus spreading the backfill across more prefixes or throttling concurrency so the ramp is gentler. It is a rate condition, not an error in your data or credentials.

Q: How does request-rate scaling interact with a `dt=`-partitioned NAV layout during a single day's heavy write?
A: All of one day's writes share the `dt=2026-06-13/` prefix, so a very large single-day backfill concentrates load on one prefix and can approach the per-prefix write ceiling. Spreading within the day (e.g. a sub-prefix by fund family or a hash) distributes writes if you hit throttling, at the cost of slightly more LISTs to read back. For normal once-daily NAV volumes this is rarely an issue, but it matters for massive historical reloads.

Q: What is S3 Select / server-side filtering, and why is it largely superseded for Parquet lakes?
A: S3 Select pushed a simple SQL filter into the storage layer to return only matching rows from a single object, reducing bytes transferred. For columnar Parquet lakes it is largely superseded because engines already do projection and predicate pushdown via HTTP range reads of footers and column chunks, which is more powerful and works across many files. So modern lakehouse engines rely on Parquet statistics and range GETs rather than S3 Select.

Q: How does a query engine read just two columns out of a wide NAV Parquet file without downloading the whole file?
A: It first range-GETs the Parquet footer to learn column-chunk byte offsets and row-group statistics, then issues targeted range GETs for only the two requested columns' chunks in the relevant row groups. This columnar projection over HTTP ranges means a 50-column file costs roughly the bytes of 2 columns plus a small footer. Many tiny files defeat this by multiplying footer reads, which is why right-sized files matter.

Q: What is predicate pushdown at the row-group level, and what file property makes it effective?
A: Predicate pushdown uses each row group's min/max (and optionally bloom filter) statistics to skip row groups that cannot match a filter, so a `WHERE valuation_date = X` or `WHERE isin = Y` reads only row groups whose ranges overlap the predicate. It is effective when data is sorted or clustered on the filter column so min/max ranges are tight and non-overlapping. Random ordering produces wide overlapping ranges that defeat skipping even though the statistics exist.

Q: Why does sorting NAV rows by ISIN before writing Parquet improve later ISIN-filtered queries on a date-partitioned lake?
A: Sorting by ISIN within each file makes each row group cover a narrow, contiguous ISIN range, so the row-group min/max statistics let the engine skip most row groups when filtering one ISIN — recovering the pruning that the `dt=` key layout cannot provide for ISIN. Unsorted data scatters every ISIN across all row groups, forcing full-file reads. So write-time sorting is the cheap lever for the otherwise-expensive "one fund across dates" query.

Q: What is a Parquet bloom filter and when would you add one for fund data?
A: A bloom filter is an optional per-column-chunk probabilistic structure that answers "is this value definitely absent" cheaply, enabling row-group skipping for high-cardinality equality predicates where min/max ranges are too wide to help. You add it on a high-cardinality lookup column like a transaction or order ID frequently queried by exact match. For low-cardinality columns or range queries it adds size without benefit, so it is targeted, not default.

Q: How does the medallion bronze-to-silver promotion typically move data between prefixes on object storage?
A: Promotion reads raw bronze objects, transforms/validates them, and writes new conformed Parquet under the `silver/` prefix — it does not rename or move bronze in place, because there is no cheap rename and bronze is immutable. The bronze copy then expires via its lifecycle rule once silver is durably written. So "promotion" is read-transform-write-new, with lifecycle reclaiming the consumed raw layer.

Q: Why should the bronze expiry window be longer than the maximum time to detect and reprocess a bad silver load?
A: Because bronze is your replay source — if a silver transformation bug is found, you reprocess from bronze, so expiring bronze before defects can surface destroys your ability to re-derive correct silver. The window must exceed the realistic detection-plus-reprocess lag (e.g. a month-end close cycle). Setting 30 days assumes issues surface within roughly a reporting period; longer-latency reconciliations may warrant a longer bronze retention.

Q: A late-arriving NAV correction for an old valuation date must be ingested — how does the `dt=` partition layout handle it?
A: You write the correction into the existing `dt=<original-valuation-date>/` partition (a new file or, under a table format, a new snapshot), keying by the business valuation date rather than ingestion date so the record lands with its peers. Without a table format you append a new file and let downstream dedup/merge logic pick the latest; with Iceberg/Delta you do a `MERGE`. Keying bronze by valuation date, not receipt date, is what makes late corrections land correctly.

Q: Why might keying the bronze prefix by ingestion (receipt) date instead of valuation date cause reconciliation pain?
A: Because a NAV for valuation date D might arrive on D+2 (or a correction weeks later), so a receipt-date layout scatters records for one valuation day across multiple ingest-date prefixes, forcing queries by business date to scan many partitions. Reconciling "all NAVs for valuation date D" then becomes a broad multi-prefix scan instead of one targeted prefix. Aligning the partition key to the business date the data is about keeps date-scoped queries cheap.

Q: How do you decide between partitioning a fund lake by `dt` alone versus `dt` plus `fund_family`?
A: Add the second partition level only if queries frequently filter by that dimension and each resulting partition still holds enough data to avoid the small-files problem; otherwise the extra level multiplies partition count and LISTs for little pruning gain. For a few large fund families queried independently, `dt/fund_family` can help; for thousands of tiny funds it creates a partition explosion. The rule is partition on a dimension only when it is both frequently filtered and sufficiently coarse.

Q: What is the "partition explosion" failure mode and how do you recognize it early?
A: Partition explosion is when high-cardinality or over-nested partition keys create so many tiny partitions that query planning spends most of its time LISTing prefixes and reading footers rather than data. You recognize it from queries whose latency is dominated by planning, huge object counts with tiny average file sizes, and metadata operations outweighing reads. The fixes are coarser partitioning, compaction, or a table format that replaces directory listing with manifest metadata.

Q: How does a table-format manifest change the request profile of planning compared to directory listing?
A: Instead of issuing many `ListObjectsV2` calls to discover files, the engine reads a few manifest files that already enumerate every data file with its partition values and column statistics, turning planning into a small number of metadata GETs. This removes the LIST fan-out that dominates cost on over-partitioned flat layouts and enables file-level pruning before any data read. It is the core reason Iceberg/Delta scale where raw directory listing does not.

Q: When MinIO is deployed with erasure coding, what does the EC parity setting (e.g. EC:4) mean for fault tolerance and usable capacity?
A: An EC parity of N (e.g. `EC:4`) means each erasure set keeps N parity shards, so the object survives the simultaneous loss of up to N drives in that set, at the cost of using roughly N/(data+N) of raw capacity for parity. Higher parity buys more fault tolerance but lowers usable capacity and write throughput. You size parity against your drive count and the failure scenarios you must survive, not arbitrarily.

Q: Why does MinIO require a minimum number of drives to provide erasure coding, and what is the common minimum?
A: Erasure coding needs enough drives to split data and parity shards across independent failure domains, so a single-drive deployment cannot tolerate any loss; MinIO's standard erasure sets commonly span a minimum of 4 drives (and is typically deployed across multiples, e.g. 4 or more). Below the minimum you get no redundancy, which is why a single-disk dev MinIO is not durable. Production runs multiple drives/nodes so the configured parity is meaningful.

Q: A `pyarrow.fs.S3FileSystem` write to MinIO returns `AWS Error NETWORK_CONNECTION` — list the ordered checks.
A: Check, in order: the container is up and listening (`docker compose ps`, port `9000`); `endpoint_override` is set to `localhost:9000` with `scheme="http"` (not the default https, which fails against plain MinIO); credentials match `MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD`; and the region/`allow_bucket_creation` settings are sane. Most local failures are the https-vs-http scheme mismatch or a stopped container, not application logic. Confirm with a quick `mc admin info local` against the same endpoint.

Q: Why does pointing a virtual-hosted-style S3 client at `localhost` fail, and what is the exact remedy?
A: Virtual-hosted style turns the bucket into a subdomain like `bucket.localhost:9000`, which does not resolve in DNS, so the request fails before reaching MinIO. The remedy is forcing path-style addressing (`force_path_style`/`path-style`) so the bucket goes in the URL path as `localhost:9000/bucket/key`; pyarrow does this automatically when you set `endpoint_override`. A `Could not resolve host` or DNS error against a bucket-prefixed hostname is the tell-tale sign.

Q: A `403 RequestTimeTooSkewed` (or signature failure) appears against MinIO that worked yesterday — what is the cause?
A: S3 SigV4 signatures include a timestamp and the server rejects requests whose clock is too far from its own (default tolerance is about 15 minutes) with `RequestTimeTooSkewed`, so the usual cause is a drifted client or container clock, not bad credentials. The fix is to sync time (NTP on the host, or restart a container whose clock froze while the laptop slept). This is a frequent gotcha on a laptop resumed from suspend where the clock lagged before re-syncing.

Q: How do you set the storage class on a PUT so a gold mart lands directly in an infrequent-access tier, and why might you do that?
A: You pass the storage class on upload — `x-amz-storage-class` / the SDK `StorageClass` parameter (e.g. `mc cp --storage-class STANDARD_IA ...`) — so the object is created in the chosen tier rather than transitioned later by lifecycle. You do this when you already know the data is read rarely (e.g. an archival gold mart produced once and queried occasionally), saving the storage cost from day one instead of paying hot rates until a transition fires. The caveat is that infrequent-access tiers add per-GB retrieval fees, so it backfires for data that turns out to be read often.
