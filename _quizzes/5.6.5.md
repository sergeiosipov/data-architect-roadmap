# Phase 4 · 5.6.5 BPM / process orchestration (Camunda, BPMN/DMN) — 150 self-test questions

Q: What does BPMN stand for and what is it?
A: BPMN stands for Business Process Model and Notation, an OMG standard (current major version BPMN 2.0) for drawing executable business processes as flow diagrams. It defines a graphical notation plus an XML serialization, so the same model a compliance officer reads as a picture is also the artifact an engine like Camunda executes. That dual nature — human-readable diagram and machine-executable file — is exactly why regulated shops favour it.

Q: Why do banks and fund administrators run BPMN engines instead of writing all orchestration in code?
A: A BPMN diagram is something a compliance officer or auditor can read and sign off, which they could never do with raw Temporal or Java code, so the model itself becomes the auditable contract. In regulated fund administration the auditability of settlement, onboarding, and exception-handling flows is a regulatory asset, not just developer convenience. The architect's job is to choose model-driven orchestration where that readability pays off and code-driven where it does not.

Q: What is the `.bpmn` file under the hood?
A: It is an XML document conforming to the BPMN 2.0 schema, with elements like `<bpmn:serviceTask>`, `<bpmn:exclusiveGateway>`, and `<bpmn:sequenceFlow>` describing the process graph. Tools such as the Camunda Modeler or `bpmn.io` render that XML as a diagram and let you edit it visually, but the engine deploys and executes the XML directly. Because it is plain XML you can version it in Git and diff it like any other source artifact.

Q: What are the three broad categories of BPMN flow objects?
A: Activities (things that get done, such as tasks and sub-processes), gateways (decision and merge points that route the token), and events (things that happen, such as start, timer, message, and error). Connecting objects called sequence flows wire them together into a directed graph. Mastering this small working subset — tasks, gateways, and events — is enough to model the great majority of real fund workflows.

Q: What is a "token" in BPMN execution semantics?
A: A token is the conceptual marker that represents a single thread of control moving through the process; the engine advances tokens along sequence flows as activities complete and gateways route them. A process instance starts with one token at the start event, and parallel gateways can split one token into several and later join them back. Reasoning about where tokens are is how you reason about what state a running process instance is in.

Q: What is a BPMN service task?
A: A service task (`<bpmn:serviceTask>`) is an automated step the engine hands off to software rather than a human — calling a microservice, running a calculation, or invoking a connector. In Camunda 8 the work is performed by an external job worker that polls for jobs of that task type, executes the logic, and reports completion. It is the workhorse for steps like "request NAV", "validate ISIN", or "post to the ledger".

Q: What is a BPMN user task and when do you use it?
A: A user task (`<bpmn:userTask>`) is a step that pauses the process until a human completes it through a task list UI, used for manual review, approval, or data entry. In Camunda 8 it surfaces in Tasklist, where a reviewer claims and completes the item. You reach for it whenever a fund workflow needs a four-eyes check or a compliance officer's sign-off before continuing.

Q: What distinguishes a send task from a receive task?
A: A send task actively dispatches a message out of the process (for example, emitting a SWIFT instruction), while a receive task suspends the instance until a matching incoming message arrives (for example, a settlement confirmation). Both deal with message flow, but send is outbound-and-continue whereas receive is wait-for-inbound. In Camunda 8 a receive task is correlated by a message name and a correlation key.

Q: What is a business rule task and why externalise the rule?
A: A business rule task (`<bpmn:businessRuleTask>`) delegates a decision to a rule engine, typically by evaluating a DMN decision table, so the branching logic lives in a maintainable table rather than tangled into the flow. This keeps the process diagram clean and lets business analysts change thresholds without touching the process. In Camunda 8 you bind the task to a DMN decision by its decision id.

Q: What is a script task and what caution applies to it?
A: A script task runs an inline script to do lightweight in-process work such as a small transformation. The caution is that embedding business logic in scripts hurts auditability and testability, so for anything non-trivial you prefer a service task with an external worker or a DMN rule task. In Camunda 8, expression evaluation generally uses FEEL rather than a general-purpose scripting language.

Q: What is a gateway in BPMN?
A: A gateway is a diamond-shaped element that controls how tokens diverge and converge — it makes routing decisions or synchronizes parallel paths but performs no work itself. The marker inside the diamond tells you its type: an X for exclusive, a plus for parallel, a circle for event-based. Choosing the right gateway is the difference between a process that branches correctly and one that deadlocks or double-runs.

Q: What does an exclusive gateway (XOR) do?
A: An exclusive gateway routes the token down exactly one outgoing path based on conditions evaluated on the sequence flows, taking the first flow whose condition is true. It models an either/or decision such as "compliance review required → yes vs no". You should provide a default flow so the token always has somewhere to go if no condition matches, otherwise the instance raises an error.

Q: What does a parallel gateway (AND) do?
A: A parallel gateway splits one token into multiple tokens that run all outgoing paths simultaneously, and a matching parallel gateway later joins them, waiting until every incoming branch has arrived before continuing. It is unconditional — every path is taken. Use it when independent steps like "run AML screening" and "fetch custodian balances" can proceed at the same time.

Q: What does an inclusive gateway (OR) do?
A: An inclusive gateway evaluates conditions on each outgoing path and activates every path whose condition is true — so anywhere from one to all branches may run. Its join waits only for the branches that were actually activated. It fits "send notifications to whichever of these parties is affected", where the number of active branches varies per instance.

Q: What is an event-based gateway?
A: An event-based gateway routes based on which event happens first rather than on data conditions — the token waits, and whichever following catch event (a message, a timer, etc.) occurs first wins and the others are discarded. It is the classic pattern for "wait for the settlement confirmation, but time out after two days". This lets you model a race between an external response and a deadline cleanly.

Q: What is the difference between a start event, an intermediate event, and an end event?
A: A start event triggers a new process instance and has no incoming flow; an intermediate event sits in the middle of the flow to catch or throw something as the token passes; an end event terminates that path of the process and has no outgoing flow. Each can carry a type marker such as message or timer. The position constrains what types are legal — for example, an error event is only thrown at the end or caught on a boundary.

Q: What is a timer event and give a fund example?
A: A timer event fires based on time — a specific date, a duration, or a cycle — and is drawn with a clock icon. A timer start event could kick off the daily NAV-strike workflow at a fixed cut-off time, while a timer boundary event on a "await confirmation" task could escalate the case if no reply arrives within the SLA. In Camunda 8, durations are expressed in ISO 8601 form such as `PT24H` for 24 hours.

Q: What is a message event in BPMN?
A: A message event represents communication crossing a process boundary — an incoming message wakes a waiting instance (catch) or an outgoing message is sent (throw), drawn with an envelope icon. A message start event can launch a new process when, say, a subscription order arrives, and a message intermediate catch event can pause until a payment confirmation lands. Correlation by message name and key is what ties an incoming message to the right instance.

Q: What is an error event and how does it differ from a message event?
A: An error event signals that something went wrong inside the process and must be handled — it is thrown by an end event or a service task and caught by an error boundary event or error event sub-process, drawn with a lightning-bolt icon. Unlike a message, an error is an internal technical or business failure, not external communication. Errors interrupt the normal flow and divert the token to the handling path.

Q: What is a boundary event?
A: A boundary event is attached to the edge of an activity and triggers when its event occurs while that activity is running — for example, a timer boundary event on a long-running task fires if the task overruns. Interrupting boundary events cancel the activity and take the token down the boundary path; non-interrupting ones spawn a parallel token and leave the activity running. They are the standard way to attach timeouts and error handling to a single step.

Q: What is the difference between an interrupting and a non-interrupting boundary event?
A: An interrupting boundary event (solid border) cancels the activity it is attached to and routes the token out the exception path, used for timeouts and errors that should abort the step. A non-interrupting boundary event (dashed border) leaves the activity running and additionally emits a token down its path, used for "meanwhile, also do X" like sending a reminder while still waiting. Choosing the wrong one either kills work you wanted to keep or duplicates work you wanted to stop.

Q: What is a terminate end event?
A: A terminate end event ends the entire process instance immediately, killing all remaining active tokens, not just the one that reached it. A plain end event only consumes its own token and lets other parallel branches finish. You use terminate when, for example, a hard validation failure should abort every in-flight branch of a subscription order at once.

Q: What is a sub-process and why use one?
A: A sub-process is an activity that itself contains a BPMN process, letting you collapse detail behind a single box and reuse or scope behaviour. A call activity invokes a separately deployed process, while an embedded sub-process inlines it. Event sub-processes are a special form triggered by an event (such as an error or escalation) to handle cross-cutting concerns without cluttering the main flow.

Q: What is a pool in BPMN?
A: A pool represents a participant or organization in a process — a self-contained business entity such as "Fund Administrator", "Transfer Agent", or "Custodian". Pools partition the diagram by who owns which part, and communication between pools happens through message flows, not sequence flows. They are the top-level unit for modelling responsibility boundaries across separate systems or companies.

Q: What is a lane in BPMN?
A: A lane is a sub-division within a pool that assigns activities to a role, team, or system inside that single participant — for example lanes for "Operations", "Compliance", and "the Camunda engine" within the fund administrator's pool. Sequence flows can cross lanes freely because they are inside one pool. Lanes answer "who or what does this step" without implying a separate organization.

Q: How do you decide between a separate pool and a separate lane?
A: Use a separate pool when the participant is an independent organization or system you communicate with by messages and whose internals you do not control — like the custodian or SWIFT network. Use a lane when the actor is a role or system inside your own process that you orchestrate directly with sequence flows. A common modelling error is putting an external counterparty in a lane, which wrongly implies you control its execution.

Q: Why can sequence flows not cross pool boundaries?
A: Because a pool is an independent participant with its own control flow, so you cannot directly advance the token in someone else's process; you can only communicate across the boundary with message flows. A sequence flow implies "I make this happen next", which is only valid within your own orchestrated process. This restriction is what forces honest modelling of where you hand off to a counterparty.

Q: In a transfer-agency workflow, how might pools and lanes map to the real organizations?
A: You would model the transfer agent, the fund administrator, the distributor, and the custodian as separate pools because they are distinct participants exchanging messages such as orders and confirmations. Within the fund administrator's pool, lanes might separate the dealing desk, the reconciliation team, the compliance reviewer, and the automated engine. The message flows between pools then map to the real interfaces — SWIFT messages, file drops, API calls.

Q: What is the value of modelling responsibility boundaries explicitly with pools and lanes?
A: It makes accountability and hand-offs visible, so an auditor can see exactly which team or system owns each step and where work crosses an organizational line. In a regulated fund context this directly supports governance and the kind of segregation-of-duties evidence DORA-style operational-resilience reviews look for. It also surfaces integration points early, since every message flow between pools is a real interface to build and monitor.

Q: What does DMN stand for and what problem does it solve?
A: DMN stands for Decision Model and Notation, an OMG standard for modelling business decisions as tables and decision requirement diagrams. It solves the problem of business logic — thresholds, eligibility rules, classifications — being buried in code or scattered through a process diagram. By externalising the rule into a decision table, analysts can read and change it without redeploying the process, which is decisive for auditable, frequently-tuned fund rules.

Q: What is a DMN decision table?
A: A DMN decision table is a grid where each row is a rule: input columns hold the conditions and output columns hold the result, with a hit policy governing how matching rows combine. It reads like a spreadsheet of "if these inputs, then this output", which is exactly the format business and compliance staff already think in. Camunda evaluates the table at runtime, typically from a business rule task.

Q: What is FEEL?
A: FEEL is the Friendly Enough Expression Language defined by the DMN standard and used throughout Camunda 8 for conditions, input entries, output entries, and process expressions. It is designed to be readable by business users while still being precise and executable — for example `amount > 1000000` or `instrumentType = "equity"`. Camunda 8 uses FEEL not only inside DMN but also for gateway conditions and input/output mappings, so learning FEEL pays off across the whole model.

Q: Write a FEEL input entry that matches an order amount between 100000 and 1000000 inclusive.
A: You would write `[100000..1000000]`, using FEEL's range syntax where square brackets are inclusive endpoints. An open interval uses parentheses, so `(100000..1000000)` would exclude both ends. FEEL ranges make threshold rules in a decision table compact and unambiguous, which matters when the cells must survive an audit.

Q: What does a hyphen `-` mean in a DMN decision table input cell?
A: A hyphen is the "any" or "don't care" marker, meaning that input is irrelevant for that rule and always matches. It keeps rules concise by letting you specify only the inputs that matter for a given outcome. For example a rule that flags any non-EU domicile regardless of amount would put the amount column as `-`.

Q: What is the UNIQUE hit policy and what is its risk if violated?
A: UNIQUE (the default) asserts that the rules do not overlap so at most one rule can match any given input, returning that single rule's output. Its risk is that if two rules can both match the same input, the table is logically inconsistent and Camunda will raise an evaluation error rather than silently picking one. It is the safest default for clear, mutually-exclusive decisions like a single review verdict.

Q: What is the FIRST hit policy?
A: FIRST allows overlapping rules and returns the output of the first matching rule in row order, so rule ordering becomes meaningful. It is convenient for "first applicable wins" cascades, but it makes the table harder to audit because correctness now depends on sequence. Many modelling-best-practice guides discourage FIRST precisely because re-ordering rows silently changes behaviour.

Q: What is the COLLECT hit policy?
A: COLLECT returns a list of the outputs of all matching rules, optionally aggregated with an operator such as sum, min, max, or count. It fits cases where multiple rules can apply and you want all results — for example collecting every compliance flag an order triggers. With an aggregator like COLLECT-SUM you can total numeric outputs across the matching rules.

Q: When modelling a "compliance review required?" rule, why put it in DMN rather than an exclusive gateway?
A: Because the criteria — amount thresholds, investor type, domicile, instrument class — are numerous, change often, and must be auditable, a DMN table captures them as readable rows a compliance analyst can verify and edit. An exclusive gateway with hand-coded conditions would bury that logic in the diagram and require a redeploy for every threshold tweak. The process stays stable while the rule evolves in the table.

Q: How does a BPMN process consume the result of a DMN decision?
A: A business rule task evaluates the DMN decision and writes its output into a process variable, which a following exclusive gateway then branches on. So DMN decides "reviewRequired = true", and the gateway routes the token to the human review task or straight through. This separation keeps the decision logic in DMN and the routing logic in BPMN, each in its natural home.

Q: What is a Decision Requirements Diagram (DRD) in DMN?
A: A DRD is the higher-level DMN view that shows decisions, their input data, and how decisions feed into one another, above the level of any single table. It lets you compose decisions — for example a "risk classification" decision feeding a "review required" decision. The DRD documents the dependency structure so reviewers see how a final verdict is built up from sub-decisions.

Q: Give a FEEL expression that checks an investor is a professional client domiciled outside the EU.
A: Something like `investorType = "professional" and domicile != "EU"` works, combining string equality with the logical `and` and the inequality operator `!=`. FEEL's `and`/`or`/`not` keywords and standard comparison operators make such compliance predicates read close to plain English. You would typically place this as an input entry or rule condition in the relevant decision table.

Q: What process engine underlies Camunda 7 versus Camunda 8?
A: Camunda 7 is built on the older Activiti-derived engine and stores process state in a relational database, executing the BPMN in the same JVM as your application (embeddable). Camunda 8 is built on Zeebe, a purpose-built, horizontally scalable, cloud-native engine that does not use a relational database for execution. This swap of execution core is the single biggest difference driving everything else about the two versions.

Q: Why was the relational database a scaling bottleneck in Camunda 7?
A: In Camunda 7 every process step is a database transaction against shared tables, so under high throughput the relational store becomes a contention and locking bottleneck that is hard to scale horizontally. Zeebe was designed specifically to remove that constraint. The relational model that made Camunda 7 simple to embed is exactly what limited its throughput ceiling.

Q: How does Zeebe persist process state instead of a relational database?
A: Zeebe is event-sourced: it appends every state change to a distributed, replicated commit log rather than mutating rows in a relational table. The current state is derived from that append-only log, which is what gives Zeebe its high write throughput and horizontal scalability. This is the same log-centric philosophy you see in Kafka-style systems applied to workflow state.

Q: What are partitions in Zeebe and why do they matter?
A: A partition is a shard of Zeebe's data and processing; the broker cluster divides process instances across partitions so work runs in parallel, which is how Zeebe scales horizontally. Each partition is an independent ordered log with its own leader. More partitions means more throughput, but you fix the count at cluster setup because re-partitioning live is not a casual operation.

Q: What role does Raft play in a Zeebe cluster?
A: Raft is the consensus protocol Zeebe uses to replicate each partition across several brokers and elect a leader for it. A record is only committed once a quorum (majority) of the partition's replicas has acknowledged it, which makes committed state durable even if an individual broker loses all its data. This is how Zeebe achieves high availability without a shared relational database.

Q: What does the Zeebe Gateway do?
A: The Zeebe Gateway is the entry point clients connect to; it routes requests to the correct partition leaders and acts as a load balancer in front of the broker cluster. Clients and job workers talk to the gateway rather than to brokers directly, so the gateway hides the cluster topology. It speaks the Zeebe protocol (gRPC) and, in newer versions, an Orchestration Cluster REST API.

Q: How does Camunda 8 execute the work of a service task?
A: Camunda 8 uses external job workers: the engine creates a "job" of the task's type, and an external worker process polls the gateway, activates available jobs, runs the business logic in its own service, then sends back a complete or fail command. The business logic therefore runs outside the engine, decoupled and independently scalable. This is a fundamental shift from Camunda 7, where Java delegates often executed inside the engine's transaction.

Q: What is "long polling" by a Zeebe job worker?
A: Long polling means a worker's activate-jobs request is held open by the gateway while no jobs of that type are available, and returns as soon as a job appears, instead of the worker hammering the server with empty polls. This reduces latency and wasted requests. A worker configures things like `maxJobsActive` and a poll interval, and Zeebe streams activated jobs to it when they become available.

Q: How does a Camunda 7 Java delegate differ from a Camunda 8 job worker in coupling terms?
A: A Camunda 7 Java delegate runs inside the engine's process and transaction, so the engine and your code are tightly coupled and deployed together, often in one monolith. A Camunda 8 job worker is a separate process communicating with the engine over the network (gRPC/REST), so business logic is decoupled, independently deployable, and language-agnostic. That decoupling is what lets Camunda 8 fit a microservices estate.

Q: What is an exporter in Zeebe and what is it commonly used for?
A: An exporter streams the records from Zeebe's internal log out to an external system; the standard one ships records to Elasticsearch (or OpenSearch), which is what powers the Operate and Tasklist monitoring tools. Zeebe itself does not serve rich queries over historical data, so exporters externalise that concern. Exporters are a self-managed concept; in the SaaS offering Camunda runs them for you.

Q: What are Operate and Tasklist in Camunda 8?
A: Operate is the operations dashboard for monitoring and inspecting running and completed process instances — seeing where instances are, diagnosing incidents, and resolving them. Tasklist is the UI where humans see and complete BPMN user tasks. Both read from the Elasticsearch index populated by the Zeebe exporter rather than from the engine's hot log directly.

Q: What is the licensing shift between Camunda 7 and Camunda 8 self-managed?
A: Camunda 7 had a genuinely open-source community edition (Apache-licensed) usable in production for free, whereas Camunda 8's core components such as Zeebe, Operate, and Tasklist are now under the Camunda License (source-available, not OSI open source). Under the unified licensing introduced around Camunda 8.6, the free self-managed edition is for non-production use, and production self-managed use requires a paid or qualifying licence. This is a material change an architect must factor into total cost.

Q: Why does the Camunda 8 licensing change matter to an architecture decision in a regulated fund?
A: Because "we can run it free in production like Camunda 7 community" is no longer true for the core components, so the architect must budget for a production licence or a qualifying non-commercial programme, and weigh that cost against alternatives. In a procurement-heavy, audited environment, licence terms and vendor lock-in are first-class architectural concerns. Getting this wrong surfaces as a surprise cost or a compliance finding late in delivery.

Q: Which Camunda 8 connector components are under Apache 2.0 rather than the Camunda License?
A: The Connector SDK, the out-of-the-box REST connector, and the Connector Runtime and Connectors Bundle Docker images are licensed under Apache 2.0. The core orchestration components (Zeebe, Operate, Tasklist, Optimize, Identity) are under the source-available Camunda License. Knowing which piece is under which licence prevents incorrect assumptions about what you may freely run in production.

Q: Can you embed the engine in your own JVM application in Camunda 8 the way you could in Camunda 7?
A: No — Camunda 7's embeddable engine ran inside your application's JVM and transaction, but Camunda 8's Zeebe is a separate distributed system you connect to as a client over the network. There is no embedded Zeebe engine; you talk to a cluster. Teams migrating from Camunda 7 must therefore rethink any design that relied on sharing the engine's database transaction with application code.

Q: In Camunda 7, what did sharing the engine's transaction with your code give you, and what replaces it in Camunda 8?
A: In Camunda 7 a Java delegate ran in the same ACID transaction as the engine, so a step and its process-state update committed or rolled back together. Camunda 8 has no shared transaction; instead it relies on the job-worker model with at-least-once job delivery, idempotent workers, and the engine's own consistency via the replicated log. You design for retries and idempotency rather than for a single distributed transaction.

Q: What is a human-task workflow?
A: A human-task workflow is a process that includes one or more BPMN user tasks requiring a person to act — review, approve, correct, or decide — before the token can advance. It blends automation with human judgement, which is essential where rules cannot fully decide an outcome or where regulation mandates a human in the loop. The engine tracks who owns each task and resumes automatically once it is completed.

Q: What is an exception queue in a process-orchestration context?
A: An exception queue is the set of process instances that have hit a problem the automation cannot resolve and are parked for human handling — a work list of failures awaiting manual review. In BPMN you model this by routing the error or failure path to a user task that lands in Tasklist. It turns silent failures into visible, assignable, auditable work, which is critical for fund operations where a stuck NAV or order must not be lost.

Q: How do you route a failure to a human task in BPMN?
A: You attach an error boundary event (or use an exclusive gateway on a failure flag) to the failing activity and direct its outgoing path to a user task for manual review. When the activity throws the error, the token leaves the automated path and lands in the reviewer's task list, where a human investigates, fixes the data, and completes the task to resume the flow. This is the canonical "happy path plus exception path to a human" pattern.

Q: After a human resolves an exception task, how does the process resume "correctly"?
A: Completing the user task advances the token from that task down its outgoing sequence flow, typically rejoining the main flow or moving to a remediation branch, so the instance continues from a well-defined point rather than restarting. The reviewer may also set process variables (such as a corrected ISIN or an approval decision) that downstream steps read. Correct resumption means the model explicitly defines where control goes after the human acts.

Q: What is the difference between task assignment and task claiming in a human-task workflow?
A: Assignment designates which user or group a task belongs to (often via a candidate group like "compliance reviewers"), while claiming is the act of a specific user taking ownership of a task from a shared group queue so others stop seeing it as available. This supports the common pattern of a team queue where any reviewer can pull the next item. Tracking assignee and claim is also part of the audit trail of who did what.

Q: Why is a human task valuable for a four-eyes (maker-checker) control in fund administration?
A: A user task lets you enforce that a second person reviews and approves a sensitive step — like a manual NAV adjustment or a large redemption — before it proceeds, and the engine records the approver and timestamp. That recorded approval is exactly the segregation-of-duties evidence auditors and regulators expect. Modelling it as a BPMN user task makes the control explicit, enforced by the engine rather than by hope.

Q: How would you add an SLA/escalation to a human review task?
A: Attach a timer boundary event to the user task; if the reviewer does not complete it within the configured duration (e.g. `PT4H` in ISO 8601), the timer fires and routes a token to an escalation path — reassigning, notifying a supervisor, or auto-rejecting. A non-interrupting timer can send a reminder while leaving the task open, whereas an interrupting one removes it. This turns "someone should look at this soon" into an enforced operational deadline.

Q: What does the "orchestration trichotomy" refer to in this lesson?
A: It refers to the architect's core decision among three classes of orchestration tool: BPM engines (Camunda/BPMN), durable execution frameworks (Temporal), and schedulers/DAG tools (Airflow). Each is the right answer for a different shape of workflow, and the architect must choose per workflow with explicit reasons. The skill is matching workflow characteristics — human involvement, auditability, code complexity, scheduling — to the right tool, not defaulting to a favourite.

Q: When is a BPM engine like Camunda the right orchestration choice?
A: When the workflow needs business-readable, auditable models, involves human tasks and approvals, and where compliance officers must review and sign off the process — classic for onboarding, settlement exceptions, and regulated reviews. The model-as-contract auditability and first-class human-task support are BPM's distinctive strengths. If those are not requirements, a lighter tool is often a better fit.

Q: When is Temporal (durable execution) the right orchestration choice?
A: When the workflow is fundamentally code — complex, developer-owned logic, long-running, needing reliable retries and state across failures — and there is no need for a non-developer to read a diagram. Temporal lets engineers write workflows as durable code with automatic state persistence and replay, which is ergonomic for engineering-heavy sagas. Its weakness in this lesson's frame is that a compliance officer cannot read Temporal code the way they read a BPMN diagram.

Q: When is Airflow (or a scheduler/DAG tool) the right orchestration choice?
A: When the work is batch data pipelines on a schedule — "run these dependent tasks daily in this order" — where the unit is a job in a DAG, not a long-lived stateful business process with human steps. Airflow excels at time-driven, data-engineering orchestration with retries and backfills. It is the wrong tool for human approvals or event-driven, instance-per-entity business processes.

Q: A daily batch that loads custodian files, computes NAV, and publishes it — which orchestration tool fits and why?
A: A scheduler/DAG tool like Airflow fits best: it is a time-triggered batch pipeline of dependent data tasks with clear ordering, retries, and backfill needs, and no human-in-the-loop step. BPMN would be over-engineering a scheduled ETL, and Temporal's durable-code model adds little for a straightforward DAG. The deciding factors are time-driven scheduling and data-pipeline shape.

Q: An investor onboarding flow with KYC checks, document collection, and a compliance officer's approval — which tool fits and why?
A: A BPM engine like Camunda fits, because the flow mixes automated steps with human review and approval, must be auditable, and benefits from a diagram a compliance officer can read and sign off. Human tasks, exception queues, and audit trails are exactly BPM's strengths. Temporal could run the logic but loses the business-readable, signable model; Airflow is simply the wrong shape for an instance-per-investor human workflow.

Q: A long-running settlement saga with intricate compensation logic owned entirely by engineers — which tool fits and why?
A: Temporal (durable execution) fits well, because the logic is complex, code-centric, long-running, and needs reliable retries and compensation, with no requirement for a non-developer to read a diagram. Engineers express the saga as durable code that survives crashes via replay. BPMN could model it but the orchestration value here is engineering ergonomics and durability, not diagram auditability.

Q: What is the key trade-off between model-driven (BPMN) and code-driven (Temporal) orchestration?
A: Model-driven orchestration buys you auditability and business-stakeholder readability at the cost of being clumsier for highly complex programmatic logic; code-driven orchestration buys you engineering expressiveness and durability at the cost of being opaque to non-developers. The lesson frames this as: a compliance officer can sign off a BPMN diagram but never Temporal code. The right choice depends on whether auditability-by-non-developers is a hard requirement.

Q: Why might a regulated fund administrator persist with BPM engines even when durable execution is technically elegant?
A: Because regulatory and audit requirements value a process artifact that compliance and audit staff can read, review, and sign off, and that maps cleanly to controls and segregation of duties — a benefit Temporal code does not provide. The engine-enforced, diagrammed process becomes evidence for operational-resilience and governance obligations. The persistence of BPM in regulated shops is driven by auditability, not by raw technical superiority.

Q: What is `bpmn.io` and how does it relate to Camunda?
A: `bpmn.io` is the open-source, browser-based toolkit (BPMN and DMN modelling libraries and a viewer/editor) maintained by Camunda for drawing and embedding diagrams. The Camunda Modeler and the web modeller are built on it. You can use `bpmn.io` to author the `.bpmn` and `.dmn` XML that you then deploy to a Camunda engine, or embed a diagram viewer in your own app.

Q: What is Zeebe's primary client protocol?
A: Zeebe's native client protocol is gRPC, used by job workers and clients to deploy processes, create instances, and activate/complete jobs against the gateway. Newer Camunda 8 versions also expose an Orchestration Cluster REST API as an alternative for some operations. Choosing gRPC versus REST affects worker latency and the long-polling behaviour available.

Q: What is the deployment unit you push to a Camunda 8 engine, and how?
A: You deploy BPMN and DMN resources (the `.bpmn` and `.dmn` files) to the cluster via the gateway, after which the engine can create instances of those process and decision definitions. A deployment is versioned, so deploying a changed model creates a new version while existing instances continue on their original version. This versioning is important when you must keep in-flight fund processes running on the model they started under.

Q: What happens to running instances when you deploy a new version of a process in Camunda?
A: Existing instances continue running on the process version they were started with, while new instances use the newly deployed version, because each deployment is independently versioned. To move running instances onto a new version you must perform an explicit process-instance migration. This protects long-running cases — like a multi-day settlement — from being broken by a mid-flight model change.

Q: What is an incident in Camunda 8?
A: An incident is the engine's record that a process instance is stuck due to an unhandled problem — a job that failed all its retries, an expression that could not be evaluated, or a missing variable — and it pauses that part of the instance until resolved. Operate surfaces incidents so operators can diagnose and retry or fix them. Incidents are how the platform makes failures explicit rather than silently dropping work.

Q: A service task keeps failing and creates an incident — what is the first thing to check?
A: First check the incident details in Operate, which show the failing job type, the error message, and the variables, because the cause is usually a worker exception, a missing process variable, or bad input data. Confirm a worker for that task type is actually running and subscribed, since "no worker" looks like a stalled task. Once the root cause is fixed you resolve the incident and the job is retried.

Q: Your service task never executes and the instance just sits there with no incident — what is the likely cause?
A: The most likely cause is that no job worker is subscribed for that task's type, so the job is created but never activated; without a worker there is no failure either, just a waiting job. Check that a worker process is running and configured with the exact task type string used in the model. A typo between the BPMN task type and the worker's registered type is a classic cause of this silent stall.

Q: How does at-least-once job delivery affect how you write a Camunda 8 worker?
A: Because Zeebe guarantees a job is delivered at least once, a worker can occasionally receive the same job twice (for example after a timeout or restart), so the worker's logic must be idempotent. You design steps like "post to ledger" or "send SWIFT instruction" so that re-execution does not double-post or double-send. This is the price of the decoupled, crash-tolerant job model replacing Camunda 7's shared transaction.

Q: What is a message correlation key in Camunda 8 and why is it needed?
A: A correlation key is the value (drawn from a process variable, e.g. an order id) that the engine uses to match an incoming message to the specific waiting instance it belongs to. Without it, an arriving settlement confirmation could not be tied to the right one of thousands of in-flight orders. You define the message name and the correlation key expression on the catch event so the engine routes the message precisely.

Q: What is a message buffering improvement in Camunda 8 compared to Camunda 7?
A: Camunda 8 can buffer a message that arrives before its target process instance is ready to receive it, holding it (within a time-to-live) until the instance reaches the matching catch point, which reduces race conditions in distributed systems. In Camunda 7 a message arriving too early would simply not correlate. This makes event-driven fund flows more robust when confirmations can race ahead of the process.

Q: How would you model the subscription-order lifecycle in BPMN with a happy path and an error path to a human task?
A: Start with a message start event for the incoming order, then automated service tasks (validate order, check ISIN, screen investor), a business rule task for "compliance review required", and an exclusive gateway that either continues to booking or routes to a user task for manual compliance review. Attach error boundary events on the automated tasks so technical failures also land in an exception user task. Completing the human task either resumes the booking path or rejects the order to an end event.

Q: In that subscription-order model, where does DMN fit?
A: DMN fits at the business rule task that decides whether compliance review is required, evaluating a decision table on inputs like order amount, investor type, and fund domicile and outputting a `reviewRequired` flag. The following exclusive gateway branches on that flag — straight-through processing if false, human review task if true. This keeps the changeable compliance thresholds in an auditable table separate from the process flow.

Q: Why deliberately trigger the error path at least once when testing a BPMN process?
A: Because the exception and human-task path is the part most likely to be wrong and least exercised by happy-path testing, so you must prove that a failure actually diverts to the review task and that completing it resumes the flow correctly. Untested error paths are where stuck or lost fund instances hide. Forcing the error validates the whole exception-queue mechanism end to end.

Q: What should a written comparison of the BPMN saga versus the Temporal saga emphasize?
A: It should weigh auditability and business-readability (BPMN's edge — a signable diagram, first-class human tasks) against engineering expressiveness and durability for complex logic (Temporal's edge — durable code, retries, replay). It should conclude per workflow: pick BPMN when human review and compliance sign-off dominate, Temporal when intricate engineer-owned logic dominates. The deliverable is a reasoned recommendation, not a generic feature list.

Q: How do FEEL conditions appear on a BPMN sequence flow out of an exclusive gateway?
A: Each conditional outgoing flow carries a FEEL boolean expression (a condition expression), and the gateway takes the first flow whose expression evaluates true, falling back to the default flow if none match. For example one flow's condition is `reviewRequired = true` and the default handles the rest. Because Camunda 8 uses FEEL here too, the same expression language spans gateways, DMN, and variable mappings.

Q: What are input and output variable mappings on a Camunda 8 task?
A: Input mappings transform or rename process variables into the local variables a task sees, and output mappings map a task's results back into process variables, both expressed in FEEL. They let you keep a clean variable contract — a worker need only know its inputs, and its outputs are merged back under controlled names. This reduces coupling and avoids variable-name collisions across a large fund process.

Q: How does Camunda 8 support multi-instance activities, and where is that useful?
A: A multi-instance marker on a task or sub-process runs it once per element of a collection, either sequentially or in parallel, gathering the results. It is useful when, say, a redemption order must run the same validation per underlying holding, or a corporate action applies per affected position. You configure the input collection and the per-item variable, typically with FEEL.

Q: What is the difference between a process variable's scope at instance level versus within a sub-process?
A: Variables can be set at the top-level instance scope or local to a sub-process/activity scope; a local variable is visible within its scope and does not leak upward unless mapped out. This scoping prevents inner steps from clobbering shared instance variables and lets you reuse variable names safely. Misunderstanding scope is a common cause of "the value I set disappeared" bugs.

Q: Why might you choose a call activity over an embedded sub-process for a reusable fund check?
A: A call activity invokes a separately deployed, independently versioned process, so a shared routine like "AML screening" or "ISIN validation" lives once and is reused across many parent processes. An embedded sub-process is inlined and not reusable elsewhere. The call-activity approach supports governance — one place to update and audit a shared control — at the cost of managing the separate definition.

Q: What is an escalation event and how does it differ from an error event?
A: An escalation event signals a non-fatal "raise this up" condition — like notifying a supervisor — and can be non-interrupting so the activity keeps running, whereas an error event signals a failure that interrupts and is handled. Escalation is for "flag it but carry on / handle in parallel"; error is for "stop and divert". Choosing escalation for a soft alert avoids needlessly aborting a still-valid process branch.

Q: How does Camunda 8 give you an audit trail of a process instance?
A: Zeebe's event-sourced log records every state transition, and via the exporter that history lands in Elasticsearch where Operate presents the full path each instance took, including which tasks ran, who completed user tasks, and when. This produces a defensible, queryable record of process execution. In a regulated fund context that trail is the evidence that controls actually executed as modelled.

Q: What is Optimize in the Camunda 8 stack?
A: Optimize is the analytics and reporting component that builds dashboards and reports over process and decision data — cycle times, bottlenecks, SLA breaches, and decision outcomes. It reads the exported history rather than the live engine. For fund operations it can answer questions like "how long do compliance reviews take" or "how often does the error path fire", supporting continuous improvement and regulatory reporting.

Q: Why is idempotency especially important for a service task that emits a SWIFT instruction?
A: Because at-least-once delivery means the worker could run twice, and accidentally sending a duplicate SWIFT settlement instruction has real financial consequences. The worker must therefore deduplicate — for example keying on a unique reference so a re-run is a no-op. Designing such steps to tolerate replay is mandatory when the side effect is a real money movement.

Q: A timer boundary event on a human review task is firing too early — first thing to check?
A: Check the timer's duration definition: it must be a valid ISO 8601 duration such as `PT4H`, and a malformed or wrongly-unit value (minutes vs hours) is the usual culprit. Also confirm the timer is interrupting/non-interrupting as intended and that the engine's clock and timezone assumptions match expectations. A common mistake is writing the duration as a plain number instead of the ISO 8601 form.

Q: How do you decide BPM vs Temporal vs Airflow for three different fund workflows in an interview-defensible way?
A: For each workflow you name the deciding characteristics — human tasks and auditability point to BPM, intricate engineer-owned durable logic points to Temporal, scheduled batch data pipelines point to Airflow — and give a concrete reason, not a preference. For example: NAV batch publish → Airflow (time-driven DAG); investor onboarding → Camunda (human approval, auditable); complex settlement compensation saga → Temporal (durable code). The defence is the mapping of workflow shape to tool strength with reasons.

Q: What is the single most important reason BPMN/DMN engines persist in regulated finance, summarised?
A: Auditability: the BPMN diagram and DMN tables are artifacts that compliance and audit staff can read, review, and sign off, and the engine enforces and records exactly that modelled behaviour. This makes the process itself a control and a piece of evidence, aligning with governance and operational-resilience obligations. No equivalently business-readable, signable artifact exists for code-driven orchestration, which is why these engines endure.

Q: What does the FEEL `not()` negation look like when filtering a list of fund order statuses?
A: FEEL offers a unary `not(...)` form for negating a value or test, so an input entry like `not("settled", "cancelled")` matches any status that is neither settled nor cancelled. This is more readable in a decision-table cell than spelling out every other status explicitly. It pairs naturally with list and range tests to express "everything except these" rules compactly.

Q: In a Camunda 8 Java worker, how do you fail a job so it retries versus so it raises an incident immediately?
A: Call `newFailCommand(job.getKey())` with `.retries(job.getRetries() - 1)` to decrement and trigger another attempt, but pass `.retries(0)` to exhaust retries and raise an incident at once. You can also add `.retryBackoff(Duration.ofSeconds(30))` so the next attempt is delayed rather than immediate, which is the right move for a transient downstream outage. Setting retries to a positive number when the failure is permanent just wastes attempts before the inevitable incident.

Q: When should a worker call `newThrowErrorCommand()` instead of `newFailCommand()`?
A: Use `newThrowErrorCommand()` with an `.errorCode(...)` for a business error that the model is meant to handle — like `"isinInvalid"` — so the token diverts to a matching BPMN error boundary event rather than stalling. Use `newFailCommand()` for technical failures that should retry or, when exhausted, become an incident for an operator. The distinction is "the process knows how to react to this" (throw error) versus "this is an unexpected breakage" (fail job).

Q: A BPMN error thrown by a worker is not being caught and the job just keeps failing — first thing to check?
A: Confirm the `errorCode` on the throw command exactly matches the code declared on an error boundary event (or error event sub-process) reachable from that task, because an unmatched error code is not caught and surfaces as an incident instead. Error matching is by exact string code, not by message text, so a casing or typo mismatch like `"ISINInvalid"` vs `"isinInvalid"` silently breaks it. Also verify the boundary event is attached to the right activity in scope.

Q: How many times does Camunda 8 retry a failed job by default, and what happens after?
A: A service task defaults to three retries (`retries = 3` in the BPMN), and once the worker has decremented retries to zero an incident is raised and the job stops being offered until the incident is resolved. You override the count by setting the task's retries attribute in the model or in the fail command. The default of three is just a convention you should tune per task — a flaky external API may warrant more, an obviously fatal validation fewer.

Q: How do you resolve an incident caused by exhausted retries, and what must happen first?
A: In Operate you update the job's retries back to a positive number (or fix the root cause first, such as restarting the missing worker or correcting a variable), then the engine re-offers the job. Resolving the incident without fixing the underlying cause just produces another failure and a fresh incident. The correct order is diagnose, fix the data or worker, then bump retries to retry.

Q: Express a Camunda 8 timer that repeats three times every hour, and one that repeats forever daily.
A: Use the ISO 8601 repeating-interval form `R3/PT1H` for three repetitions one hour apart, and `R/P1D` for an unbounded daily cycle. Omitting the repetition count (just `R/...`) means repeat infinitely until the instance is cancelled. These cycle expressions belong on timer start events or non-interrupting timer boundary/intermediate events, not on a one-shot interrupting timer.

Q: Camunda 8 also supports cron in timer cycles — what is the one gotcha about its fields?
A: Camunda 8 supports cron expressions for timer cycles, but the first field is seconds, not minutes, so a standard five-field cron pasted in will be off by one position. For example `0 0 9 * * MON` fires at 09:00:00 every Monday because the leading `0` is the seconds field. Forgetting the seconds field is the classic cause of a "NAV strike job runs at the wrong time" bug.

Q: When is a timer start event's cycle expression evaluated versus a timer catch event's?
A: A timer start event's expression is evaluated at deploy time, so the schedule is fixed when you deploy the process, whereas an intermediate/boundary timer catch is evaluated when the token activates that event. This means a process variable can drive a catch timer's duration dynamically but cannot drive a start timer, which has no instance yet. Architecting a per-instance SLA therefore uses a boundary timer fed by a FEEL expression, not a start timer.

Q: How do you give each waiting order instance a different timeout duration from a process variable?
A: Put a FEEL expression on the timer boundary event that resolves to an ISO 8601 duration string from a variable, for example `=duration(slaWindow)` or a variable already holding `"PT48H"`. Because the catch timer is evaluated on activation, the engine reads the instance's variable at that moment and schedules accordingly. This is how a premium investor gets a shorter confirmation SLA than a retail one in the same model.

Q: What guarantees idempotent message handling in Zeebe, and which fields drive it?
A: Supplying both a `correlationKey` and a `messageId` makes a published message idempotent: the same `messageId`/`correlationKey` pair is only ever correlated (or only ever starts a process) once within the message's lifetime. This lets an at-least-once upstream re-deliver a settlement confirmation without double-correlating it. Omitting `messageId` removes that dedup guard and reopens the door to duplicate processing.

Q: What does the `timeToLive` on a published message control, and why does it matter for racing events?
A: `timeToLive` is how long Zeebe buffers a published message waiting for a subscription to appear, after which the unconsumed message is discarded. A generous TTL lets a confirmation that arrives before the process reaches its receive task still correlate once the token gets there. Set it too short and an early-arriving confirmation is dropped, leaving the instance to wait forever or time out.

Q: An incoming confirmation message is not waking the waiting instance — what is the first thing to check?
A: Verify the message name and correlation key match exactly: the published message's `name` and `correlationKey` value must equal the catch event's message name and the resolved correlation-key expression on the instance. A mismatch between, say, the order id stored on the instance and the key sent with the message means there is simply no subscription to hit. Next, confirm the message did not expire because its TTL elapsed before the token reached the receive event.

Q: How do you start a process instance and publish a message from the command line during testing?
A: Use the Zeebe CLI `zbctl`: `zbctl create instance <processId>` to start an instance and `zbctl publish message <name> --correlationKey <key>` to send a correlating message, optionally with `--variables`. It talks to the gateway over gRPC just like a worker. This is the quickest way to force the error path or drive a receive task in a smoke test without writing client code.

Q: What FEEL list function gives the position of an element, and what is the index base?
A: Use `index of(list, element)`, which returns a list of the 1-based positions where the element occurs, because FEEL lists are indexed from 1, not 0. So `index of(["a","b","c"], "b")` yields `[2]`. The 1-based convention trips up developers from zero-indexed languages and is a frequent off-by-one source in decision logic.

Q: How would you write a FEEL condition that an order's instrument type is one of several allowed values?
A: Use the membership test `list contains(["equity","bond","fund"], instrumentType)`, which returns true when the variable matches any element. It is cleaner than chaining `instrumentType = "equity" or instrumentType = "bond"`. The same list can be reused across several rules, which keeps an allow-list consistent and auditable.

Q: What FEEL function extracts part of a list, and what do its 1-based and negative indices mean?
A: `sublist(list, start, length)` returns a slice beginning at the 1-based `start`; a negative start counts from the end, so `sublist(items, -1)` returns the last element's slice. For example `sublist([10,20,30,40], 2, 2)` gives `[20,30]`. Using these on a collection of holdings lets a rule reason about, say, the top-N positions without external code.

Q: How do you sum a numeric output across all matching rules in a DMN table?
A: Set the hit policy to COLLECT with the sum aggregator, shown as `C+` in the table header, so the result is the total of every matching rule's numeric output. This fits totalling a risk score that several independent flags each contribute to. The aggregator only applies to single-output numeric tables; mixing it with non-numeric outputs is invalid.

Q: What is the RULE ORDER hit policy and when is it appropriate?
A: RULE ORDER (`R`) returns the outputs of all matching rules as a list in the order the rules appear in the table, without deduplication. It suits cases where every applicable rule should fire and their declared sequence is meaningful — for example a prioritized list of compliance actions. Unlike COLLECT it preserves order and unlike FIRST it does not stop at the first match.

Q: What is the PRIORITY hit policy and how does it differ from FIRST?
A: PRIORITY (`P`) returns a single output chosen by the declared priority order of output values, independent of the order the rules are written in, whereas FIRST depends on physical row order. So PRIORITY lets you reorder rows freely without changing behaviour because precedence is defined on the outputs themselves. It is safer for audit than FIRST since correctness no longer hinges on spreadsheet row positions.

Q: What happens when a UNIQUE-policy table gets two overlapping matching rules at runtime?
A: Camunda raises an evaluation error (surfacing as an incident on the business rule task) rather than silently returning one output, because UNIQUE asserts the rules are mutually exclusive. This is by design: it forces you to fix the overlapping thresholds rather than ship an ambiguous decision. The fix is to tighten the input conditions so at most one rule can match any input.

Q: What does a DMN table return when the hit policy is UNIQUE or FIRST and no rule matches?
A: It returns `null` (an empty result) for a single-hit policy when no rule matches, which downstream FEEL must handle. A gateway branching on a `null` `reviewRequired` will not take a `= true` flow, so you should either provide a catch-all rule or default the variable. Unhandled `null` outputs are a common cause of "the order went down neither branch" surprises.

Q: How do you supply a sensible fallback so a decision table never returns `null`?
A: Add a catch-all rule whose input cells are all hyphens (`-`, meaning "any") placed so it matches when nothing more specific does, returning a safe default like `reviewRequired = true`. With FIRST/PRIORITY ordering this acts as the final fallback; with UNIQUE you must ensure it does not overlap real rules. Defaulting to the conservative outcome (review required) is the prudent choice for a compliance gate.

Q: Why might you model the "compliance review required" logic as a DRD of several decisions rather than one table?
A: A Decision Requirements Diagram lets you compose sub-decisions — for example a "risk rating" decision feeding a "review required" decision — so each table stays small, single-purpose, and independently testable. One giant table with every input becomes unreadable and error-prone to audit. The DRD also documents which input data and which upstream decision each table depends on.

Q: How does a business rule task pass the order's data into the DMN decision in Camunda 8?
A: The decision reads process variables by the names its input expressions reference, so the variables present on the instance (e.g. `amount`, `investorType`, `domicile`) must match the table's input expressions, and you can use input mappings to shape them. The task's result variable then captures the decision output back onto the instance. A name mismatch between the variable and the input expression yields a `null` input and likely a wrong or empty decision.

Q: In Camunda 8, how do you keep a worker's local job variables from leaking into the global instance scope?
A: Configure the service task's output mapping to merge only the named results you want back, and/or have the worker complete with a scoped variable that an output mapping renames, rather than dumping its whole payload. Without an output mapping, all variables a worker returns are merged at the activity's scope. Disciplined output mappings are how a large fund process avoids accidental variable-name collisions across dozens of tasks.

Q: What is the difference between completing a job with variables at the local scope versus the propagated scope?
A: Variables a worker returns are applied at the task's scope and then propagated up to the process instance scope unless an output mapping confines them, so by default they become visible to the whole instance. To keep something local you set it within a sub-process scope or restrict it via mappings. Understanding this propagation is key to the recurring "a value I thought was local overwrote a shared variable" bug.

Q: Why might you prefer a non-interrupting timer boundary event plus an escalation over an interrupting one on a review task?
A: A non-interrupting timer (dashed border) lets you fire a reminder or escalation to a supervisor while the reviewer keeps working on the still-valid task, whereas an interrupting timer would cancel the review entirely. For a four-eyes NAV approval you usually want "nudge after 4 hours, hard-escalate after 24" without destroying the in-progress task. Reaching for interrupting here would throw away legitimate work and force a restart.

Q: How would you model a "remind, then escalate, then auto-reject" SLA on a single human task?
A: Attach two non-interrupting timer boundary events (e.g. `PT4H` reminder, `PT24H` escalate to supervisor) plus one interrupting timer (e.g. `PT48H`) that cancels the task and routes to an auto-reject path. The non-interrupting timers emit parallel tokens for side actions while the task survives; the interrupting one finally removes it. This layered pattern encodes a realistic operational deadline ladder in the model itself.

Q: What is the difference between an escalation event and an error event in throw/catch semantics?
A: An escalation can be thrown and caught non-interruptingly so the originating activity continues while a parallel handler runs, whereas a BPMN error is always interrupting and aborts the activity to its error handler. Escalation suits "alert a human but keep processing"; error suits "stop and divert". Picking error for a soft alert needlessly kills a still-valid branch of, say, an order in flight.

Q: A parallel gateway split never reaches its join and the instance hangs — what is the likely modelling cause?
A: A parallel join waits for a token on every incoming sequence flow, so if one branch ends early (e.g. routed to a separate end event or killed by an exclusive gateway) the join never receives its token and the instance deadlocks. The fix is to ensure every branch that the split created actually arrives at the join, or to use an inclusive gateway whose join only waits for activated branches. This "missing token at the AND-join" is the classic parallel-gateway deadlock.

Q: Why can mixing an exclusive split with a parallel join cause a stuck instance?
A: An exclusive split sends a single token down one path, but a parallel join expects a token on each of its incoming flows, so it waits forever for branches that were never activated. The gateways must be paired by type — XOR split with XOR merge, AND split with AND join — or the token arithmetic breaks. This is one of the most common BPMN soundness errors and is exactly what static model validators flag.

Q: What does it mean for a BPMN process to be "sound", and why care?
A: Soundness means every started instance can always reach a proper completion with no leftover tokens and no dead (unreachable) activities — i.e. no deadlocks and no livelocks. An unsound model can strand a fund order forever or leave orphaned tokens that never clean up. Architects validate soundness (via tooling or careful gateway pairing) before deploying because a stuck regulated process is an operational incident.

Q: How does an inclusive (OR) gateway join know how many branches to wait for?
A: An inclusive join evaluates which incoming branches still have tokens in flight that could arrive and waits only for those, synchronizing exactly the branches the matching inclusive split activated. This is more complex for the engine than an AND join but is what makes variable fan-out safe. It is the right join when "send to whichever parties are affected" produced an unknown number of active branches.

Q: When would you use a multi-instance sub-process over an inclusive gateway for fanning out work?
A: Use a multi-instance marker when you run the same activity once per element of a collection whose size is data-driven — e.g. validate each holding in a redemption — because the number of branches comes from a list at runtime, not from a fixed set of modelled paths. An inclusive gateway fans out across a fixed, modelled set of conditional flows. Multi-instance scales to "N items I do not know until runtime"; inclusive gateways do not.

Q: What is the difference between a parallel and a sequential multi-instance, and when does ordering force sequential?
A: A parallel multi-instance runs all iterations concurrently, while a sequential multi-instance runs them one after another, reusing one token. You choose sequential when iterations must not overlap — for example posting ledger entries that depend on the running balance — and parallel when they are independent like screening each investor. Picking parallel where order matters introduces race conditions on shared state.

Q: How do you stop a parallel multi-instance early once a condition is met?
A: Define a completion condition (a FEEL expression) on the multi-instance that, when true, cancels the remaining instances and proceeds — for example stop screening once any holding fails. Without it, all iterations must finish. This is how you model "reject the whole order as soon as one underlying check fails" efficiently rather than running every check pointlessly.

Q: What is an event sub-process and how does it differ from a boundary event?
A: An event sub-process sits inside a process or sub-process and is triggered by a start event (error, message, timer, escalation) without an incoming sequence flow, handling cross-cutting concerns from within the scope, whereas a boundary event hangs on the edge of one specific activity. An interrupting event sub-process cancels its enclosing scope; a non-interrupting one runs alongside. It is the right tool when a handler should cover the whole scope, not just a single task.

Q: When do you choose a call activity over an embedded sub-process for a shared AML screening routine?
A: Use a call activity when the routine is reused across many parent processes and must be independently deployed, versioned, and governed in one place — a single audited AML definition every flow calls. An embedded sub-process is inlined and cannot be shared. The call-activity choice trades the overhead of managing a separate definition for one-place-to-change governance of a shared control.

Q: When a parent calls a sub-process via a call activity, how do variables cross the boundary?
A: You declare explicit input mappings to pass parent variables into the called process and output mappings to bring results back, because the called instance has its own variable scope and does not automatically share the parent's. This explicit contract is a feature: the shared routine has a defined interface rather than depending on the parent's internal variable names. Forgetting the output mapping is why "the AML result never came back" happens.

Q: How does Operate help you find every order that took the manual-review path last week?
A: Operate lets you filter and inspect process instances by process, state, variables, and the elements (activities) they visited, so you can query instances that activated the review user task within a time window. It reads the history exported to Elasticsearch, not the live Zeebe log. For deeper aggregate analytics like average review duration you would use Optimize instead.

Q: What is the division of labour between Operate, Tasklist, and Optimize?
A: Operate is for operators to monitor and fix running/completed instances and incidents, Tasklist is for end users to claim and complete human tasks, and Optimize is for analysts to build dashboards and KPIs over historical process/decision data. All three read from the exported index rather than the hot engine. Knowing which tool answers which question avoids, say, trying to do bottleneck analysis in Operate.

Q: Why does Zeebe need an exporter at all, given it already has its event log?
A: Zeebe's internal log is optimized for fast sequential writes and processing, not for ad-hoc historical queries, so it deliberately does not serve rich queries or retain history indefinitely; the exporter streams records out to Elasticsearch/OpenSearch where Operate, Tasklist, and Optimize query them. This separation keeps the hot path fast. In SaaS Camunda runs the exporter for you; self-managed clusters must operate it.

Q: What is the failure consequence if the Zeebe exporter or its Elasticsearch falls behind?
A: Zeebe will not delete log segments that have not yet been exported, so a stalled or slow exporter causes the broker's disk usage to grow until it can hit a backpressure/disk threshold and reject new commands. The engine keeps running but monitoring data in Operate goes stale and ingestion may eventually be throttled. Watching exporter lag and Elasticsearch health is therefore an operational must, not optional.

Q: What is Zeebe backpressure and what does a client see when it triggers?
A: Backpressure is Zeebe rejecting new requests when a broker is overloaded (for example processing or exporting cannot keep up), returning a resource-exhausted style error so clients should retry with backoff rather than pile on. It protects cluster stability under load. A worker hitting backpressure should slow its job activation, and seeing it is a signal to add partitions or capacity.

Q: Why is the Zeebe partition count fixed at cluster creation, and what does that imply for capacity planning?
A: The number of partitions is set when the cluster is created and cannot be changed on a live cluster, because partitions are the unit of parallelism and re-sharding the replicated logs is not a casual online operation. You therefore size partitions up front for peak throughput with headroom. Under-provisioning means a disruptive migration later, so this is a genuine architectural decision, not a tunable knob.

Q: How does Zeebe decide which partition a given process instance lives on?
A: Each process instance is assigned to a single partition and all its state and processing stay there, with the gateway distributing new instances across partitions (e.g. round-robin) for load balancing. Keeping an instance on one partition is what lets Zeebe process it as an ordered, single-threaded stream without cross-partition coordination. It also means one hot instance cannot be split across partitions for more throughput.

Q: What does a Raft quorum loss do to a Zeebe partition, and how do you avoid it?
A: If a partition loses its quorum (a majority of its replicas), it cannot elect a leader or commit new records, so that partition stalls until enough replicas return, even though other partitions may keep working. You avoid it by running an odd replication factor (commonly 3) spread across failure domains so a single broker or zone loss never costs the majority. This is why replication factor and broker placement are first-class HA decisions.

Q: Summarise the decisive factor for picking Camunda over Temporal for a regulated investor-onboarding flow, in audit terms.
A: The decisive factor is that onboarding mixes automated checks with mandatory human approvals and must be signed off by compliance, and a BPMN diagram is an artifact a compliance officer can read, review, and attest to — Temporal code is not. The engine also records who completed each user task and when, producing the segregation-of-duties evidence auditors require. Temporal could run the logic but cannot deliver that business-readable, signable, engine-enforced control.

Q: How do you migrate long-running in-flight instances onto a newly deployed process version in Camunda 8?
A: You run an explicit process-instance migration that maps each active element of the old version to an element in the new version, because deploying a new version alone leaves existing instances on the version they started under. The mapping must account for elements that moved or were renamed, or the migration is rejected. This matters when a multi-day settlement must adopt a corrected model without being cancelled and restarted.

Q: Why is at-least-once job delivery, not exactly-once, the guarantee that shapes worker design, and how do you live with it?
A: Zeebe guarantees a job reaches a worker at least once because exactly-once delivery is impossible across a network with crashes and timeouts, so a worker can legitimately see the same job twice after a timeout or restart. You live with it by making side effects idempotent — dedup a SWIFT instruction on a unique reference, upsert a ledger row on an idempotency key — so a replay is a harmless no-op. Designing for replay rather than wishing for exactly-once is the core discipline of the decoupled job model.
