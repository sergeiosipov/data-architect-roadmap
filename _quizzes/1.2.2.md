# Phase 4 · 1.2.2 + 1.2.3 Micro-batch vs true streaming — 150 self-test questions

Q: What is the core processing-model difference between Spark Structured Streaming and Apache Flink?
A: Spark Structured Streaming is fundamentally micro-batch: it slices the incoming stream into many small batch jobs that run back-to-back. Flink is event-at-a-time (true streaming): each record flows through the operator graph and updates state as it arrives, with no batch boundary. This distinction drives every downstream difference in latency floor, state handling, and ops profile.

Q: What does "micro-batch" actually mean mechanically in Spark Structured Streaming?
A: The engine repeatedly plans and executes a small batch query over the data that arrived since the last batch, then commits, then starts the next batch. Each micro-batch reads new offsets, runs the same Spark SQL/DataFrame plan, writes output, and records progress in the checkpoint. So a "stream" is really a fast loop of tiny batch jobs over an unbounded source.

Q: What is a "trigger" in Spark Structured Streaming?
A: A trigger defines when a new micro-batch is launched — it is the cadence of the processing loop. You set it with `.trigger(...)` on the `DataStreamWriter`, choosing options like fixed `ProcessingTime`, the default trigger, `AvailableNow`, or experimental `Continuous`. The trigger choice is the single biggest lever over end-to-end latency in the micro-batch model.

Q: What is the default trigger when you do not specify one in Structured Streaming?
A: The default trigger runs micro-batches as fast as possible: as soon as one batch finishes, the next starts immediately with whatever data is available. There is no fixed interval, so throughput and latency are governed by how long each batch takes. This is distinct from `ProcessingTime`, which pins a fixed wall-clock interval between batch starts.

Q: How do you configure a fixed-interval micro-batch trigger in Structured Streaming?
A: Use `.trigger(Trigger.ProcessingTime("10 seconds"))` (Scala/Java) or `.trigger(processingTime="10 seconds")` (PySpark). The engine attempts to start a new micro-batch at each interval boundary. If a batch overruns the interval the next one starts immediately after, so the interval is a target, not a hard guarantee.

Q: What happens if a micro-batch takes longer than its configured `ProcessingTime` interval?
A: The next micro-batch does not wait for the next interval tick; it kicks off as soon as the slow batch finishes. The engine effectively skips the missed ticks and falls back to back-to-back execution until it catches up. This is a key gotcha: a too-tight interval on a heavy workload silently degrades to as-fast-as-possible behaviour.

Q: What is the `AvailableNow` trigger and when do you use it?
A: `Trigger.AvailableNow` processes all data currently available across potentially many micro-batches, then stops the query automatically. It is for batch-style incremental runs over a streaming source — for example a scheduled job that drains a Kafka topic or a directory and exits. It preserves streaming semantics (offsets, checkpoint, exactly-once) while behaving like a finite job.

Q: What is `Trigger.Once` and what is its current status?
A: `Trigger.Once` ran exactly one micro-batch over all available data and stopped. As of Spark 3.4 it is deprecated in favour of `Trigger.AvailableNow`, because `Once` tried to process everything in a single batch (risking memory blowups on large backlogs) whereas `AvailableNow` splits the backlog across multiple right-sized batches before stopping.

Q: What is "continuous processing" mode in Spark Structured Streaming?
A: Continuous processing is an experimental low-latency execution mode (introduced in Spark 2.3) that abandons the micro-batch loop and runs long-lived tasks that process records as they arrive, achieving roughly 1 ms end-to-end latency. You enable it with `.trigger(Trigger.Continuous("1 second"))`, where the interval is a checkpoint cadence, not a batch interval. It exists to push Spark closer to true-streaming latencies.

Q: In continuous processing, what does the interval passed to `Trigger.Continuous("1 second")` actually control?
A: It is the checkpoint interval — how often the continuous engine records query progress — not a micro-batch interval. The docs note these checkpoints stay format-compatible with the micro-batch engine, so a query can be restarted under any trigger. So `Continuous("1 second")` means "snapshot progress every second" while records still flow continuously between snapshots.

Q: What delivery guarantee does Spark continuous processing provide, and how does it differ from micro-batch?
A: Continuous processing provides only at-least-once fault tolerance, whereas the default micro-batch engine provides end-to-end exactly-once (given replayable sources and idempotent/transactional sinks). Trading exactly-once for at-least-once is the price of dropping the batch boundary that micro-batch uses to commit cleanly. This alone disqualifies continuous mode for use cases that cannot tolerate duplicates.

Q: What are the main limitations of Spark continuous processing mode?
A: It is experimental, supports only a restricted set of operations (essentially map-like projections, selections, and simple aggregation-free transformations), works with a limited set of sources/sinks (e.g. Kafka, rate, console/memory), and offers only at-least-once. Stateful operations like arbitrary aggregations and joins are not supported. Databricks explicitly does not recommend it, steering users to other low-latency paths instead.

Q: What is the approximate latency floor of Spark's default micro-batch model versus continuous processing?
A: Micro-batch realistically lands around 100 ms at best and more commonly hundreds of milliseconds to seconds, because every record must wait for a batch boundary plus planning and commit overhead. Continuous processing can reach about 1 ms but only at-least-once and for a narrow operation set. Flink's event-at-a-time model also reaches sub-millisecond-to-millisecond per-record latency without giving up exactly-once.

Q: Why does micro-batch impose a latency floor that event-at-a-time processing does not?
A: A record cannot produce output until the micro-batch that contains it is triggered, planned, executed, and committed — so even an instantaneously-arriving event waits for the batch cycle. That per-batch fixed cost (scheduling, query planning, offset/commit bookkeeping) is amortised over the batch but never goes to zero, setting a floor. Event-at-a-time engines have no batch boundary, so a record can be processed the moment it arrives.

Q: A teammate sets `Trigger.ProcessingTime("0 seconds")` expecting zero latency. What actually happens?
A: A zero interval just means "run as fast as possible" — equivalent to the default trigger — so batches run back-to-back, not instantaneously. You still pay the per-batch planning and commit overhead on every cycle, so latency is bounded by batch execution time, not driven to zero. To go below the micro-batch floor you would need continuous mode or a true-streaming engine like Flink.

Q: How does Spark Structured Streaming persist progress so it can recover after a failure?
A: It writes to a checkpoint location containing the offset log (which input offsets each batch read), the commit log (which batches completed), and the state store snapshots/deltas for stateful operators. On restart, Spark reads the last committed batch, replays uncommitted offsets, and restores state, giving exactly-once for replayable sources. Losing or moving the checkpoint directory breaks recovery and exactly-once guarantees.

Q: What is the write-ahead log (WAL) role in Spark Structured Streaming checkpointing?
A: Before processing a micro-batch, Spark records the range of offsets to be processed in the offset log (a form of WAL) so that on recovery it knows exactly which data each batch was responsible for. Combined with the commit log it can determine whether a batch finished, and re-run it deterministically if not. This offset-then-process-then-commit discipline is what makes micro-batch exactly-once achievable.

Q: What is the state store in Spark Structured Streaming, and what are the two backend options?
A: The state store holds intermediate state for stateful operations (aggregations, joins, deduplication, `mapGroupsWithState`). The default is the `HDFSBackedStateStore`, which keeps state in JVM memory and persists deltas/snapshots to the checkpoint (commonly on HDFS or object storage); the alternative is the RocksDB state store, enabled via `spark.sql.streaming.stateStore.providerClass`. RocksDB spills state off-heap to local disk, reducing GC pressure for very large state.

Q: When would you switch Spark's state store from the default to RocksDB?
A: Switch to RocksDB when state grows large enough that keeping it on the JVM heap causes long GC pauses or out-of-memory errors — for example wide keyspaces in streaming aggregations or large stream-stream joins. RocksDB stores state as an embedded on-disk key/value store, so heap usage stays bounded while state scales. The trade-off is some per-access disk/serialization overhead versus pure in-memory access.

Q: How does Flink manage state compared with Spark Structured Streaming?
A: Flink maintains state continuously inside each operator as records flow, with keyed state behaving like an embedded per-key key/value store partitioned alongside the stream. State updates are local operations (no distributed transaction), and Flink snapshots state via barrier-based checkpoints rather than per-batch commits. Spark instead rebuilds/updates state at each micro-batch boundary and persists it through the checkpoint mechanism.

Q: What is the difference between keyed state and operator state in Flink?
A: Keyed state is scoped to a key extracted from each record and is partitioned/distributed strictly with the stream, so a given key's state always lives where that key is processed — it behaves like a sharded key/value store. Operator state is scoped to a parallel operator instance regardless of key (e.g. a Kafka source tracking its partition offsets). Keyed state is the workhorse for aggregations and joins; operator state is mainly for source/sink bookkeeping.

Q: What are Flink's two main state backends and how do they differ?
A: `HashMapStateBackend` keeps working state as objects on the JVM heap — fast access but bounded by memory and subject to GC. `EmbeddedRocksDBStateBackend` stores state in an embedded RocksDB instance on local disk, allowing state far larger than memory and supporting incremental checkpoints, at the cost of serialization and disk access per operation. The choice mirrors Spark's heap vs RocksDB state-store decision.

Q: How does Flink checkpoint state, and what algorithm underlies it?
A: Flink uses a Chandy-Lamport-inspired distributed snapshot algorithm: special markers called stream barriers are injected at the sources and flow with the records. When an operator has seen barrier n on all its inputs, it snapshots its state asynchronously and forwards the barrier downstream. This gives a globally consistent cut of the streaming computation without stopping the flow of data.

Q: What are stream barriers in Flink and what invariant do they maintain?
A: Barriers are lightweight markers injected into the data flow at the sources that separate records belonging to one checkpoint from the next. The key invariant is that barriers never overtake records: everything before barrier n is in snapshot n, everything after is in snapshot n+1. This ordering is what lets Flink take a consistent snapshot of in-flight state.

Q: What is barrier alignment in Flink, and why can it add latency?
A: When an operator has multiple input streams, aligned checkpointing makes it wait until barrier n has arrived on every input before snapshotting, buffering records from faster inputs in the meantime. This guarantees exactly-once but the wait — the alignment phase — can add latency and backpressure when one input lags. Flink offers unaligned checkpointing to mitigate this on slow paths.

Q: What is unaligned checkpointing in Flink and what does it trade off?
A: Introduced in Flink 1.11, unaligned checkpointing lets a barrier overtake buffered in-flight records by making those records part of the checkpointed operator state, so the barrier propagates quickly without waiting for alignment. This speeds up checkpoints under backpressure but increases checkpoint size and I/O pressure on the state backend. It is a latency/throughput-vs-checkpoint-cost trade-off.

Q: In Flink, what is the difference between exactly-once and at-least-once checkpointing modes?
A: In exactly-once mode Flink aligns barriers so each record is reflected in state exactly once on recovery. In at-least-once mode it skips alignment, so an operator keeps processing all inputs after the first barrier; on restart some records from after the snapshot are replayed, appearing as duplicates. You choose at-least-once when you need lower checkpoint latency and your sink/logic tolerates duplicates.

Q: What is the difference between a checkpoint and a savepoint in Flink?
A: Checkpoints are automatic, periodic snapshots taken by the runtime for fault tolerance and are typically expired/overwritten as newer ones complete. Savepoints are manually triggered snapshots that use the same mechanism but persist indefinitely until you delete them, and are meant for planned operations like upgrades, rescaling, or migrating a job. Use savepoints to stop-and-resume a job across code or cluster changes without losing state.

Q: Why is per-record state update in Flink described as a "local" operation?
A: Because keyed state is co-partitioned with the stream, the state for a given key always resides on the same task that processes that key, so updating it requires no network round-trip or distributed transaction. This locality is what makes event-at-a-time stateful processing cheap enough to do on every record. Spark, by contrast, reconciles state at batch boundaries through the checkpoint.

Q: What is the typical operational profile of a Spark Structured Streaming deployment?
A: Spark streaming jobs usually run on a long-lived, shared Spark cluster (YARN, Kubernetes, or standalone) that also runs batch and SQL workloads, with the streaming query as one of many applications reusing that cluster. This means you amortise one cluster across batch and streaming and reuse existing Spark skills and tooling. The trade-off is coarser isolation and a latency floor inherited from the micro-batch model.

Q: What is the typical operational profile of a Flink deployment?
A: Flink commonly runs as per-job (application-mode) clusters where each streaming job gets its own JobManager and TaskManagers, giving strong isolation and independent scaling/upgrades per job. This isolation suits many independent low-latency pipelines but multiplies the number of clusters and the operational surface to manage. It contrasts with Spark's "one big shared cluster" reuse model.

Q: Why might an organisation already running a Spark cluster prefer to add a streaming job in Spark rather than adopt Flink?
A: Because they reuse the existing long-lived cluster, the existing Spark/SQL codebase and skills, and one set of monitoring and deployment tooling — no new engine to learn or operate. For minute-grained or second-grained use cases the micro-batch latency floor is irrelevant, so the simplicity wins. Adopting Flink would add operational complexity that those latency requirements do not justify.

Q: What is the "latency-class rubric" this lesson asks you to apply?
A: It is a decision aid that sorts a use case into a latency class — minutes, seconds, or sub-second — and maps each class to an engine: minutes-class fits batch or relaxed micro-batch (Spark), seconds-class fits micro-batch with tight triggers, and sub-second fits an event-at-a-time engine like Flink. The architect's job is to place each fund use case in the right class and name the engine. Mismatching the class either misses a latency floor or buys needless complexity.

Q: Which latency class is "nightly fund reconciliation" and which engine fits?
A: Nightly reconciliation is firmly in the minutes (indeed hours-cadence) class — it runs once per day and has no sub-second requirement. A batch job or a Spark micro-batch with `AvailableNow` is the right fit; reaching for Flink here adds per-job cluster complexity for nothing. The honest engineering answer is the simplest tool that meets the (very relaxed) latency need.

Q: Which latency class is a "sub-second exposure alert" and which engine fits?
A: A sub-second exposure or risk-limit alert is in the sub-second class, below the micro-batch latency floor, so it points to an event-at-a-time engine like Flink. Spark micro-batch cannot reliably meet a sub-second SLA because of the batch boundary plus planning/commit overhead. This is the canonical case where the latency floor forces the engine choice.

Q: Which latency class is "intraday fund-flow totals refreshed every minute" and which engine fits?
A: Minute-granular intraday flow totals are in the seconds-to-minutes class, comfortably above the micro-batch floor, so Spark Structured Streaming with a `ProcessingTime` trigger of, say, 30–60 seconds is a clean fit. Flink would also work but adds operational overhead the latency target does not require. Pick the engine that meets the SLA most simply.

Q: A new requirement says NAV-error detection must fire within 500 ms of a bad price arriving. Which model can meet it?
A: 500 ms is sub-second, below Spark micro-batch's realistic floor (~100 ms best case but typically higher once planning/commit and real workloads are included), so you should use an event-at-a-time engine such as Flink. Flink processes each price as it arrives and can evaluate the NAV-error rule immediately. Trying to hit 500 ms with tight micro-batch triggers is fragile and may breach the SLA under load.

Q: For a fund pipeline that recomputes share-class flow totals every minute, why would choosing Flink be over-engineering?
A: Because a one-minute target sits far above the micro-batch latency floor, Spark Structured Streaming on the existing shared cluster meets it trivially while reusing code, skills, and tooling. Flink would deliver latency you do not need at the cost of per-job clusters and a second engine to operate. The architect should name micro-batch here precisely to avoid that operational tax.

Q: For a sub-second pre-trade exposure check, why is micro-batch the wrong choice?
A: Sub-second sits below the micro-batch latency floor, so even an as-fast-as-possible Spark trigger cannot guarantee the SLA once you account for batch planning, execution, and commit per cycle. An event-at-a-time engine evaluates each position change immediately, with no batch boundary to wait on. Forcing micro-batch here means accepting SLA misses under load — a correctness-of-design failure.

Q: What does "end-to-end latency" mean when comparing these two engines, and why measure it rather than reason about it?
A: End-to-end latency is the wall-clock time from a record arriving at the source to its effect appearing in the sink/output, spanning ingestion, processing, state update, and write. You measure rather than assume because real planning overhead, trigger settings, state size, checkpoint cost, and sink behaviour shift the number well beyond textbook floors. The lesson's "Do" step explicitly has you measure both implementations.

Q: How would you actually measure end-to-end latency for a Spark vs Flink implementation of the same aggregation?
A: Stamp each source event with an ingestion timestamp, then at the sink compute the difference between the current time (or output commit time) and that stamp, and report the distribution (median, p95, p99) rather than a single number. Run both engines on the same input rate and the same aggregation so the comparison is apples-to-apples. Watching the tail percentiles, not the mean, is what exposes the micro-batch floor.

Q: When comparing "code volume" between rebuilding a Flink aggregation in Spark, what usually drives the difference?
A: Spark Structured Streaming often expresses windowed aggregations concisely in DataFrame/SQL with `groupBy(window(...))` and an output mode, whereas Flink may require more explicit wiring of watermarks, keyed process functions, and state. Conversely, advanced event-time and late-data control can be terser to express precisely in Flink. The half-page note should report measured code volume, not impressions, since it cuts both ways.

Q: When comparing "operational burden" between the two engines, what concrete factors belong in the note?
A: Number and lifecycle of clusters (shared long-lived vs per-job), checkpoint configuration and storage, upgrade/rescale procedure (savepoints in Flink), monitoring/alerting surface, and how recovery is performed and verified. You also weigh team familiarity and existing tooling. The point is to ground "ops burden" in observable, comparable facts rather than a vibe.

Q: Why can event-time windowing be done in both engines, yet the engines still differ in latency?
A: Both Spark and Flink support event-time windows, watermarks, and late-data handling, so functionally you can compute the same windowed result in either. The latency difference is about when a result is emitted relative to the triggering record: micro-batch emits at batch boundaries, event-at-a-time can emit as soon as the window fires. So equivalent semantics do not imply equivalent latency.

Q: What is a watermark, and is it specific to one of these engines?
A: A watermark is a moving threshold in event time asserting that no (or few) events older than it will still arrive, used to decide when an event-time window can be finalized and how late data is treated. Both Spark Structured Streaming and Flink implement watermarks; they are a streaming concept, not engine-specific. The micro-batch vs event-at-a-time difference affects emission timing, not the existence of watermarks.

Q: In a fund context, give an example where minute-class micro-batch is exactly right.
A: An intraday dashboard of cumulative subscription/redemption flow per share class, refreshed every minute for the dealing desk, has no sub-second need and fits a Spark `ProcessingTime("60 seconds")` trigger on the existing cluster. The relaxed SLA means you reuse infrastructure and keep ops simple. Naming micro-batch here is the architect demonstrating right-sized engineering.

Q: In a fund context, give an example where sub-second event-at-a-time is genuinely required.
A: A trading-adjacent intraday exposure or limit-breach monitor that must alert before the next order is placed needs sub-second reaction to each position change, which only an event-at-a-time engine like Flink reliably delivers. Here the latency floor of micro-batch would let breaches slip past the alert window. The architect names Flink because the SLA sits below the micro-batch floor.

Q: Why is most core fund-administration work (NAV strike, TA processing, EMT/EPT generation) NOT a sub-second streaming problem?
A: NAV is typically struck once per valuation point (often daily), transfer-agency dealing cuts off at set times, and EMT/EPT files are produced on a periodic schedule — none of these are continuous sub-second flows. They are batch or minute/second-class at most, so micro-batch or pure batch is the honest fit. Recognising this prevents over-engineering a streaming spine where a scheduled batch suffices.

Q: A streaming job built for "real-time NAV" turns out to only need end-of-day output. What does the latency-class rubric tell you?
A: It tells you the use case is minutes/daily-class, so a true-streaming engine is unjustified and even default micro-batch is heavier than needed — a scheduled batch or `AvailableNow` run is the right answer. The rubric guards against latency-class inflation driven by the word "real-time" rather than an actual SLA. Always pin the requirement to a number before naming the engine.

Q: What does exactly-once mean in Spark Structured Streaming, and what does it require from sources and sinks?
A: Exactly-once means each input record affects the output state once despite failures and retries, achieved via the offset/commit logs plus deterministic re-execution of uncommitted batches. It requires a replayable source (e.g. Kafka, file source) so offsets can be re-read, and an idempotent or transactional sink so re-written output does not duplicate. Without both, you fall back to at-least-once in practice.

Q: Why does the lesson pair the engine choice with a delivery-guarantee discussion?
A: Because latency and guarantee are linked levers: Spark's exactly-once lives in the micro-batch path, and its lowest-latency continuous mode only offers at-least-once, while Flink offers exactly-once via aligned checkpoints at some latency cost or at-least-once for lower latency. You cannot pick an engine/mode without stating which guarantee you keep. In regulated fund data, the guarantee often constrains the choice more than raw latency.

Q: For a regulated NAV-flow aggregation feeding accounting, which guarantee do you usually want, and what does that rule out?
A: You almost always want exactly-once so totals are neither double-counted nor dropped, which rules out Spark's continuous-processing mode (at-least-once) and Flink's at-least-once mode. That pushes you to micro-batch with a replayable source and idempotent sink, or Flink with aligned exactly-once checkpoints. The guarantee requirement, not latency, drives the decision here.

Q: What is the risk of using Spark continuous processing for a financial aggregation despite its low latency?
A: Its at-least-once guarantee means a failure/restart can replay records, double-counting amounts in an aggregation — unacceptable for monetary totals like fund flows or NAV components. It also supports only a narrow set of operations, so most real aggregations are not even expressible in it. For financial sums you keep exactly-once via micro-batch or Flink aligned checkpoints instead.

Q: How does the choice of state backend (heap vs RocksDB) interact with latency in either engine?
A: Heap-based state (`HDFSBackedStateStore` in Spark, `HashMapStateBackend` in Flink) gives the fastest per-access latency but risks GC pauses and OOM as state grows, which can spike tail latency. RocksDB-based state bounds memory and scales to huge state but adds serialization and disk-access cost per operation. So the backend choice is itself a latency-vs-scale trade-off, separate from the engine model.

Q: A Spark streaming job's p99 latency periodically spikes by seconds. What state-related cause should you check first?
A: Long JVM garbage-collection pauses from large heap-resident state in the `HDFSBackedStateStore`, which stall the whole executor and inflate the tail. Check GC logs and state-store size; if state is large, switch to the RocksDB state store to move state off-heap. The mean may look fine while p99 suffers, so always inspect the tail when chasing latency SLAs.

Q: Why does the lesson insist you "name the engine," not just the latency class?
A: Because the architect's deliverable is an actionable decision, and a latency class only becomes actionable when tied to a concrete engine (and mode/guarantee) the team will operate. Naming the engine forces you to confront ops profile, guarantee, and existing-cluster reuse, not just the abstract SLA. "Seconds-class" is an observation; "Spark micro-batch at 30s ProcessingTime, exactly-once" is a decision.

Q: What does "rebuild one Flink aggregation in Spark Structured Streaming on the existing cluster" teach you?
A: It forces a like-for-like comparison: you implement the same windowed aggregation in both engines, then measure latency, code volume, and ops burden directly rather than arguing from theory. Doing it on the existing Spark cluster also surfaces the real reuse benefit (no new cluster) versus Flink's per-job model. The exercise produces evidence for the half-page comparison note.

Q: What is the danger of defaulting all streaming work to Flink "because it's true streaming"?
A: You take on per-job cluster proliferation, a second engine's operational complexity, and barrier-alignment/checkpoint tuning for use cases whose SLAs micro-batch already meets — pure over-engineering. For minute and many second-class workloads, Spark on the existing cluster is simpler and cheaper. True streaming is a tool for sub-second needs, not a default.

Q: What is the danger of defaulting all streaming work to Spark micro-batch "because we already have it"?
A: For genuinely sub-second use cases the micro-batch latency floor means you silently miss SLAs no matter how you tune triggers, and continuous mode only buys latency by dropping to at-least-once and a narrow op set. The right move for sub-second is an event-at-a-time engine. Reusing what you have is only correct when the latency class allows it.

Q: How does Spark's micro-batch commit step contribute to its latency floor?
A: At the end of every micro-batch Spark must commit progress — writing the offset/commit log and finalizing sink output — before results are durably visible and the next batch starts. This commit, plus query planning at batch start, is fixed overhead per cycle that cannot be driven to zero. It is precisely this per-batch bookkeeping that an event-at-a-time engine avoids on the per-record path.

Q: In Flink, why does at-least-once mode have lower checkpoint latency than exactly-once mode?
A: At-least-once skips barrier alignment, so operators with multiple inputs do not pause to wait for barrier n on every input before snapshotting — they keep processing. Removing that alignment wait reduces the latency the checkpoint adds, at the cost of replaying some post-barrier records (duplicates) on recovery. You trade a duplicate-tolerant guarantee for faster, smoother checkpoints.

Q: What does "embarrassingly parallel" have to do with Flink's guarantees in at-least-once mode?
A: Dataflows of only map/flatMap/filter-style operators (no multi-input operators or reshuffles) effectively give exactly-once even in at-least-once mode, because there is no alignment point where duplicates could be introduced. Alignment — and thus the duplicate risk — only arises at operators with multiple inputs or after a keyBy/shuffle. So topology shape, not just mode, affects the realised guarantee.

Q: A Flink job's checkpoints keep timing out under heavy load. What mechanism is designed to help, and what's the trade-off?
A: Unaligned checkpointing (since Flink 1.11) lets barriers overtake buffered in-flight data by snapshotting that data as operator state, so checkpoints complete even under backpressure where alignment would stall. The trade-off is larger checkpoints and higher I/O pressure on the state backend. It is the go-to mitigation when aligned checkpoints cannot finish in time on slow/backpressured paths.

Q: What is the relationship between this lesson's micro-batch-vs-streaming choice and the Lambda-vs-Kappa debate?
A: Lambda/Kappa is about whether you maintain separate batch and speed layers or one replayable-log stream path; this lesson is one level down, choosing the engine for the streaming (speed) path once you have one. You can run Kappa on either Spark micro-batch or Flink; the latency class decides which. The two decisions are related but distinct: architecture shape versus engine/latency-class.

Q: Why might a single platform legitimately use both Spark and Flink?
A: Because different workloads fall in different latency classes: minute/second-class batch-and-stream reconciliation and reporting fit Spark on the shared cluster, while a specific sub-second alerting path fits Flink. Using each engine where its model is the right fit is sound architecture, not indecision. The architect names the engine per use case rather than forcing one engine everywhere.

Q: What does it mean that Spark continuous-processing checkpoints are "format-compatible with the micro-batch engine"?
A: It means a query checkpointed under continuous mode can be stopped and restarted under the micro-batch engine (or vice versa) because the progress format is shared. This gives an escape hatch: you can prototype in low-latency continuous mode and fall back to robust exactly-once micro-batch without rebuilding checkpoint state. It reflects that the trigger is a runtime choice over a common progress representation.

Q: What does the `console` sink versus a production sink tell you when benchmarking these engines?
A: The `console` (or `memory`) sink is convenient for inspecting output during development but does not exercise real commit/transaction overhead, so latency measured against it understates production end-to-end latency. Benchmark against the real sink you will use (e.g. Kafka, an Iceberg table) so the per-batch commit cost in Spark and the sink's exactly-once handshake in Flink are included. Otherwise your measured latency floor is artificially low.

Q: Why is throughput, not just latency, part of a fair micro-batch-vs-streaming comparison?
A: An engine can hit a low latency at trivial input rates but collapse under realistic load, so you must report sustained throughput at the target latency rather than latency in isolation. Micro-batch amortises fixed overhead better at high throughput, while event-at-a-time keeps per-record latency low but must keep up with the arrival rate. Reporting both latency percentiles and sustained records-per-second is what makes the comparison honest.

Q: How would you justify an engine mapping "with numbers, not vibes" for a fund use case?
A: State the SLA as a number (e.g. p99 ≤ 1 s), state the required guarantee (e.g. exactly-once for monetary totals), then show the engine/mode that meets both with measured end-to-end latency from your test. Add the ops cost (shared vs per-job cluster) as a concrete factor. The justification is a number-backed match of SLA + guarantee + ops to a named engine, not a preference.

Q: What is the first thing to check when a Spark Structured Streaming query is "running" but produces no output?
A: Check the trigger and the source: with the default or `ProcessingTime` trigger, output appears only when a micro-batch fires and the source actually has new offsets, so verify new data is arriving and offsets are advancing in the streaming progress logs. Also confirm the output mode (append vs update vs complete) and any watermark are not withholding results. No new offsets means no batch, means no output.

Q: A Structured Streaming aggregation never emits results in append mode. What is the likely cause?
A: Append output mode for an event-time aggregation only emits a window's result once the watermark has passed the window end, so if the watermark never advances (e.g. no event-time column or stale timestamps) results are never finalized. Either advance the watermark with real event-time data or use update/complete mode if appropriate. This is a classic append-plus-watermark gotcha, not an engine fault.

Q: After restarting a Spark streaming job, it reprocesses old data unexpectedly. What should you check first?
A: Check that the checkpoint location is the same as before and intact — if it changed or was cleared, Spark has no committed offsets and may reprocess from the source's earliest available offsets. The checkpoint's offset/commit logs are what define "where we left off." Pointing a restart at a fresh checkpoint dir is the usual cause of unexpected reprocessing.

Q: A Flink job fails and on restart you see duplicate aggregation results. What configuration likely caused it?
A: It was likely running in at-least-once checkpointing mode (or with an at-least-once sink), so post-barrier records were replayed as duplicates on recovery. Switch to exactly-once checkpointing with an exactly-once-capable sink (transactional/idempotent) if duplicates are unacceptable. For monetary fund aggregations, exactly-once is normally required.

Q: Why is changing parallelism (rescaling) easier to reason about in Flink than mid-stream in Spark?
A: Flink supports rescaling from a savepoint, redistributing keyed state across new parallelism using key groups, so you stop at a savepoint, restart at the new parallelism, and resume with correct state. Spark micro-batch can change some parallelism but state-store partitioning and shuffle assumptions make arbitrary mid-stream rescaling more constrained. Savepoints are Flink's explicit tool for this lifecycle operation.

Q: What is a key group in Flink and why does it matter for rescaling?
A: A key group is the atomic unit of keyed-state assignment; the number of key groups equals the job's maximum parallelism, and each parallel operator instance owns a contiguous range of key groups. On rescale, Flink reassigns whole key groups to new instances, which makes redistributing keyed state efficient and deterministic. Set max parallelism deliberately, because it bounds how far you can scale out later.

Q: Why does Spark's "one shared long-lived cluster" model affect noisy-neighbour risk for streaming jobs?
A: Because batch, SQL, and streaming applications share the same cluster resources, a heavy batch job can steal CPU/memory and inflate your streaming job's batch durations and latency. You mitigate with scheduler pools/queues and resource isolation, but the coupling is inherent to the reuse model. Flink's per-job clusters avoid this by giving each job its own resources, at the cost of more clusters.

Q: How does the operational burden of upgrading a job differ between the two engines?
A: In Flink you typically take a savepoint, deploy the new job version, and restore from the savepoint, getting an explicit, state-preserving upgrade path per job. In Spark you stop the query and restart against the same checkpoint, but compatibility of state across code changes can be more constrained, and the shared cluster may host many jobs. The note should compare these concrete upgrade procedures.

Q: If a use case needs seconds-class latency AND exactly-once on a replayable source, what is a clean Spark answer?
A: Use Spark Structured Streaming in default micro-batch with a `ProcessingTime` trigger sized to the SLA (e.g. a few seconds), a replayable source like Kafka, and an idempotent/transactional sink — this delivers seconds-class latency with exactly-once. You stay on the existing cluster and reuse the codebase. Continuous mode is unnecessary and would sacrifice exactly-once.

Q: Why is "real-time" a dangerous word in a fund-platform requirements conversation?
A: Because "real-time" is used loosely for anything from sub-second alerting to "fresh by end of day," and each maps to a wildly different latency class and engine. The architect must convert it to a number and a guarantee before designing, or risk building a Flink spine for a daily report or a micro-batch path for a true sub-second alert. Pin the SLA, then name the class and engine.

Q: How does the micro-batch interval interact with the cost of small files written to a sink like Iceberg?
A: A very tight trigger interval produces many tiny output commits, which in a table format like Iceberg means lots of small files and metadata churn, hurting read performance and requiring frequent compaction. A relaxed interval batches more data per commit, producing fewer, larger files. So trigger choice trades latency against downstream file/compaction overhead — a real ops consideration.

Q: When measuring code volume and ops burden, why is a "half-page note" the deliverable rather than a benchmark suite?
A: Because the goal is an architect-level decision artifact that captures the meaningful comparison (latency, code, ops) concisely enough to inform engine selection, not an exhaustive benchmark. The note forces you to distil measurements into a defensible recommendation per latency class. It is decision documentation, the same spirit as an ADR.

Q: Why might Flink's event-time and late-data handling feel more first-class than Spark's for complex streaming logic?
A: Flink was designed event-at-a-time around event-time semantics, exposing fine-grained control via watermarks, allowed lateness, side outputs for late events, and keyed process functions with timers. Spark supports event time and watermarks too but expresses them within the batch-oriented DataFrame model, which can be less granular for arbitrary per-event logic. For intricate late-data flows, Flink's primitives are often a better fit.

Q: What is a "side output" in Flink and how does it relate to late fund events?
A: A side output is a secondary stream from an operator used to route specific records — classically late events that arrive after their window plus allowed lateness — to a separate sink for inspection or correction. For fund flows, late price or order corrections can be diverted to a side output rather than silently dropped or wrongly aggregated. It is part of why Flink suits precise late-data handling.

Q: Does Spark Structured Streaming support handling late data, and how?
A: Yes — you declare a watermark with `withWatermark("eventTime", "10 minutes")`, and Spark accepts late records within that bound into their windows while it can drop or finalize those beyond it. The watermark governs how long state is retained for a window and when results are emitted in append mode. So late-data handling exists in Spark; it is expressed within the micro-batch/watermark model.

Q: Why can a too-aggressive watermark cause incorrect fund-flow totals in either engine?
A: A watermark that advances too fast declares lateness prematurely, so legitimately late price or order events are treated as too-late and excluded from their window, undercounting the flow total. Set the watermark/allowed-lateness to the real maximum out-of-orderness of the feed. This is a correctness gotcha shared by Spark and Flink, independent of the micro-batch-vs-streaming choice.

Q: What does the "Done when" criterion "explain why micro-batch has a latency floor that event-at-a-time does not" require you to articulate?
A: You must explain that in micro-batch a record waits for its batch to be triggered, planned, executed, and committed — fixed per-cycle overhead that bounds minimum latency above zero — whereas event-at-a-time processes each record immediately on arrival with no batch boundary. The floor is structural, not a tuning artifact. Stating this clearly is the test of understanding the core distinction.

Q: How would you place "EMT/EPT file generation for distributors" in the latency-class rubric?
A: EMT (European MiFID Template) and EPT (European PRIIPs Template) files are produced on a periodic schedule (e.g. monthly/quarterly or on data change), so this is firmly batch/minutes-class with no streaming need at all. A scheduled batch job or `AvailableNow` run is the fit; neither micro-batch streaming nor Flink is warranted. Recognising file-drop cadences as batch prevents streaming over-engineering.

Q: How would you place "SWIFT message processing for settlement" in the latency-class rubric?
A: Inbound SWIFT settlement messages arrive as a continuous-ish event flow that often needs prompt (seconds-class) processing for status updates and exception handling, fitting micro-batch streaming with a short trigger, or Flink if a specific path needs sub-second reaction. The exact class depends on the downstream SLA for acknowledgements/exceptions. Convert the operational SLA to a number, then pick the engine.

Q: How would you place "intraday position/exposure monitoring feeding a risk limit" in the latency-class rubric?
A: If the limit must be enforced before the next action (trade/order), it is sub-second-class and points to an event-at-a-time engine like Flink; if it is an informational refresh every minute, it is minutes-class and Spark micro-batch suffices. The same nominal feature can sit in different classes depending on whether it gates an action. Always tie the class to the consequence of latency.

Q: Why is the guarantee question often more binding than latency in regulated fund data?
A: Because mis-stating monetary aggregates (double-counted or dropped flows/NAV components) is a correctness and compliance failure, exactly-once is frequently non-negotiable, which immediately rules out at-least-once-only modes regardless of how fast they are. Latency targets in fund admin are often relaxed (minutes/seconds), so the guarantee, not speed, usually decides the engine/mode. The architect states the guarantee first.

Q: What is the difference between "trigger interval" and "checkpoint interval" in these engines?
A: In Spark micro-batch the trigger interval governs how often a batch runs and thus latency, while checkpointing happens per batch to persist progress; in Spark continuous mode the `Continuous("…")` interval is purely a checkpoint cadence, not a batch cadence. In Flink there is no trigger interval (event-at-a-time), and the checkpoint interval governs snapshot frequency for fault tolerance. Conflating these two intervals is a common source of confusion.

Q: How does increasing the Flink checkpoint interval affect latency and recovery?
A: A longer checkpoint interval reduces per-checkpoint overhead and can lower steady-state latency, but it increases the amount of work replayed (more records reprocessed from sources) after a failure, lengthening recovery and widening the at-least-once duplicate window if applicable. It is a recovery-cost-vs-runtime-overhead trade-off. Tune it to balance acceptable recovery time against checkpoint pressure.

Q: Why does processing-time-based output differ from event-time-based output, and which engine cares more?
A: Processing-time logic emits based on when records are handled (wall clock), so results depend on system timing and are non-deterministic under reprocessing; event-time logic emits based on timestamps in the data, so reprocessing yields the same result. Both engines support both, but event-at-a-time engines like Flink expose richer event-time controls. For reproducible fund aggregations you prefer event-time semantics.

Q: A Spark streaming job is stable at a 5-second trigger but you tighten it to 200 ms and throughput collapses. Why?
A: At 200 ms the fixed per-batch overhead (planning, scheduling, commit) is no longer amortised over enough data, so the engine spends most of its time on bookkeeping rather than useful work, and batches start overrunning the interval. This is the micro-batch floor manifesting as overhead-dominated batches. The fix is either a more relaxed interval or, for genuine sub-second needs, a different engine.

Q: What does "ops profile" mean as a comparison axis in this lesson?
A: It is the operational shape of running the engine: cluster lifecycle (shared long-lived vs per-job), checkpoint/state storage and tuning, upgrade/rescale/recovery procedures, monitoring surface, and team familiarity. The lesson contrasts Spark's long-lived-cluster reuse against Flink's per-job clusters as the headline ops difference. The architect weighs ops profile alongside latency and guarantee, not after them.

Q: Why is "reuse the existing Spark cluster" itself a legitimate architectural argument?
A: Because operating one engine and one cluster reduces toil, on-call surface, tooling sprawl, and hiring/skill needs — real costs that a second engine multiplies. When the latency class permits Spark, reuse is often the cheapest correct answer over the platform's lifetime. The architect counts operational simplicity as a first-class factor, not an afterthought.

Q: When is Flink's added operational complexity clearly worth it?
A: When the use case is genuinely sub-second and/or needs rich per-event event-time logic (timers, side outputs, fine-grained state) that micro-batch cannot meet, the operational cost of per-job Flink clusters buys a capability Spark cannot provide. Here the complexity is justified by an SLA or semantic need, not by preference. The test is whether the latency class or logic actually requires event-at-a-time.

Q: How do you avoid building a sub-second Flink path that the business never actually needed?
A: Insist that every "real-time/sub-second" requirement come with a measured SLA and a stated consequence-of-latency before you commit to an event-at-a-time engine; if the honest SLA is minutes or seconds, place it in that class and use Spark. The discipline is converting adjectives to numbers and matching them to the simplest engine that meets them. This is exactly the latency-class rubric in action.

Q: Summarise the one-sentence decision rule this lesson hands the architect.
A: Place each use case in a latency class (minutes, seconds, sub-second) and required guarantee (exactly-once vs at-least-once), then name the simplest engine/mode that meets both — Spark micro-batch on the shared cluster for minutes/seconds-class exactly-once, and Flink event-at-a-time for sub-second or rich per-event needs. Justify with measured latency, code, and ops, not vibes. The latency floor of micro-batch is the structural reason sub-second forces event-at-a-time.

Q: Why does the lesson combine 1.2.2 (micro-batch) and 1.2.3 (true streaming) into one block?
A: Because they are two ends of the same axis — micro-batch versus event-at-a-time — and you only understand each by contrast with the other. The architect's real deliverable is the comparison and the resulting placement rule, not either model in isolation. Studying them together yields the latency-class rubric that drives engine selection.

Q: What is the exact class name you set `spark.sql.streaming.stateStore.providerClass` to in order to enable the RocksDB state store?
A: You set it to `org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider`, which swaps the default heap-backed provider for the embedded RocksDB instance per partition. This is a SparkSession/SQL config applied when the query starts, and it cannot be changed for an existing query without a fresh checkpoint. Getting the fully-qualified class name wrong (a common typo) silently leaves you on the default `HDFSBackedStateStoreProvider`.

Q: What does RocksDB "changelog checkpointing" do for a Spark stateful streaming query and why does it lower latency?
A: Changelog checkpointing writes only the records that changed since the last checkpoint to durable storage instead of snapshotting and uploading full state files each time, so the per-batch checkpoint cost drops and end-to-end latency improves. The full snapshots are then reconstructed in the background from the changelog plus older snapshots. It is recommended for all RocksDB stateful queries on supported runtimes, and it matters most when state is large and the trigger is tight.

Q: A Spark streaming job starting from `earliest` on a Kafka topic with billions of backlogged messages tries to read everything in the first batch. Which option fixes this?
A: Set `maxOffsetsPerTrigger` to cap the total Kafka offsets consumed per micro-batch, so the engine drains the backlog over many right-sized batches instead of one giant batch that OOMs. The cap is distributed across partitions proportionally to each partition's current lag, so busier partitions get a larger slice. The file-source equivalent is `maxFilesPerTrigger`.

Q: Why do `maxOffsetsPerTrigger` and `maxFilesPerTrigger` have no effect under `Trigger.AvailableNow`?
A: `AvailableNow` (and the deprecated `Once`) ignore those rate-limit options because they are designed to process all currently-available data before stopping, splitting it into batches by their own internal sizing rather than your per-trigger cap. Those options only apply in continuous stream mode with the default or `ProcessingTime` triggers. If you need bounded batch sizes during a backlog drain, run a normal streaming trigger rather than `AvailableNow`.

Q: Which two `StreamingQueryProgress` fields do you compare to detect that a Spark micro-batch job is falling behind, and what ratio signals trouble?
A: Compare `inputRowsPerSecond` (rate of arrival across sources) against `processedRowsPerSecond` (rate Spark is consuming); when input persistently exceeds processed, the job is falling behind and backlog grows. A common rule of thumb flags backpressure when the input-to-processed ratio exceeds roughly `1.2` for a sustained period. You read these via `query.lastProgress` or a `StreamingQueryListener`'s `onQueryProgress` callback.

Q: How do you programmatically subscribe to per-batch metrics in Spark Structured Streaming without polling?
A: Register a `StreamingQueryListener` on the SparkSession and override `onQueryStarted`, `onQueryProgress`, and `onQueryTerminated`; the `onQueryProgress` event delivers a `StreamingQueryProgress` each batch with `batchDuration`, `inputRowsPerSecond`, `processedRowsPerSecond`, and per-source/sink details. This pushes metrics to you as batches complete rather than requiring you to read `lastProgress` on a timer. It is the hook you wire into your monitoring/alerting for SLA breaches.

Q: A monitoring rule fires when `batchDuration` exceeds 80% of the trigger interval. What is it warning you about?
A: It warns that micro-batches are nearly taking as long as the interval between them, so the job is close to the point where batches start overrunning and the `ProcessingTime` trigger degrades to back-to-back execution. Once `batchDuration` exceeds the interval, latency stops tracking the trigger and grows with the backlog. Catching it at 80% gives you headroom to relax the trigger, scale out, or optimise before the job falls behind.

Q: In Flink, which retention setting keeps an externalized checkpoint after you cancel a job, and what is the catch?
A: Set externalized-checkpoint retention to `RETAIN_ON_CANCELLATION` (versus `DELETE_ON_CANCELLATION`), which preserves the checkpoint in `FAILED`, `SUSPENDED`, and `CANCELED` states so you can restore from it later. The catch is that you must clean these checkpoints up manually, and a job reaching `FINISHED` state still has its externalized checkpoints removed automatically. Production jobs typically use `RETAIN_ON_CANCELLATION` so a cancel does not destroy recoverable state.

Q: Where does Flink write externalized/retained checkpoints, and how does that differ from where you point a restore?
A: Externalized checkpoints are written under the directory configured by `state.checkpoints.dir`; to restore you point the new job at the specific checkpoint subdirectory (the metadata path), not the parent. This mirrors savepoint restore but uses the checkpoint directory rather than the savepoint directory. Confusing the parent directory with the individual checkpoint path is a common restore failure.

Q: You must restart a Flink job from a retained checkpoint after a crash. What command-level mechanism do you use?
A: Submit the job with the checkpoint path supplied as the restore point, the same way you pass a savepoint — for example `flink run -s <checkpoint-or-savepoint-path> ...` (or the equivalent on the application-mode deployment). Flink then rebuilds keyed and operator state from that snapshot before processing resumes. The job graph must be compatible (matching operator UIDs) or state cannot be mapped back.

Q: Why must you assign stable operator UIDs in a Flink job before you rely on savepoint/checkpoint restore across code changes?
A: Flink maps saved state back to operators by their UID, so if you change the topology and Flink auto-generates new UIDs, the restore cannot match old state to the new operators and either fails or silently drops state. Calling `.uid("...")` on stateful operators pins the identity so state survives code edits and rescaling. Omitting UIDs is a classic cause of "state could not be mapped" restore errors after a refactor.

Q: A Flink savepoint restore fails with a "could not map state" / allowNonRestoredState error after you removed an operator. What flag addresses it and what is the risk?
A: Restore with `--allowNonRestoredState` (the `-n` flag on `flink run`) so Flink ignores saved state that no longer has a matching operator instead of aborting. The risk is masking a genuine mistake: if the unmatched state was actually still needed, you silently lose it, so use the flag only when you intentionally dropped that operator. Always confirm which operator's state is being discarded before enabling it.

Q: How does `spark.sql.shuffle.partitions` specifically affect a stateful streaming query, and why can you not change it freely later?
A: For stateful operators the number of shuffle partitions fixes the number of state-store partitions, and that partitioning is baked into the checkpoint; changing `spark.sql.shuffle.partitions` after the query has run invalidates the existing state layout. So you must choose it deliberately at first start and keep it stable for the life of the checkpoint. This is the Spark analogue of Flink's max-parallelism/key-group constraint on rescaling.

Q: In Spark Structured Streaming, what does `outputMode("complete")` do to state, and why is it dangerous for unbounded keys?
A: Complete mode re-outputs the entire result table every batch and therefore keeps state for every group indefinitely, since any past key could still be emitted again. With an unbounded or high-cardinality key space (e.g. per-order or per-instrument keys) state grows without bound and the job eventually OOMs or stalls on GC. Use append mode with a watermark, or update mode, when keys are unbounded so state can be evicted.

Q: When is `outputMode("update")` the right choice over append for a fund-flow aggregation, and what does it emit?
A: Update mode emits only the rows whose aggregate value changed in this batch, so a per-share-class running total can be pushed downstream as soon as it moves, without waiting for the watermark to finalize a window like append requires. Choose it when the sink can handle upserts/overwrites of keys and you want fresher partial results. Append, by contrast, emits each window's value once and only after the watermark passes the window end.

Q: How does Spark decide it can drop state for an event-time window, and which configuration controls it?
A: Spark drops a window's state once the watermark — set by `withWatermark("eventTime", "<delay>")` — advances past the window's end plus the allowed lateness implied by that delay, because no further accepted events can fall into it. The watermark delay is therefore both a late-data tolerance and a state-retention bound. Too long a delay bloats state; too short drops legitimately late events and undercounts.

Q: In Flink, which API call sets how late events may still be accepted into a window after the watermark, and what happens to events later than that?
A: `allowedLateness(Time.minutes(n))` on a windowed stream lets events arriving up to `n` after the watermark still update and re-fire the window; events later than that are dropped unless you capture them via a side output. Without `allowedLateness`, an event past the watermark is dropped immediately. This is Flink's explicit knob separating watermark progress from final state eviction.

Q: How do you capture Flink's dropped late events instead of losing them, and why does this matter for fund data?
A: Define an `OutputTag` and call `.sideOutputLateData(tag)` on the windowed stream, then read the late stream via `getSideOutput(tag)` to route those records to a correction sink or alert. For fund flows, a late price correction or back-dated order must not be silently dropped from a NAV-flow total; the side output gives you an auditable path to reconcile it. Spark has no direct per-record side-output equivalent for late data.

Q: A Spark append-mode aggregation emits results far later than expected even though data is flowing. What watermark-related cause is most likely?
A: The watermark is advancing slowly or not at all because the event-time column has stale, future-dated, or wrongly-parsed timestamps, so the window-end-plus-delay threshold is never crossed and append withholds the result. Check that the timestamp column is real event time, correctly typed, and that the slowest source partition is advancing it. Mixing in a record with a far-future timestamp can also stall the watermark for everything behind it.

Q: Why can a single straggler partition with an old or absent timestamp freeze the global watermark in both engines?
A: The global watermark is the minimum across input partitions/sources, so one partition that stops advancing event time (idle, or emitting old timestamps) holds the whole watermark back and prevents windows from firing. Flink mitigates this with idleness detection (`withIdleness(...)`) that excludes quiet partitions from the watermark calculation. Spark users typically must ensure every partition produces fresh event-time data or the aggregation stalls.

Q: What does Flink's `withIdleness(Duration)` on a watermark strategy do, and when do you need it?
A: It marks a source partition as idle if it produces no records for the given duration so that partition stops holding back the global watermark, letting downstream windows fire from the active partitions. You need it when some Kafka partitions are sparsely populated (e.g. a low-volume share class) and would otherwise stall event-time progress. Without it, an idle partition can indefinitely block window emission across the whole job.

Q: Compare how Spark and Flink express a 1-minute tumbling event-time sum of fund flows at the API level.
A: In Spark you write `df.withWatermark("ts","2 minutes").groupBy(window(col("ts"),"1 minute"), col("shareClass")).sum("amount")` with an output mode — concise DataFrame/SQL. In Flink you assign a watermark strategy, `keyBy(shareClass)`, apply `window(TumblingEventTimeWindows.of(Time.minutes(1)))`, and supply a reduce/aggregate function — more explicit wiring. Same semantics, different verbosity, which is exactly the "code volume" axis the half-page note measures.

Q: What is a sliding (hopping) window versus a tumbling window, and give a fund example of each?
A: A tumbling window partitions time into fixed non-overlapping intervals (e.g. flow total per clock-minute), so each event belongs to exactly one window; a sliding window of size S advancing by step T < S overlaps, so each event can fall into multiple windows (e.g. a 5-minute trailing exposure recomputed every 1 minute). Sliding windows produce more output and hold more concurrent state. Pick tumbling for clean periodic totals and sliding for moving/rolling metrics.

Q: What is a session window and when would it appear in a fund-platform stream?
A: A session window groups events separated by gaps smaller than a configured inactivity timeout, so the window length is data-driven rather than fixed — it closes after a quiet period. In a fund platform it could group a burst of dealing activity or a user's order-entry session, closing when activity pauses. Both Flink and Spark support session windows; they need a gap/timeout parameter rather than a fixed size.

Q: How does Flink's `ProcessFunction` with timers let you express logic that is awkward in Spark micro-batch?
A: A `KeyedProcessFunction` can register event-time or processing-time timers per key (`ctx.timerService().registerEventTimeTimer(...)`) and run arbitrary `onTimer` logic, enabling precise per-key timeouts, debouncing, or delayed firing that micro-batch's window/aggregation model does not express directly. For example, "alert if no confirmation message for an order within N seconds" is natural with a per-key timer. This fine-grained event-time control is part of why Flink suits intricate per-event logic.

Q: In Spark, what is `flatMapGroupsWithState` / `mapGroupsWithState`, and what must you configure for it not to leak state?
A: They are arbitrary stateful operators letting you maintain custom per-key state across batches in Structured Streaming, with full control over update and output. You must set a state timeout (`GroupStateTimeout.EventTimeTimeout` with a watermark, or `ProcessingTimeTimeout`) and remove state in your function, or per-key state accumulates forever for keys that go silent. They are Spark's closest analogue to Flink's keyed process functions.

Q: A stream-stream join in Spark keeps growing state until the job dies. What design element prevents unbounded join state?
A: Stream-stream joins require watermarks on both sides plus an event-time range condition (an interval/time-bounded join predicate) so Spark knows when matching rows can no longer arrive and can evict buffered rows. Without a watermark and time constraint, Spark must buffer all rows from both sides indefinitely to remain correct. The watermark-plus-time-bound is what makes the join state bounded.

Q: What is the equivalent bounded-join concept in Flink, and what is it called?
A: Flink calls it an interval join: each record from one stream joins records from the other within a relative time bound (e.g. `between(-5 minutes, +5 minutes)`), so state for a record is dropped once that interval has passed the watermark. This bounds join state the same way Spark's watermarked time-range join does. It is the standard pattern for joining, say, orders to price ticks within a tolerance window.

Q: Why does a stream-stream join's watermark configuration directly trade off completeness against state size?
A: A wider join-window/watermark delay accepts more out-of-order or late matches (higher completeness) but forces both engines to retain buffered rows longer, increasing state size and checkpoint cost; a tighter bound evicts sooner but risks dropping valid late matches. So join correctness and resource use are tuned by the same delay parameter. You set it from the real maximum skew between the two feeds, not arbitrarily.

Q: How does Flink's incremental checkpointing (with the RocksDB backend) differ from a full snapshot, and what does it cost?
A: Incremental checkpoints upload only the RocksDB SST files that changed since the last checkpoint rather than the entire state, drastically shrinking per-checkpoint I/O for large slowly-changing state. The cost is that restore must reassemble state from a chain of incremental deltas, which can make recovery slower and complicates checkpoint cleanup. Only the `EmbeddedRocksDBStateBackend` supports incremental checkpoints, not the heap backend.

Q: Why is `EmbeddedRocksDBStateBackend` the only Flink backend that supports incremental checkpoints?
A: Because RocksDB already organises state as immutable on-disk SST files via log-structured merge, Flink can identify and ship just the newly-created/changed SST files since the last checkpoint, which is the unit of "increment." The heap-based `HashMapStateBackend` holds state as live Java objects with no such file-level delta structure, so it can only take full snapshots. This is a concrete reason to pick RocksDB for very large state even beyond memory limits.

Q: A Flink job's checkpoints succeed but recovery after a crash is very slow. What incremental-checkpoint trade-off is the likely cause?
A: With incremental checkpoints the running checkpoints are cheap, but on restart Flink must download and replay a long chain of incremental SST deltas to rebuild state, so recovery time grows with how much has accumulated since the last full materialization. Periodic compaction/consolidation and tuning checkpoint frequency bound this chain. It is the inverse trade-off of full snapshots, which are costly to write but quick to restore.

Q: How does the `console` sink's exactly-once behaviour differ from a real sink, and why does that matter for a benchmark?
A: The `console`/`memory` sink does not implement transactional commit or idempotent writes, so it neither exercises real exactly-once handshake cost nor tells you anything about production durability; latency measured against it is artificially low. A real sink like a Kafka topic or an Iceberg table incurs commit/transaction overhead each batch. Benchmark against the actual sink so the measured end-to-end latency includes that cost.

Q: What is the `foreachBatch` sink in Spark Structured Streaming and when do you reach for it?
A: `foreachBatch` gives you the micro-batch output as a regular DataFrame plus a batch id, so you can write it with any batch sink, do multi-table writes, or implement custom idempotent upserts keyed on the batch id. You reach for it when no built-in streaming sink fits — e.g. merging into a Delta/Iceberg table or writing to a JDBC store with exactly-once via the batch id. It bridges streaming output back into the batch write APIs.

Q: How can the `batchId` provided by `foreachBatch` be used to achieve idempotent writes to a non-transactional sink?
A: Because `batchId` is monotonically increasing and stable across retries of the same batch, you can record the last-committed `batchId` in the sink (or a side table) and skip or overwrite re-deliveries of an already-committed batch, making the write effectively exactly-once. On retry after failure Spark re-runs the same `batchId`, so deduplicating on it prevents double-writes. This is the standard pattern for exactly-once into sinks that lack native transactions.

Q: Why does a tight micro-batch trigger writing to Iceberg create a small-files problem, and what operation reverses it?
A: Each micro-batch commit produces at least one data file per partition, so a sub-second trigger generates many tiny files plus metadata churn, degrading read planning and scan performance. The reverse is a compaction/rewrite operation (Iceberg `rewrite_data_files`) that coalesces small files into fewer larger ones. Relaxing the trigger or batching writes reduces how often compaction must run.

Q: How would you size a `ProcessingTime` trigger to balance freshness against the Iceberg small-files cost for a minute-class fund dashboard?
A: Pick the largest trigger interval that still meets the freshness SLA — for a one-minute dashboard a `ProcessingTime("60 seconds")` trigger writes one set of files per minute rather than thousands of tiny commits, keeping file counts manageable while meeting the SLA. The principle is "as relaxed as the SLA allows" so each commit carries more data. Then schedule periodic compaction to clean up whatever small files remain.

Q: When draining a large historical backlog into Iceberg, why is `Trigger.AvailableNow` preferable to a fast `ProcessingTime` trigger?
A: `AvailableNow` splits the whole backlog into multiple right-sized batches, processes them with exactly-once semantics, and stops, avoiding both the single-giant-batch OOM of `Once` and the thousands-of-tiny-commits problem of a fast continuous trigger. It behaves like a finite incremental batch job over the streaming source. You then switch to a normal streaming trigger for the steady-state tail.

Q: Two engineers disagree on whether a "real-time NAV deviation alert within 2 seconds" needs Flink. How does the rubric settle it?
A: A 2-second SLA is seconds-class, comfortably above the micro-batch floor, so Spark Structured Streaming with a `ProcessingTime` trigger of around one second and a replayable source meets it on the existing cluster — Flink is not required. The rubric settles it by converting the adjective "real-time" to the number 2 s and matching the simplest engine. Flink would only be forced if the SLA dropped below the micro-batch floor.

Q: For a settlement-status update that must reflect an inbound SWIFT message within 3 seconds, which engine/mode and guarantee do you specify?
A: Three seconds is seconds-class, so specify Spark Structured Streaming in default micro-batch with a `ProcessingTime` trigger of ~1 second, a replayable source, and an idempotent/transactional sink to keep exactly-once — status must not be double-applied or lost. The guarantee (exactly-once) is as load-bearing as the latency here because a duplicated settlement status is a correctness defect. Flink is unnecessary for a 3-second target.

Q: A pre-trade limit check must block an order in under 200 ms. Why does even Spark continuous mode not rescue micro-batch here?
A: Two hundred ms is sub-second, and while continuous processing can reach ~1 ms it only provides at-least-once and supports a narrow op set, so a stateful limit check that must be correct and fast is not safely expressible in it. The honest answer is an event-at-a-time engine like Flink that evaluates each position change immediately with exactly-once. Continuous mode trades away exactly the guarantee a limit check needs.

Q: Why is "exactly-once" in Flink really "effectively-once" at the sink, and what does the sink have to provide?
A: Flink guarantees its internal state is updated exactly once per record on recovery, but end-to-end exactly-once also requires the sink to either be idempotent or participate in a two-phase commit so replayed output after a restart is not duplicated externally. A Kafka sink uses transactions; a file/table sink commits atomically tied to the checkpoint. Without a cooperating sink you get exactly-once state but at-least-once externally-visible output.

Q: What is the two-phase commit (2PC) sink protocol in Flink and how does it tie to checkpoints?
A: A 2PC sink pre-commits (writes but does not finalize) output as part of each checkpoint's pre-commit phase, then finalizes (commits) only after the checkpoint completes successfully across all operators; on failure the un-finalized output is rolled back. This binds external commit to checkpoint success, delivering end-to-end exactly-once. The Kafka transactional sink and `TwoPhaseCommitSinkFunction`-based sinks implement this handshake.

Q: When a Kafka transactional sink underpins exactly-once, why does the transaction timeout interact with the Flink checkpoint interval?
A: The Kafka producer's `transaction.timeout.ms` must exceed the maximum time a transaction stays open — roughly the checkpoint interval plus recovery time — or Kafka aborts the transaction and the pending records are lost rather than committed. So a long checkpoint interval or slow recovery requires a correspondingly large transaction timeout. Misconfiguring this is a classic cause of silent data loss in exactly-once Flink-to-Kafka pipelines.

Q: How do you measure end-to-end latency fairly when one engine commits per batch and the other commits per checkpoint?
A: Measure from the source ingestion timestamp stamped on each event to the moment the record's effect is durably visible at the same real sink for both engines, and report the full distribution (median, p95, p99) at an identical input rate and aggregation. This captures Spark's per-batch commit and Flink's checkpoint/2PC cost in the same currency. Comparing against different sinks or only the mean produces a misleading result.

Q: Why must a fair Spark-vs-Flink comparison fix the input rate and the aggregation, not just the latency target?
A: Because latency depends on load and operation complexity — an engine can look fast at a trivial rate and collapse at a realistic one, and a simple projection hides costs that a windowed join exposes. Holding input rate and the exact aggregation constant makes the only variable the engine model, so the measured latency and throughput differences are attributable to micro-batch vs event-at-a-time. Otherwise you are comparing two different workloads, not two engines.

Q: Summarise, as a checklist, what the half-page note must contain to be a defensible engine-selection artifact.
A: It must state the SLA as a number and the required guarantee, report measured p50/p95/p99 end-to-end latency and sustained throughput for both engines against the same real sink and aggregation, quantify code volume, and lay out ops burden (cluster lifecycle, checkpoint/state config, upgrade/rescale/recovery procedure), ending in a named engine/mode per latency class. It reads like an ADR: decision, alternatives, evidence, and the fund/EU constraint that drove it. Numbers and a named engine, not vibes.

Q: How does Flink's barrier-alignment latency cost scale with the skew between an operator's input streams, and why is that a fund-feed concern?
A: Alignment makes an operator buffer records from faster inputs until barrier `n` arrives on the slowest input, so the added latency grows roughly with the time-skew between inputs — a badly-lagging input stalls the checkpoint and backpressures the fast ones. If you join a fast price feed against a slow order feed, the slow feed's lag inflates checkpoint latency for the whole join. Unaligned checkpointing or rebalancing the slow source is the mitigation.

Q: For a nightly reconciliation that drains a Kafka topic and exits, what is the single best Spark trigger and why?
A: Use `Trigger.AvailableNow`, which processes all currently-available offsets across multiple right-sized micro-batches with exactly-once semantics and then stops the query — exactly the shape of a scheduled drain-and-exit job. It avoids both the OOM risk of the deprecated `Once` (one giant batch) and the never-stopping behaviour of a continuous trigger. The reconciliation is minutes/daily-class, so no streaming-resident job or Flink is warranted.

Q: A Flink job restarted from a savepoint but throughput is near zero and lag keeps climbing. What ordering-of-operations mistake should you check first?
A: Check that you actually restored from the savepoint and that the new parallelism does not exceed the job's configured max parallelism (key groups) — if max parallelism is too low, the rescaled job cannot spread keyed state across the new instances and chokes. Also confirm operator UIDs match so state mapped back rather than starting cold and rebuilding. A cold restart with no restored state plus a large source backlog looks exactly like climbing lag at near-zero useful throughput.
