# Phase 4 · 2.2.2 Stream connectors (Kafka Connect) — 150 self-test questions

Q: What is Kafka Connect, in one sentence?
A: Kafka Connect is a framework and runtime for streaming data between Apache Kafka and external systems using reusable, declarative connectors instead of bespoke producer/consumer code. It runs as its own cluster of worker processes that you configure via REST, so integrations like a CDC source or an object-store sink become configuration rather than application code. This makes it the operational workhorse for most CDC and sink plumbing in a fund estate.

Q: What is a Connect "worker" and what role does it play?
A: A worker is a single JVM process running the Kafka Connect runtime; it is the unit of scaling and fault tolerance. Workers host connectors and tasks, coordinate via Kafka's group protocol in distributed mode, and expose the REST API. You add throughput and resilience by adding workers, and a worker dying triggers a rebalance that reassigns its tasks elsewhere.

Q: What is a "task" in Kafka Connect and how does it relate to a connector?
A: A connector is the logical integration (configuration plus a plan for splitting work), while a task is the actual unit that copies data and runs on a worker. One connector produces one `Connector` instance plus N `Task` instances; the tasks are what consume or produce records in parallel. Tasks are the thing that gets spread across workers, so they are the real parallelism knob.

Q: What is the difference between standalone mode and distributed mode?
A: Standalone mode runs a single worker process storing offsets in a local file, suitable for development or a single-machine source like log tailing. Distributed mode runs multiple coordinating workers that store config, offsets, and status in Kafka topics, giving you scaling, automatic rebalancing, and fault tolerance. The Phase 4 lab and any production fund pipeline should use distributed mode.

Q: Why must a production Connect deployment use distributed mode rather than standalone?
A: Distributed mode survives worker failure by rebalancing tasks onto surviving workers and persists all state in replicated Kafka topics, so no single machine is a point of failure. Standalone keeps offsets in a local file that is lost if the host dies, and it cannot scale horizontally. For a regulated NAV or transfer-agency feed you need the recovery and scale that only distributed mode provides.

Q: Which three internal Kafka topics does a distributed Connect cluster use, and what does each store?
A: `config.storage.topic` stores connector and task configurations, `offset.storage.topic` stores source-connector progress offsets, and `status.storage.topic` stores connector and task status. All three are created in Kafka, are compacted, and are replicated. They are what let a restarted or rebalanced worker recover exactly where the cluster left off.

Q: Why is `config.storage.topic` configured with a single partition while the others have many?
A: Configuration is a small, totally-ordered log where order matters for the whole cluster, so it uses one partition to guarantee a single consistent sequence of config updates. `offset.storage.topic` and `status.storage.topic` carry far more, key-partitionable traffic, so they get multiple partitions for throughput. All three are log-compacted so only the latest value per key is retained.

Q: What does the worker `group.id` setting do in distributed mode, and what must you avoid?
A: `group.id` names the Connect cluster; all workers sharing it form one coordinated cluster that shares connectors, tasks, and internal topics. You must not reuse the same `group.id` for two clusters that should be independent, and it must not collide with a consumer `group.id`, or the group coordinator will mix unrelated members. Each separate Connect cluster gets its own unique `group.id`.

Q: How does Connect parallelise the work of a single connector?
A: The connector reports a desired number of tasks and a way to split its work (for a source, dividing input partitions; for a sink, the topic partitions it consumes). The framework instantiates up to `tasks.max` task instances and distributes them across the available workers. Throughput scales with task count, bounded by how finely the source or topic can actually be partitioned.

Q: What is `tasks.max` and why can setting it high not always help?
A: `tasks.max` is the upper bound on how many tasks a connector may create. The connector will only create as many tasks as it has independent work units, so for a sink the effective parallelism is capped at the number of partitions in the consumed topics. Setting `tasks.max=20` on a 4-partition topic still yields only 4 active sink tasks.

Q: What triggers a rebalance in a distributed Connect cluster?
A: A rebalance is triggered by membership or assignment changes: a worker joining or leaving, a connector being created, deleted, paused, resumed, or reconfigured, or a task count change. During a rebalance the cluster recomputes which worker owns each connector and task. Frequent rebalances are a smell that workers are flapping or connectors are being reconfigured too often.

Q: What problem does the incremental cooperative rebalancing protocol solve compared with eager rebalancing?
A: Eager rebalancing stopped all connectors and tasks across the cluster on every membership change, causing a global stop-the-world pause. Incremental cooperative rebalancing only revokes and reassigns the specific tasks that must move, leaving the rest running. This dramatically reduces downtime when, for example, one worker is added to a busy fund-data ingest cluster.

Q: What is a source connector?
A: A source connector pulls data from an external system into Kafka, for example reading database change events, files, or an API and producing them to Kafka topics. It is the ingestion direction of Connect. A JDBC or Debezium CDC connector feeding fund transactions into a topic is a typical source.

Q: What is a sink connector?
A: A sink connector consumes records from Kafka topics and writes them to an external system such as an object store, database, or search index. It is the egress direction of Connect. The Phase 4 lab uses an S3/MinIO sink connector to land Kafka records as objects in a bucket.

Q: For a source connector, where are offsets tracked, and what do they represent?
A: Source offsets are tracked by Connect itself in the `offset.storage.topic`, and they represent the connector's position in the external source (for example a database log sequence number or file byte position), not a Kafka consumer offset. Connect periodically commits these source offsets so that on restart the connector resumes from the last committed source position. This is distinct from how sinks track progress.

Q: For a sink connector, where are offsets tracked, and why is that different from a source?
A: A sink connector reads from Kafka, so its progress is an ordinary Kafka consumer-group offset stored in the `__consumer_offsets` topic, committed via the consumer group whose id is derived from the connector name. Because it is just a Kafka consumer, the sink's position is managed by Kafka's normal offset machinery rather than by Connect's `offset.storage.topic`. So sources use `offset.storage.topic` and sinks use consumer offsets.

Q: A teammate says "Connect stores all offsets in `offset.storage.topic`." Where are they wrong?
A: Only source-connector offsets live in `offset.storage.topic`; sink-connector progress is stored as standard Kafka consumer-group offsets in `__consumer_offsets`. Conflating the two leads people to look in the wrong place when diagnosing a stuck sink or trying to reset its position. The direction of the connector determines where to look.

Q: What is a converter in Kafka Connect?
A: A converter handles serialization and deserialization between Connect's internal in-memory record representation and the bytes on the Kafka topic. Source connectors use the converter to serialize records onto Kafka; sink connectors use it to deserialize topic bytes into Connect records. Common converters are `JsonConverter`, `AvroConverter`, and `ProtobufConverter`.

Q: Which two settings name the converters, and what do they control?
A: `key.converter` and `value.converter` name the classes used to serialize/deserialize the record key and value respectively. They control the on-the-wire format, for example `org.apache.kafka.connect.json.JsonConverter` or Confluent's Avro converter. They can be set globally on the worker and overridden per connector.

Q: What does `value.converter.schemas.enable=true` do for the `JsonConverter`?
A: With `schemas.enable=true`, `JsonConverter` wraps each record in an envelope containing both a `schema` and a `payload`, so the message carries its structure. Setting it to `false` produces plain schemaless JSON with just the data. Mismatching this between producer and a Connect sink is a classic cause of deserialization failures.

Q: What is an SMT (Single Message Transform)?
A: An SMT is a lightweight, per-record transformation applied in the Connect pipeline between the connector and the converter, configured declaratively in the connector config. Examples include masking a field, renaming a field, routing to a different topic, or inserting a timestamp. SMTs let you reshape data without writing a stream-processing job.

Q: What is the fundamental difference between a converter and an SMT?
A: A converter handles serialization format (turning structured records into bytes and back, e.g. Avro vs JSON), while an SMT transforms the structured record itself (e.g. dropping a field) before serialization. Converters operate at the boundary with Kafka; SMTs operate on the in-memory `SinkRecord`/`SourceRecord`. They solve different problems and are configured separately.

Q: In what order do an SMT chain and a converter execute for a source connector?
A: For a source, the connector produces a record, the SMT chain transforms it, and then the configured `value.converter`/`key.converter` serialize it onto the topic. SMTs run on the structured record before serialization. For a sink the order is reversed: the converter deserializes, then the SMTs transform the structured record before it reaches the sink.

Q: How do you chain multiple SMTs in a connector configuration?
A: You list aliases under the `transforms` config, then configure each by alias, for example `transforms=route,mask` plus `transforms.route.type=...` and `transforms.mask.type=...`. The transforms execute left to right in the order listed. Each transform's class and its parameters are namespaced under `transforms.<alias>.`.

Q: Give a fund-data example where an SMT is the right tool.
A: Masking or dropping a sensitive field like an investor's national ID before a transfer-agency feed lands in an analytics topic is a natural SMT job using `MaskField` or `ReplaceField`. Another is routing records to topic names by fund code with `RegexRouter`. These are per-record reshapings that do not need a full stream processor.

Q: Why are SMTs not a substitute for a stream-processing job like Kafka Streams?
A: SMTs are stateless, operate on one record at a time, and cannot join, aggregate, or window across records. They are designed for cheap per-message tweaks in the Connect pipeline, not for enrichment that needs other streams or accumulated state. For NAV aggregation or joining orders to positions you need Kafka Streams or ksqlDB, not an SMT.

Q: Where does a converter deserialization failure get reported, and why does converter choice matter for reliability?
A: A converter failure (bad bytes, schema mismatch) is a record-level error that, depending on `errors.tolerance`, either fails the task or routes the record to the DLQ. Because the converter is the first thing a sink applies, a single malformed message can stall an entire connector if tolerance and a DLQ are not configured. Choosing and configuring the converter correctly is therefore central to keeping the pipeline alive at 2am.

Q: For a source connector, what does an "offset" actually mean?
A: It is a connector-defined position in the upstream source, expressed as a key/value pair the connector understands, such as `{filename, position}` for a file source or a log sequence number for a CDC source. Connect persists this so the connector can resume from where it stopped. It is opaque to Connect itself and meaningful only to that connector.

Q: How and when does Connect commit source offsets?
A: Connect periodically flushes a source task's produced records and then commits the corresponding source offsets to `offset.storage.topic`, on an interval governed by `offset.flush.interval.ms`. Offsets are committed only after the matching records are safely written to Kafka, preserving at-least-once semantics. On restart the task reads the last committed offset and resumes from there.

Q: What does `offset.flush.interval.ms` control and what is the risk of setting it too high?
A: It controls how often source-connector offsets are flushed and committed to the offset topic. Setting it very high means more records may be reprocessed (replayed) after a crash, because the committed position lags further behind the actual produced position. Setting it very low increases commit overhead; you tune it to balance duplicate reprocessing against throughput.

Q: What does "resetting a source connector's offsets" do?
A: Resetting source offsets clears or rewrites the connector's stored position so it re-reads from the beginning (or a chosen point) of the source on next start. This can cause the source to re-emit data into Kafka, producing duplicates downstream. It is a deliberate, sometimes destructive operation used for reprocessing or recovery.

Q: Which REST endpoints manage a connector's offsets, and what is the precondition for changing them?
A: `GET /connectors/{name}/offsets` lists offsets, `PATCH /connectors/{name}/offsets` alters them, and `DELETE /connectors/{name}/offsets` resets them. The connector must be in the `STOPPED` state for `PATCH` or `DELETE` to be allowed. This guards against altering offsets while tasks are actively committing.

Q: Why can offsets only be altered when a connector is in the `STOPPED` state?
A: When stopped, the connector's tasks are shut down and no longer committing offsets, so there is no race between an external offset edit and a running task's own commits. If tasks were live, a `PATCH` could be immediately overwritten or produce inconsistent state. Requiring `STOPPED` makes offset surgery deterministic and safe.

Q: How does resetting a sink connector's offsets differ in mechanism from a source?
A: A sink's progress is a Kafka consumer-group offset, so resetting it means rewinding that consumer group's committed positions on the topics it reads. This is conceptually like resetting any consumer group, exposed through the same `/offsets` endpoints in modern Connect. Rewinding a sink causes it to re-consume and re-write already-delivered records to the external system, so downstream idempotency matters.

Q: A CDC source connector was rebuilt with new config and is now re-reading the entire database. What likely happened to its offsets?
A: Its stored source offset was probably lost or its offset key changed, so Connect found no committed position and the connector started from the snapshot/beginning. This commonly happens when a connector is deleted and recreated with a different name, since the offset key includes the connector name. Preserving the name and the `offset.storage.topic` is essential to avoid a full re-snapshot.

Q: What is `errors.tolerance` and what are its two values?
A: `errors.tolerance` controls whether per-record processing errors fail the task or are tolerated. The default `none` fails the task on the first error; `all` reports the error (log and/or DLQ) and continues processing subsequent records. Production fund pipelines typically set it to `all` so one poison message does not halt the connector.

Q: With `errors.tolerance=none`, what happens when a single malformed record arrives?
A: The task fails immediately and moves to the `FAILED` state, stopping all processing for that task until it is restarted. The bad record blocks progress because the default is to treat any error as fatal. This is why a single malformed market-data message can stall an entire connector at 2am if tolerance is left at the default.

Q: Which config keys enable a dead-letter queue and what does each do?
A: `errors.deadletterqueue.topic.name` names the DLQ topic to which failed records are routed, and it only takes effect when `errors.tolerance=all`. `errors.deadletterqueue.context.headers.enable=true` adds diagnostic headers to each dead-lettered record. `errors.deadletterqueue.topic.replication.factor` sets the DLQ topic's replication factor, defaulting to `3`.

Q: Does a DLQ work for both source and sink connectors?
A: No — the dead-letter queue feature applies only to sink connectors. It captures records that fail in the sink's converter, transforms, or the sink task itself. Source-connector errors are handled by tolerance, logging, and retries, but not by a Connect DLQ.

Q: What must `errors.tolerance` be set to for the DLQ to receive records, and why?
A: It must be `all`; with the default `none`, the task fails on the first error and never gets the chance to route the record to the DLQ. The DLQ is the "tolerated error" destination, so tolerance must permit the connector to keep running past the failure. Setting a DLQ topic name without `errors.tolerance=all` produces no dead-lettered records.

Q: What diagnostic headers does Connect add when `errors.deadletterqueue.context.headers.enable=true`?
A: It adds headers prefixed `__connect.errors.`, including `__connect.errors.topic`, `__connect.errors.partition`, `__connect.errors.offset`, `__connect.errors.connector.name`, `__connect.errors.task.id`, `__connect.errors.stage`, `__connect.errors.class.name`, and `__connect.errors.exception.message`. These let you trace a dead-lettered record back to its exact source position and failing stage. Without them the DLQ record is just opaque failed bytes.

Q: You open a DLQ record and read the `__connect.errors.exception.message` and `__connect.errors.stage` headers. What do they tell you?
A: `__connect.errors.exception.message` gives the exception text explaining why the record failed (for example a schema or deserialization error), and `__connect.errors.stage` identifies which pipeline stage failed, such as the value converter, a transformation, or the sink put. Together they pinpoint both the cause and the location of the failure. That is usually enough to decide whether the fix is a converter change, an SMT bug, or upstream bad data.

Q: In the Phase 4 lab you send a wrong-schema "poison" message through the source topic. Where should it end up and how do you prove it?
A: With `errors.tolerance=all` and a DLQ configured, the bad record should be parked in the dead-letter topic rather than crashing the sink. You prove it by consuming the DLQ topic and inspecting the record plus its `__connect.errors.*` headers, which name the original topic/partition/offset and the failing stage. The connector should remain `RUNNING` throughout.

Q: What is the default replication factor for a DLQ topic, and why might you change it?
A: The default `errors.deadletterqueue.topic.replication.factor` is `3`. In a small lab cluster with only one broker you must lower it to `1`, or DLQ topic creation fails because there are not enough brokers to satisfy the factor. In production you keep it at three for durability of the failed records.

Q: A DLQ topic fails to auto-create in your single-broker MinIO lab. What is the first thing to check?
A: Check `errors.deadletterqueue.topic.replication.factor`; its default of `3` cannot be satisfied by one broker, so creation fails with a replication-factor error. Setting it to `1` for the lab resolves it. More generally, verify the worker has permission to create topics and that auto-create is allowed.

Q: What do `errors.log.enable` and `errors.log.include.messages` control?
A: `errors.log.enable=true` makes Connect write failed-record errors to the worker log, and `errors.log.include.messages=true` additionally logs the record's contents and metadata for diagnosis. Both default to `false`. The message flag is powerful for debugging but can leak sensitive payloads, so be cautious with it on investor-data pipelines.

Q: Why might `errors.log.include.messages=true` be risky in a fund-administration context?
A: It writes the actual record contents into the worker logs, which can expose PII or confidential trade data (investor IDs, holdings, NAV figures) to anyone with log access. Under data-protection and DORA-style controls that may be a compliance breach. Prefer the DLQ with restricted access, or scrub fields with an SMT before logging.

Q: What do `errors.retry.timeout` and `errors.retry.delay.max.ms` do?
A: `errors.retry.timeout` is the total time Connect will spend retrying a failed operation before giving up, with `0` (the default) meaning no retries. `errors.retry.delay.max.ms` caps the exponential backoff between those retries. They are useful for transient failures like a brief network blip to the sink system, distinct from poison-record handling.

Q: How does Connect's error handling distinguish a transient error from a poison record?
A: Retries (`errors.retry.timeout`/`errors.retry.delay.max.ms`) address transient, retryable failures such as a temporary connection drop to the sink. Tolerance plus DLQ (`errors.tolerance=all` with a DLQ topic) address deterministic per-record failures that will never succeed on retry, like a malformed schema. A robust connector configures both layers.

Q: Why is a DLQ preferable to simply setting `errors.tolerance=all` with logging only?
A: With logging only, the bad records are silently dropped from the pipeline and exist solely as log lines, so they cannot be reprocessed and are easy to lose. A DLQ preserves the actual failed records on a durable Kafka topic where they can be inspected, alerted on, and replayed after a fix. For regulated data you generally must not silently discard records, making a DLQ the correct pattern.

Q: After fixing a schema bug, how do you reprocess records sitting in a DLQ?
A: You consume the DLQ topic and re-publish the now-valid records back onto the source topic (or feed them through a corrected pipeline), since the DLQ retains the original payloads. The `__connect.errors.*` headers help you understand and, if needed, target specific failures. Reprocessing must account for idempotency so replays do not duplicate downstream effects.

Q: What does pausing a connector do?
A: `PUT /connectors/{name}/pause` stops the connector's tasks from processing records but keeps the tasks and their assignments allocated on the workers. It is a quick, lightweight halt intended for short interruptions, after which `resume` restarts processing almost immediately. Offsets and assignments are retained.

Q: What does resuming a paused connector do?
A: `PUT /connectors/{name}/resume` returns a paused connector to the `RUNNING` state so its already-allocated tasks begin processing records again from their committed offsets. Because the tasks were never deallocated, resumption is fast. It is the natural counterpart to `pause`.

Q: What does the newer `STOPPED` state do that `PAUSED` does not?
A: `PUT /connectors/{name}/stop` shuts the tasks down entirely and deallocates their resources, rather than just halting processing as `pause` does. This frees worker capacity, allows offset modification via the `/offsets` endpoints, and is the intended state for longer outages or maintenance. Restarting from `STOPPED` takes longer because tasks must be recreated.

Q: When would you choose `pause` over `stop`, and vice versa?
A: Choose `pause` for a brief, planned interruption where you want instant resumption and to keep tasks warm, such as a short downstream maintenance window. Choose `stop` for longer outages, to free worker resources, or when you need to edit offsets, since offset changes require the `STOPPED` state. The trade-off is resume speed versus resource release and offset access.

Q: What does the connector restart endpoint do and what is its URL?
A: `POST /connectors/{name}/restart` restarts the connector instance and, optionally, its tasks. By default it restarts only the `Connector` object, not the tasks, unless you pass query parameters. It is used to recover from a `FAILED` connector without recreating it.

Q: What do the `includeTasks` and `onlyFailed` query parameters on the restart endpoint do?
A: `includeTasks=true` restarts the task instances as well as the connector (default `false` restarts only the connector); `onlyFailed=true` restarts only the instances currently in `FAILED` state (default `false` restarts all). You typically use `POST /connectors/{name}/restart?includeTasks=true&onlyFailed=true` to surgically restart just the broken tasks. This avoids needlessly disrupting healthy tasks.

Q: A single task is in `FAILED` state but the connector and other tasks are `RUNNING`. What is the least disruptive recovery?
A: Restart only that task with `POST /connectors/{name}/restart?includeTasks=true&onlyFailed=true`, which targets the failed instances and leaves healthy tasks untouched. This is far less disruptive than deleting and recreating the connector or restarting all tasks. First, though, read the task's status/trace to understand why it failed, so you do not just loop on the same poison condition.

Q: Why is restarting a failed task not always enough to fix it?
A: If the failure is deterministic — a poison record, a persistent schema mismatch, or a permanent downstream outage — restarting just replays the same record and fails again. Restart fixes transient failures; deterministic failures need a config change, a DLQ to route the bad record past, or fixing the upstream data. Always inspect the failure trace before restarting in a loop.

Q: How do you check the current status of a connector and its tasks?
A: Call `GET /connectors/{name}/status`, which returns the connector's state, the worker hosting it, and a list of each task's state and, for failures, the error trace. States include `RUNNING`, `PAUSED`, `STOPPED`, `FAILED`, and `UNASSIGNED`. This is the first call when triaging a stalled or failed connector.

Q: What does a task in `UNASSIGNED` state mean?
A: `UNASSIGNED` means the task exists in the cluster's plan but is not currently assigned to any worker, typically during or just after a rebalance, or when there is insufficient worker capacity. It usually resolves once the rebalance settles. If it persists, check whether workers are flapping or whether the cluster is overloaded.

Q: What is the difference between deleting and recreating a connector versus restarting it?
A: Restarting reuses the existing connector config and, for sources, its stored offsets, so it resumes where it left off. Deleting and recreating wipes the in-memory connector and, if the name or offset key changes, may lose offset continuity and trigger a full re-read. Prefer restart for recovery; reserve delete/recreate for genuine config rebuilds, and preserve the connector name to keep source offsets.

Q: When you pause a connector, what happens to its committed offsets?
A: Nothing — committed offsets are retained exactly as they were, because pausing only halts processing without deallocating tasks or rewinding position. On resume, tasks continue from those same committed offsets. Pause is offset-neutral, which is precisely why it is safe for short interruptions.

Q: How is the consumer group for a sink connector named, and why does that matter for offset operations?
A: A sink connector's consumer group id is derived from the connector name (conventionally `connect-<connector-name>`). This matters because the sink's progress lives in that group's committed offsets, so renaming the connector changes the group and loses progress continuity. It is also how you locate the right group when inspecting or resetting sink offsets directly.

Q: How would you size a Connect cluster's worker count for a stated connector load?
A: Estimate total task count across all connectors (bounded by source/topic partitioning), the per-task throughput and resource footprint (CPU, memory, network), then provision enough workers so peak load fits with headroom and at least one worker can fail without overload. A common heuristic is to ensure N-1 workers can carry the full task set. You also account for converter cost (Avro deserialization is heavier than JSON) and SMT overhead.

Q: For a sink connector consuming a 12-partition topic, what is the maximum useful number of tasks?
A: Twelve — a sink task is a Kafka consumer, and consumer parallelism is capped at the partition count, so at most 12 tasks can each own a distinct partition. Setting `tasks.max` above 12 leaves the extra tasks idle. To exceed 12-way parallelism you must repartition the topic with more partitions.

Q: When sizing, why does converter choice affect worker resourcing?
A: Heavier serialization formats consume more CPU per record: Avro/Protobuf converters do schema lookups and binary encode/decode, while a schemaless JSON converter is cheaper. A high-throughput connector using Avro therefore needs more CPU headroom per task than the same load on JSON. Sizing must factor the chosen converter, not just record count.

Q: If a worker dies in a 3-worker distributed cluster, what is the failure-recovery behaviour?
A: The remaining two workers detect the membership change and a rebalance reassigns the dead worker's connectors and tasks across the survivors, resuming from committed offsets. With cooperative rebalancing only the affected tasks move, so unaffected tasks keep running. Provided you sized so N-1 workers can carry the load, throughput continues, possibly degraded until the worker is replaced.

Q: What is the risk of running a Connect cluster with no spare worker capacity?
A: If every worker is at capacity and one fails, the survivors cannot absorb the reassigned tasks, so tasks may lag, fail, or stay `UNASSIGNED`, and a cascading overload can take down the cluster. Sizing for N-1 survivability is the standard guard. For a NAV feed that must not stall, you provision headroom deliberately.

Q: How do you create or update a connector via REST?
A: `POST /connectors` with a JSON body containing `name` and a `config` object creates it, while `PUT /connectors/{name}/config` with just the config object creates-or-updates it idempotently. The `PUT` form is convenient for declarative deployments since it applies the desired config regardless of current state. Both trigger a rebalance to (re)assign tasks.

Q: What does `GET /connector-plugins` return and why is it useful?
A: It lists the connector and transform plugin classes installed on the worker, which is how you confirm a sink or source connector class is actually on the classpath before referencing it. If your `connector.class` is missing here, deployment will fail with a class-not-found-style error. It is the first check when a connector config is rejected as an unknown class.

Q: A `POST /connectors` request is rejected because the connector class cannot be found. What is the first thing to check?
A: Verify the connector plugin JARs are installed in the worker's `plugin.path` and that the class appears in `GET /connector-plugins`. Connect only loads plugins from configured plugin directories, so a missing or misplaced JAR yields a class-not-found rejection. Confirm the exact `connector.class` string matches the installed plugin's class name.

Q: What is `plugin.path` and why does it matter for isolation?
A: `plugin.path` is the worker config listing directories where connector, converter, and transform plugins live, each in its own isolated classloader. This isolation prevents dependency conflicts between plugins (for example two connectors needing different versions of a library). Misconfiguring it is a common reason connectors fail to load.

Q: In the Phase 4 lab, what is the role of MinIO, and what does the S3 sink connector do with it?
A: MinIO is an S3-compatible object store standing in for Azure Blob/ADLS or AWS S3, serving as the sink target. The S3/MinIO sink connector consumes records from a Kafka topic and writes them as objects (files) into a MinIO bucket, exercising the egress half of Connect. It lets you practise object-store landing without a cloud account.

Q: Why does an object-store sink connector typically buffer records before writing?
A: Object stores favour fewer, larger objects over many tiny ones, so the sink batches records and flushes a file when a size, record-count, or time threshold is met (a `flush.size`-style setting). This reduces object count and request overhead and improves read efficiency downstream. The trade-off is added latency before data appears as an object.

Q: How does an object-store sink connector usually achieve exactly-once-style output despite at-least-once delivery?
A: It names output files deterministically from the topic, partition, and Kafka offset range, so a replay after failure overwrites the same files rather than creating duplicates. This makes re-delivery idempotent at the object level. Understanding this is key when you rewind a sink's offsets and worry about duplicate landing.

Q: What does it mean that source-connector delivery is "at-least-once" by default?
A: Because offsets are committed after records are produced to Kafka, a crash between producing and committing causes those records to be re-produced on restart, yielding possible duplicates but no loss. Consumers must therefore tolerate duplicates unless exactly-once support is explicitly enabled and supported by the connector. This is the standard guarantee you design around.

Q: How does a sink connector commit offsets relative to writing to the external system?
A: A well-behaved sink writes (or durably stages) records to the external system first and only then commits the corresponding Kafka consumer offsets, preserving at-least-once delivery. If it crashes after writing but before committing, those records are re-delivered and re-written on restart. This ordering is why idempotent or deterministic writes matter for sinks.

Q: A connector shows `RUNNING` but no data is landing in the sink. What are the first things to check?
A: Confirm the source topic actually has new records and the consumer group is not already at the log end, check for buffering thresholds that have not yet flushed (`flush.size`/rotation interval), and inspect task-level status/logs for silent errors or a stuck task. Also verify the topic name and any `RegexRouter` SMT are routing as expected. A `RUNNING` connector with empty input or an unmet flush threshold looks identical to a broken one at the topic level.

Q: A sink task fails with a deserialization error on every record. What configuration mismatch is most likely?
A: The `value.converter` (or `key.converter`) on the sink does not match how the data was serialized — for example using `JsonConverter` against Avro-encoded bytes, or `schemas.enable` set inconsistently. The converter is the first thing the sink applies, so a format mismatch fails every record. Align the sink's converter and schema settings with the producer's, then route any stragglers to a DLQ.

Q: Why can a single malformed market-data message stall an entire connector, and how do you prevent it?
A: With the default `errors.tolerance=none`, the first malformed record throws, the task goes `FAILED`, and processing stops for that task until manual intervention. You prevent it by setting `errors.tolerance=all` and configuring a DLQ so the bad record is parked with diagnostic headers while good records keep flowing. This is exactly the 2am scenario the lesson warns about.

Q: How would you design failure handling for a CDC source feeding fund transactions into Kafka?
A: Configure retries for transient database/network issues (`errors.retry.timeout` greater than `0` with bounded backoff), keep the connector name and `offset.storage.topic` stable so restarts resume from the log position rather than re-snapshotting, and monitor connector/task status for `FAILED` states. Since sources lack a DLQ, you rely on tolerance, logging, and alerting plus upstream data quality. Document that recovery resumes from the last committed source offset.

Q: How would you design failure handling for the sink half of that same pipeline (writing to an object store)?
A: Set `errors.tolerance=all`, configure a DLQ with `errors.deadletterqueue.context.headers.enable=true` for poison records, set the DLQ replication factor appropriately, and ensure deterministic file naming so replays are idempotent. Add retries for transient store outages and monitor consumer lag and task status. This pairs converter/SMT failures into the DLQ while keeping the sink running.

Q: Why is preserving the connector name important when migrating or rebuilding a connector?
A: Source offsets are keyed partly by connector name in `offset.storage.topic`, and a sink's consumer group is derived from the name, so changing the name severs offset continuity. The result is a full re-read for a source or re-consumption for a sink, causing duplicate processing. Keeping the name (and internal topics) intact preserves exactly-where-it-was recovery.

Q: What is the operational consequence of frequent connector reconfiguration in a busy cluster?
A: Each config change triggers a rebalance; with cooperative rebalancing only affected tasks move, but very frequent changes still churn assignments and can disrupt throughput. Batching config changes and avoiding needless redeploys keeps the cluster stable. Frequent unexplained rebalances in logs are a signal to investigate flapping workers or automation re-applying configs.

Q: How do you monitor a Connect cluster's health beyond the REST status endpoint?
A: Scrape JMX/metrics for connector and task state counts, source-record and sink-record rates, error/DLQ rates, rebalance frequency, and consumer lag for sink connectors. Alert on any task entering `FAILED`, on rising DLQ throughput, and on growing sink lag. Combining metrics with `GET /connectors/{name}/status` gives both the aggregate and the per-connector picture.

Q: Why is rising consumer lag a key signal for a sink connector specifically?
A: Because a sink is a Kafka consumer, growing lag means it is falling behind the topic's produce rate, indicating under-provisioned tasks, a slow downstream system, or backpressure. For a source there is no consumer lag in the same sense, so lag is the sink-side throughput alarm. Persistent lag on a NAV-publishing sink is a direct delivery-SLA risk.

Q: What does it mean that the internal Connect topics are log-compacted?
A: Compaction retains only the latest value per key and removes superseded entries, which is correct for config, offset, and status topics where you only ever need the current state per key. It keeps these topics bounded in size despite continuous updates. If compaction were disabled, these topics would grow unbounded with stale history.

Q: Can two independent Connect clusters share the same internal topics, and what happens if they do by accident?
A: No — sharing `config.storage.topic`, `offset.storage.topic`, or `status.storage.topic` (typically via a shared `group.id` or topic names) makes them read each other's config and offsets, corrupting both clusters' state. Each cluster must have unique internal topic names and `group.id`. Accidental sharing manifests as connectors mysteriously appearing or vanishing across clusters.

Q: What replication factor should the Connect internal topics have in production and why?
A: They should be replicated across at least three brokers (replication factor `3`) because losing the only copy of config or offsets would lose the cluster's entire operational state and committed positions. These topics are the cluster's durable brain, so durability is non-negotiable. In a single-broker lab you drop the factor to `1` only because no alternative exists.

Q: What is the difference between a converter set on the worker versus on a connector?
A: A converter set in the worker config is the cluster-wide default applied to every connector that does not override it; a converter set in a connector's config overrides the default for just that connector. This lets a mostly-Avro cluster host one JSON connector by overriding `value.converter` on that connector alone. Per-connector overrides are how you mix formats in one cluster.

Q: Why might you set `key.converter` differently from `value.converter`?
A: Keys are often simple strings or primitives while values are rich structured records, so a common pattern is `StringConverter` for keys and `AvroConverter` (or JSON) for values. Decoupling them avoids forcing a schema onto a trivial key. Mismatching the key converter with the actual key bytes is, like the value case, a source of deserialization failures.

Q: How does an SMT like `RegexRouter` interact with a sink connector's topic-to-destination mapping?
A: `RegexRouter` rewrites the record's topic name in the pipeline, which a sink connector then uses to decide the destination (for example the object-store path or table). So routing fund records into per-fund destinations can be done by transforming the topic name rather than running multiple connectors. Misjudging the regex can silently send records to the wrong destination, so test the routing on sample data.

Q: Why are SMTs evaluated per record and what performance implication does that carry?
A: Each SMT runs synchronously on every record passing through the task, so a chain of transforms adds CPU cost proportional to throughput. Heavy or many SMTs can become the task's bottleneck and reduce maximum records-per-second. When sizing, factor SMT cost into per-task throughput, especially for high-volume tick or order streams.

Q: What is the difference between `errors.tolerance` and a connector simply not having a DLQ?
A: `errors.tolerance` decides whether the task fails or continues on a record error; the DLQ decides where tolerated failures go. With `tolerance=all` but no DLQ, failed records are skipped (and optionally logged) but not preserved; with a DLQ they are durably captured. Tolerance is the gate, the DLQ is the catch-net — you usually want both.

Q: In a regulated fund context, why is silently skipping bad records (tolerance=all, no DLQ, no logging) usually unacceptable?
A: Dropping records without trace breaks data lineage and auditability, both of which regulators and frameworks like DORA expect for financial data flows. A skipped NAV or order record could go unnoticed and unrecoverable. The compliant pattern routes failures to a DLQ where they are retained, alertable, and replayable, preserving an audit trail.

Q: What information does the connector-level status (versus task-level) in `GET /connectors/{name}/status` give you?
A: The connector-level entry reports the overall connector state and which worker owns the `Connector` instance, while the `tasks` array reports each task's individual state, worker, and any error trace. A connector can be `RUNNING` while individual tasks are `FAILED`, so you must read both levels. The task entries are where you find the actual failure stack traces.

Q: A connector is `RUNNING` but all its tasks are `FAILED`. What does that combination mean and what do you do?
A: It means the connector object is healthy but every task hit a fatal error, so no data is moving despite the top-level `RUNNING`. Read each task's trace in the status output, fix the root cause (often a converter mismatch, missing dependency, or downstream outage), then restart the failed tasks with `restart?includeTasks=true&onlyFailed=true`. Never judge health from the connector state alone.

Q: How does Connect's distributed mode decide which worker handles REST requests and connector coordination?
A: Workers form a group and elect a leader that performs assignment and config coordination, but any worker's REST endpoint can accept requests and will forward to the leader as needed. So clients can hit any worker URL. This is why a load balancer in front of the workers is a common deployment pattern.

Q: Why should the architect treat Connect cluster sizing and failure design as a first-class deliverable, not a one-time wiring task?
A: Because connectors, converters, transforms, and error handling are where streaming integrations live or die operationally — a misconfigured converter or absent DLQ stalls a whole pipeline, and an under-sized cluster cannot survive a worker loss. The architect owns the capacity model (workers/tasks), the failure-recovery behaviour, and the error/DLQ policy across the fund estate. Getting these right is what keeps NAV, EMT/EPT, and transfer-agency feeds flowing under failure.

Q: On which port does the Connect REST API listen by default, and how do you change it?
A: The Connect REST API listens on `8083` by default on every worker in the cluster. You change it with the worker config `listeners` (for example `listeners=http://0.0.0.0:8083` or an `https://` listener), and `rest.advertised.host.name`/`rest.advertised.port` control the address other workers and clients are told to use. In a fund estate you typically front the workers with a load balancer and terminate TLS, since any worker can serve any REST call.

Q: How do you validate a connector config before deploying it, and what is the exact endpoint?
A: You `PUT` the proposed config to `PUT /connector-plugins/{connector-class}/config/validate` with the config JSON as the body, and Connect returns each property with any validation errors and recommended values without creating the connector. This is a dry run that catches missing required keys or bad values up front. It is far safer than `POST /connectors` straight to production, where a bad config trips a rebalance and a `FAILED` connector.

Q: How do you list every connector currently running on a Connect cluster from the command line?
A: Call `GET /connectors`, for example `curl -s http://localhost:8083/connectors`, which returns a JSON array of connector names. Add `?expand=status&expand=info` to get each connector's state and config in one call rather than looping over `GET /connectors/{name}/status`. This is the first orientation command when you inherit an unfamiliar cluster.

Q: What HTTP status does `POST /connectors` return on success, and what does `409 Conflict` mean?
A: A successful create returns `201 Created` with the connector's config and tasks in the body. A `409 Conflict` means the request cannot be served right now because a rebalance is in progress (or the cluster has no leader yet), so the worker rejects config changes until the group stabilises. The fix is usually to retry after a moment, not to change the config.

Q: A `POST /connectors` returns `400 Bad Request` with a list of config errors. What is your first action?
A: Read the returned error map — Connect lists each offending property and why it failed (missing required value, unparseable type, unknown enum) — and fix those specific keys rather than guessing. Running the same body through `PUT /connector-plugins/{class}/config/validate` gives the same diagnostics structured per-field. A `400` is a config-shape problem, distinct from a `409` (rebalance) or a runtime `FAILED` task.

Q: What is the `storage.class` you would set for an S3/MinIO sink, and what does it abstract?
A: For the Confluent S3 sink you set `storage.class=io.confluent.connect.s3.storage.S3Storage`, which is the storage backend abstraction the connector writes objects through. It is what lets the same connector target AWS S3 or an S3-compatible store like MinIO by only changing `store.url` and credentials. Pointing `store.url` at the MinIO endpoint is how the Phase 4 lab reuses the AWS connector against a local store.

Q: Which setting points the S3 sink at MinIO instead of AWS, and what else must match?
A: `store.url` (e.g. `store.url=http://minio:9000`) directs the connector to the MinIO endpoint, and you must also supply S3-style credentials and the bucket via `s3.bucket.name`. Path-style access is typically required for MinIO, so the client must not assume virtual-hosted bucket URLs. Get the endpoint or credentials wrong and the sink task fails on connection or `403`, not on a Kafka error.

Q: What does `format.class` control on the S3 sink, and name two common values?
A: `format.class` controls the on-disk object format written to the store, independent of the Kafka wire format the converter handled. Common values are `io.confluent.connect.s3.format.json.JsonFormat`, `io.confluent.connect.s3.format.parquet.ParquetFormat`, `io.confluent.connect.s3.format.avro.AvroFormat`, and `io.confluent.connect.s3.format.bytearray.ByteArrayFormat`. For an analytics landing zone you would usually pick Parquet so downstream NAV queries read columnar files.

Q: What is the difference between the S3 sink's `format.class` and the worker's `value.converter`?
A: `value.converter` deserializes the Kafka topic bytes into Connect's in-memory record; `format.class` then re-encodes that record into the object file written to the store. So you could consume Avro from Kafka (`AvroConverter`) and land Parquet in the bucket (`ParquetFormat`). They are two independent encode/decode stages, which is why a converter mismatch fails before `format.class` is ever reached.

Q: What does `flush.size` do on the S3 sink and what is its default?
A: `flush.size` is the number of records the sink buffers per partition before committing them as one object to the store; its default is `1000`. A larger value yields fewer, bigger files (better for object stores and columnar reads) at the cost of higher latency before data appears. Setting it to `1` in a lab makes each record its own object so you can see writes immediately.

Q: How do `rotate.interval.ms` and `rotate.schedule.interval.ms` differ on the S3 sink?
A: `rotate.interval.ms` rolls a file when the spread of record timestamps within the open file exceeds the interval, so it is driven by event-time and is deterministic for exactly-once-style naming. `rotate.schedule.interval.ms` rolls files on wall-clock time regardless of record timestamps and defaults to `-1` (disabled). Because the wall-clock variant is non-deterministic, it can break the deterministic-file-naming guarantee on replays.

Q: Why does the S3 sink need a `partitioner.class`, and what does `DefaultPartitioner` do?
A: The partitioner decides the object key/path layout under the bucket, which shapes how data is laid out for downstream readers. `io.confluent.connect.storage.partitioner.DefaultPartitioner` lays records out by Kafka topic and partition (e.g. `topics/<topic>/partition=<n>/`). A `TimeBasedPartitioner` instead writes `dt=YYYY-MM-dd` style paths, which is what you want for date-partitioned NAV history that query engines can prune.

Q: For deterministic, idempotent S3 object naming on replay, which partitioner and rotation settings matter?
A: Use a deterministic partitioner (Default or a time-based one keyed on record time, not wall clock) and rotate by `flush.size` and `rotate.interval.ms` rather than `rotate.schedule.interval.ms`. Then file names encode topic, partition, and the starting Kafka offset, so a re-delivered batch overwrites the same object instead of creating a duplicate. Mixing in wall-clock rotation reintroduces non-determinism and can produce duplicate objects after a crash.

Q: What does the `STOPPED` state allow that lets you safely run `DELETE /connectors/{name}/offsets`?
A: `DELETE /connectors/{name}/offsets` (an offset reset) is only permitted when the connector is `STOPPED`, because stopping deallocates the tasks so nothing is concurrently committing offsets. With tasks live, a reset could race with their commits and leave inconsistent state. So the workflow is `PUT /connectors/{name}/stop`, then `DELETE` or `PATCH` on `/offsets`, then bring it back with `POST /connectors/{name}/restart` (not `resume`, which only un-pauses a `PAUSED` connector).

Q: After resetting a source connector's offsets, how do you actually get it running again from `STOPPED`?
A: From `STOPPED` you return the connector to running by reconfiguring it (`PUT /connectors/{name}/config`) or by issuing `POST /connectors/{name}/restart`; `resume` only applies to a `PAUSED` connector, not a `STOPPED` one. The connector then reads the freshly written offset and proceeds from there. Confusing `resume` (un-pause) with restarting a stopped connector is a common operational slip.

Q: How do you read a source connector's current offsets via REST, and when is the value trustworthy?
A: `GET /connectors/{name}/offsets` returns the connector's stored source offsets as partition/offset pairs. The value is most trustworthy when the connector is `STOPPED`, because a running task may have produced records whose offsets have not yet been flushed (`offset.flush.interval.ms`), so the reported value can lag the true position. For offset surgery you stop first, then read, then patch.

Q: You want a source to re-read from a specific point rather than the very beginning. Which endpoint and precondition?
A: Use `PATCH /connectors/{name}/offsets` with a body specifying the new source partition/offset values, which requires the connector to be `STOPPED`. Unlike `DELETE` (full reset to the source's default start), `PATCH` rewinds or fast-forwards to a precise position you supply. This is how you replay just the affected window after, say, a bad transform was deployed for one hour.

Q: How do you reset a sink connector's position without using the Connect offsets endpoints?
A: Because a sink's progress is an ordinary consumer-group offset, you can rewind it with `kafka-consumer-groups.sh --reset-offsets --group connect-<connector-name> --topic <t> --to-earliest --execute` while the connector is stopped. Modern Connect also exposes `/offsets` for sinks, but the consumer-group tool is the underlying mechanism. Either way the sink re-consumes and re-writes, so downstream idempotency must hold.

Q: A sink task logs `No such file or directory` or a `403` against the object store, not a Kafka error. Where do you look first?
A: That is a sink-side connectivity/permissions failure, so check `store.url`, the bucket name (`s3.bucket.name`), credentials, and path-style access against MinIO — not the converter or topic. A `403` usually means bad keys or a missing bucket policy; a connection refusal means the endpoint or port is wrong. The Kafka half is fine; the failure is between the sink task and the store.

Q: A connector's status shows a task `FAILED` with a long Java stack trace in the `trace` field. How do you read it efficiently?
A: In `GET /connectors/{name}/status` each failed task carries a `trace` string; read the top exception type and message first (e.g. `DataException`, `SerializationException`, `ConnectException`), then the first stage frame to locate where it failed. The exception type tells you the category — converter/schema, transform, or sink I/O — so you fix that layer rather than guessing. Only then decide whether a restart or a config change is the remedy.

Q: A `SerializationException` or `DataException` appears at the converter stage on a sink. What is the likely root cause?
A: The sink's `value.converter` does not match how the records were serialized — classically `JsonConverter` reading Avro bytes that begin with the Schema Registry magic byte `0x00`, or `schemas.enable` set inconsistently between producer and sink. Because the converter is applied first, every record fails identically. Align the converter (and registry URL for Avro) with the producer, then DLQ any historical stragglers.

Q: How do you tell whether a stalled sink is blocked by an unmet flush threshold versus a real error?
A: Check `GET /connectors/{name}/status` — if all tasks are `RUNNING` with no `trace`, no data landing usually means buffered records have not hit `flush.size` or a rotation interval yet, so nothing is committed to the store. Compare the consumer-group lag: low lag plus buffered-but-not-flushed means a threshold issue, not a failure. Sending enough records or lowering `flush.size` confirms it by triggering a flush.

Q: How do you confirm a sink connector is actually consuming from Kafka and not idle?
A: Inspect its consumer group with `kafka-consumer-groups.sh --describe --group connect-<connector-name>`, which shows current offset, log-end offset, and lag per partition. Advancing current offset proves consumption; flat current offset with growing log-end proves it is stuck or paused. This ties the sink's observable progress to ordinary Kafka consumer mechanics.

Q: What is the `connect-distributed.sh` / `connect-standalone.sh` distinction at the command level?
A: `connect-distributed.sh worker.properties` starts a distributed worker that joins a cluster by `group.id` and stores state in Kafka topics; `connect-standalone.sh worker.properties connector1.properties ...` starts a single process that takes connector configs as files and stores offsets in a local file. Distributed is driven by REST after startup; standalone is driven by the property files passed in. Production fund pipelines always use the distributed launcher.

Q: In a standalone worker, which setting names the offset file and why does it matter for recovery?
A: `offset.storage.file.filename` (e.g. `/tmp/connect.offsets`) is where a standalone worker persists source offsets locally. It matters because if that file is on ephemeral storage and the host is replaced, the offset is lost and the source re-reads from the start. This single-file fragility is precisely why standalone is unsuitable for a regulated production source.

Q: Why must `key.converter` and `value.converter` settings be consistent across all workers in a cluster, and what if they are not?
A: They define the cluster-wide serialization defaults, and a connector's tasks may run on any worker after a rebalance, so divergent converter defaults mean a task could serialize/deserialize differently depending on where it lands. That produces intermittent, host-dependent deserialization failures that are maddening to diagnose. Keep worker configs identical and override per connector only in the connector config, which travels with the task.

Q: How does a per-connector converter override travel with a task during a rebalance, while a worker default does not?
A: A converter set in the connector config is stored in `config.storage.topic` and is part of the connector's definition, so whichever worker a task lands on applies the same override. A worker-level default lives only in that worker's local properties file, so it varies if workers differ. This is why mixing formats safely is done via per-connector overrides, not by editing individual workers.

Q: What does `InsertField` SMT do, and give a fund-data use for it on a source?
A: `org.apache.kafka.connect.transforms.InsertField` adds a field to each record, populated from a static value or record metadata like the Kafka topic, partition, offset, or timestamp. On a CDC source you might inject an ingestion timestamp or a source-system tag so every downstream NAV record carries provenance. It is a stateless per-record add, ideal for lineage stamping without a stream processor.

Q: How does the `Cast` SMT differ from a converter, using a numeric field as the example?
A: `org.apache.kafka.connect.transforms.Cast` changes a field's logical type within the structured record (e.g. cast a string `"100.5"` to a float), whereas a converter only changes how the whole record is serialized to bytes. Cast operates field-by-field on the in-memory record before serialization. Use it to coerce a mistyped quantity field, not to switch the topic between JSON and Avro.

Q: What does the `ValueToKey` plus `ExtractField` SMT pattern accomplish, and why is it common before a sink?
A: `ValueToKey` copies one or more fields from the value into the record key, and `ExtractField$Key` then reduces that key struct to a single primitive. Together they set a meaningful key (e.g. ISIN or fund code) so downstream partitioning, compaction, or upserts key on a business identifier. It is the standard recipe when the upstream produced records with null or unhelpful keys.

Q: A `RegexRouter` SMT is silently sending records to the wrong destination. How do you debug it?
A: `org.apache.kafka.connect.transforms.RegexRouter` rewrites the topic name via `regex` and `replacement`, and a too-greedy pattern or wrong capture group sends records to an unintended topic/path. Test the exact `regex`/`replacement` against sample topic names offline, and temporarily add an `InsertField` of the post-route topic or inspect the sink's object paths to see where records actually go. The fix is the pattern, not the connector class.

Q: What is a predicate in an SMT chain and why was it added to Connect?
A: A predicate (configured under `predicates` with types like `TopicNameMatches`, `HasHeaderKey`, `RecordIsTombstone`) lets you apply an SMT conditionally, by attaching `transforms.<alias>.predicate=<name>` and optionally `negate`. It was added so a single transform can fire only on records that match a condition rather than on every record. For example, mask a field only on records from the investor topic, leaving market-data records untouched.

Q: How do you drop Kafka tombstone records before a sink with an SMT predicate?
A: Configure a `RecordIsTombstone` predicate and attach it to a filter transform (`org.apache.kafka.connect.transforms.Filter`), so records with a null value are dropped rather than written. This prevents a sink that cannot represent deletes from erroring or writing empty objects. It is a clean, stateless way to handle compacted-topic deletes at the connector edge.

Q: How can a faulty SMT cause records to land in the DLQ, and which stage header confirms it?
A: An SMT that throws (e.g. `Cast` on a field that is not castable, or `ExtractField` on a missing field) raises a per-record error in the transform stage, which with `errors.tolerance=all` routes the record to the DLQ. The `__connect.errors.stage` header reads `TRANSFORMATION` (and `__connect.errors.class.name` names the SMT), confirming the transform, not the converter or sink, was at fault. That tells you to fix the SMT config or guard it with a predicate.

Q: Does an SMT failure on a source connector get routed to a DLQ?
A: No — DLQ routing applies only to sink connectors, so a source SMT failure is governed by `errors.tolerance` plus logging/retries, and with `tolerance=none` it fails the source task. There is no Connect dead-letter topic for source-side errors. This is why source data-quality and tested transforms matter more, since you cannot park bad source records in a DLQ.

Q: Why is `errors.tolerance=all` with no DLQ and no logging the worst of the three error policies for regulated data?
A: That combination silently skips failed records with no durable trace, no log line, and no replayable copy, so a dropped NAV or transaction record is invisible and unrecoverable, breaking lineage and audit obligations under DORA-style controls. At minimum you want the record preserved (DLQ) or recorded (`errors.log.enable=true`). Silent loss is the one outcome a regulated pipeline must never accept.

Q: How would you alert specifically on records entering the DLQ rather than on task failure?
A: Monitor the DLQ topic's produce rate (via JMX/consumer metrics or by tracking its log-end offset) and alert when it grows, since a `RUNNING` connector that is DLQ-ing every record shows no `FAILED` state at all. Task-failure alerts miss this because tolerance keeps the task running. A rising DLQ rate is the signal that good data is silently being diverted.

Q: What JMX metric family tells you a connector's task state counts at a glance?
A: Connect exposes worker-level metrics under `kafka.connect:type=connect-worker-metrics` including `connector-count`, `task-count`, `connector-failed-task-count`, and per-connector `connect-worker-metrics(connector=...)` gauges. Scraping these lets you alert when any `failed-task-count` is non-zero without polling REST per connector. They are the aggregate counterpart to `GET /connectors/{name}/status`.

Q: Which source-connector JMX metric most directly indicates throughput, and which one warns of buffering pressure?
A: `source-record-poll-rate` and `source-record-write-rate` (under `kafka.connect:type=source-task-metrics`) indicate how fast a source is polling and producing, your throughput signal. `source-record-active-count` shows records produced but not yet acknowledged/flushed; a steadily rising value warns of downstream Kafka backpressure or a too-large `offset.flush.interval.ms`. Together they separate "slow source" from "blocked on commit".

Q: For a sink, which metric pair distinguishes "consuming fine but slow to write" from "not consuming at all"?
A: `sink-record-read-rate` (records pulled from Kafka) versus `sink-record-send-rate`/put latency under `kafka.connect:type=sink-task-metrics`, combined with consumer-group lag. High read-rate but low send-rate with rising `put-batch-avg-time-ms` means the external store is the bottleneck; flat read-rate with rising lag means the sink is not consuming. This pinpoints whether to scale tasks or fix the downstream store.

Q: How do you compute the maximum useful `tasks.max` for a sink reading three topics with 4, 8, and 6 partitions?
A: A sink task is a Kafka consumer, so effective parallelism is capped by the total partitions it consumes across all assigned topics — here `4 + 8 + 6 = 18` — meaning at most 18 tasks each own distinct partitions. Setting `tasks.max` above 18 leaves the surplus tasks idle. To exceed 18-way parallelism you must add partitions to the source topics.

Q: A source connector with `tasks.max=10` only ever spawns 3 tasks. Is that a bug?
A: No — a source creates only as many tasks as it has independent work units, so a connector reading, say, 3 database tables or 3 file groups will produce 3 tasks regardless of a higher `tasks.max`. `tasks.max` is a ceiling, not a target. To get more parallelism you must give the source more partitionable work, not raise the number.

Q: When sizing for N-1 worker survivability, how do you translate that into a concrete worker count for 24 tasks?
A: Estimate each task's CPU/memory/network footprint, sum the load, then choose a worker count W such that any W-1 workers can carry all 24 tasks with headroom — e.g. if a worker comfortably hosts 8 tasks, you need at least 4 workers so that 3 survivors can still hold 24. You add margin for converter cost (Avro heavier than JSON) and SMT overhead. This guarantees a single worker loss does not overload the survivors.

Q: How does heavier converter choice change your worker count for the same record rate, concretely?
A: Avro/Protobuf converters do schema lookups and binary encode/decode, costing more CPU per record than schemaless JSON, so the same records-per-second needs more CPU headroom per task and thus possibly more workers. If JSON lets a worker host 10 tasks at a given rate, Avro might cap it at 6, pushing the worker count up. Sizing must start from the converter and SMT cost per record, not record count alone.

Q: Why can adding more workers fail to increase a single connector's throughput?
A: Throughput is bounded by the connector's task count, which is itself bounded by source/topic partitioning, so once tasks are spread one-per-worker, extra workers sit idle for that connector. Adding workers only helps if there are unassigned tasks or other connectors to host. To speed up one partition-bound connector you must repartition, not add workers.

Q: What does `config.storage.replication.factor` (and the offset/status equivalents) need to be in a single-broker MinIO lab, and why?
A: In a one-broker lab you must set `config.storage.replication.factor`, `offset.storage.replication.factor`, and `status.storage.replication.factor` to `1`, because the default `3` cannot be satisfied and the internal topics fail to create, so the worker will not start. The same one-broker limit forces `errors.deadletterqueue.topic.replication.factor=1`. In production all of these go to `3` for durability of the cluster's operational state.

Q: A distributed worker refuses to start, logging that it cannot create an internal topic. What is the most likely single-broker cause?
A: The internal-topic replication factors (`config/offset/status.storage.replication.factor`) default to `3`, which a single broker cannot satisfy, so topic creation fails and the worker aborts startup. Lowering them to `1` for the lab resolves it. The same class of error explains a DLQ topic that will not auto-create, and the fix is identical.

Q: Two Connect clusters accidentally created with the same `group.id` start stealing each other's connectors. What is the fix and the cleanup?
A: The shared `group.id` makes both sets of workers join one logical cluster and share `config/offset/status` topics, so connectors appear to migrate between them. The fix is to give each cluster a distinct `group.id` and distinct internal topic names, then redeploy. Cleanup means reconciling the corrupted config/offset state, since the merged history may have interleaved updates that need a clean re-create of affected connectors.

Q: Summarise the exact endpoint and state transitions to safely rewind a source and replay one bad hour, end to end.
A: Stop the connector with `PUT /connectors/{name}/stop`, read current offsets via `GET /connectors/{name}/offsets`, rewind precisely with `PATCH /connectors/{name}/offsets` (allowed only because it is `STOPPED`), then bring it back with `POST /connectors/{name}/restart` so it resumes from the patched position. Confirm via `GET /connectors/{name}/status` that tasks return to `RUNNING` and downstream consumers are idempotent to absorb the replayed hour. This sequence keeps the connector name and internal topics intact so no unintended full re-snapshot occurs.

Q: Why does object-store landing for a NAV feed pair naturally with a Parquet `format.class` and a time-based partitioner, in audit terms?
A: Writing `ParquetFormat` objects under `dt=YYYY-MM-dd` paths via a `TimeBasedPartitioner` gives a columnar, date-partitioned history that query engines can prune and that reads cheaply for point-in-time NAV reconstruction. The date partitioning also maps cleanly to retention and audit windows regulators expect, since you can express "keep N years" as a path prefix. Combined with deterministic offset-based file names, replays overwrite the same objects, so the audit trail stays consistent rather than accumulating duplicates.
